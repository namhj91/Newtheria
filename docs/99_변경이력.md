## ver.0.3.67(260429-debug데스크톱클릭드래그분리)
- 데스크톱에서 디버그 FAB 클릭 시 최소/최대화 토글이 간헐적으로 동작하지 않던 문제를 수정했다.
- FAB/헤더 드래그 로직에 이동 임계값(4px)을 도입해, 미세 흔들림은 클릭으로 처리하고 실제 이동이 있을 때만 드래그로 판정하도록 분리했다.
- 포인터 캡처 대상을 시작 핸들(FAB/헤더) 기준으로 저장/해제해, 데스크톱 마우스 입력에서 클릭 토글과 드래그 이동이 충돌하지 않도록 정리했다.

## ver.0.3.66(260429-debug플로팅드래그강화뷰버튼이관)
- 디버그 플로팅 메뉴의 드래그 핸들을 패널 헤더뿐 아니라 축소 상태의 설정(FAB) 버튼에도 확장해, 접힌 상태에서도 위치 이동이 가능하도록 개선했다.
- 기존 메인 화면에 있던 뷰 전환 버튼(기본/대화/맵제작/월드맵/전투) DOM을 완전히 제거하고, DEBUGMODE 플로팅 메뉴 내부에서만 생성·사용하도록 이관했다.
- 뷰 전환 버튼을 그라디언트/하이라이트/쉐도우 기반 스타일로 재디자인해 시인성과 조작감을 개선했다.

## ver.0.3.65(260429-debug플로팅메뉴뷰포인트탭)
- `main.html?DEBUGMODE=on` 진입 시 우하단에 설정 아이콘 형태의 디버그 플로팅 메뉴를 표시하고, 클릭 시 확장/축소 가능한 패널을 열도록 구성했다.
- 플로팅 메뉴 패널 헤더를 드래그 핸들로 지정해, 사용자가 원하는 위치로 이동할 수 있게 했다.
- 기존 메인 화면 하단의 개발용 뷰 전환 버튼(기본/대화/맵제작/월드맵/전투)을 디버그 메뉴의 `뷰포인트` 탭 안으로 이동시켜, 디버그 상태에서 한곳에서 제어하도록 정리했다.

## ver.0.3.54(260429-여정빌더페이지제거정리)
- 더 이상 사용하지 않는 여정 빌더 페이지(`journey_builder.html`)를 삭제했다.
- 여정 빌더 전용 스크립트(`assets/js/journey_builder.js`)와 스타일(`assets/css/journey_builder.css`)을 함께 제거해 미사용 자산을 정리했다.

## ver.0.3.53(260429-여정빌더월드맵제작뷰구축)
- `journey_builder.html` 2단계를 "세계 생성"에서 "월드맵 제작 뷰"로 전환하고, 프롤로그 직후 동일 페이지에서 월드맵 생성이 이어지도록 흐름을 조정했다.
- `assets/js/journey_builder.js`에 실제 헥스 타일 월드맵 제네레이션(시드 기반 지형 분류)과 캔버스 프리뷰 렌더 단계를 추가해 임베드/라우팅 없이 제작 결과를 확인할 수 있게 했다.
- `assets/css/journey_builder.css`에 월드맵 프리뷰 컨테이너 스타일을 추가했다.

## ver.0.3.52(260429-프롤로그후월드맵제작시퀀스)
- `main_view_router`에서 프롤로그 대화 종료 시 기본 뷰로 복귀하던 동선을 월드맵 뷰 진입으로 변경하고, 종료 직후 월드맵 제작 시퀀스를 자동 시작하도록 연결했다.
- 월드맵 뷰 본문을 플레이스홀더 문구 대신 4단계 제작 진행 UI(상태 문구 + 단계 리스트 + 수동 시작 버튼)로 교체해, 프롤로그 다음 흐름을 즉시 확인할 수 있게 구성했다.
- 제작 시퀀스는 단계별 `pending/running/done` 상태를 시간차로 갱신하며, 완료 시 “탐험 준비 완료” 상태 문구로 마무리되도록 정리했다.

## ver.0.3.51(260429-월드맵초기렌더고정정리)
- 월드맵 테스트 페이지에서 상호작용 이전에 캔버스가 비어 보이던 원인을 줄이기 위해, 월드 생성 직후 즉시 `final` 단계 렌더를 1회 예약하도록 고정했다.
- 사용되지 않던 생성 단계 자동재생 함수(`playGenerationStages`)와 관련 토큰 참조를 제거해 렌더 흐름을 단순화했다.
- 부팅 구간의 생성 단계 선택 이벤트 바인딩 위치를 정리해, 월드맵 화면 초기화 코드가 한 블록에서 읽히도록 구조를 정돈했다.

## ver.0.3.50(260429-절경타입별특이조건확장)
- 절경 현실성 필터를 확장해 `해식절벽/사구능선/밀림연못/빙식분지/협곡문/대협곡/대산맥/빙하장벽`에 타입별 특이 조건을 추가했다.
- 인접 지형 접촉 판정(`groupTouchesTerrain`)과 군집 외곽 복잡도 판정(`countGroupBorderEdges`) 유틸을 도입해, 단순 타일 구성 외에 형태·접경 조건까지 반영했다.
- 기존 폭포 단일 타일 승격 구조는 유지하면서, 다른 절경도 과장된 승격이 줄어들도록 전체 필터 강도를 보강했다.

## ver.0.3.49(260429-폭포군집내단일타일승격)
- 폭포(`S1`)는 강 군집 전체를 절경 처리하지 않고, 군집 내부에서 산맥계열과 인접한 단일 강 타일 1개만 절경으로 승격하도록 로직을 수정했다.
- `pickWaterfallScenicTile` 헬퍼를 추가해 폭포 후보 타일을 분리 선정하고, 일반 절경 타입과 동일한 승격 파이프라인에서 단일 타일 그룹으로 처리하도록 연결했다.
- 폭포 카탈로그의 군집 크기를 `4~20`으로 되돌려 “여러 강이 이어진 구간 중 포인트 1개 선정” 의도를 반영했다.

## ver.0.3.48(260429-절경현실성조건강화)
- 절경 카탈로그 후처리에 현실성 필터(`isRealisticScenicGroup`)를 추가해 타입별로 추가 제약을 검증하도록 확장했다.
- `폭포`는 산맥계열 지형과 인접한 단일 `강` 타일(`group.length === 1`)일 때만 절경으로 승격되도록 제한했다.
- `동굴/동굴군`, `산호만`, `오아시스권`, `대호수`에도 지형 구성 비율/연결성 기반 제약을 추가해 과장된 절경 군집이 선정되는 경우를 완화했다.

## ver.0.3.47(260429-절경승격랜덤점수전환)
- 월드맵 절경 승격 기준에서 평균 고도/습도/온도의 극단치 기반 `scenicScore` 계산식을 제거했다.
- 절경 후보 점수는 시드 기반 RNG(`mulberry32`)에서 가져온 랜덤값으로만 부여하도록 변경해, 조건을 만족한 군집 중 무작위성이 중심이 되도록 조정했다.
- `applyScenicCatalog` 시그니처를 확장해 외부 랜덤 함수를 주입받고, 월드 생성 파이프라인에서 동일 시드 RNG를 전달하도록 연결했다.

## ver.0.3.46(260429-절경외곽선엣지표시내부하이라이트)
- 월드맵 절경 레이어에서 기존의 타일별 내부 테두리 렌더를 제거하고, odd-r 헥사 이웃 규칙을 이용한 절경 군집 외곽 엣지만 표시하도록 정리했다.
- 절경 하이라이트(선택) 시에는 외곽선 색 강화와 함께 선택된 절경 타일 내부를 반투명 색으로 채우는 표시를 추가했다.
- 중복으로 남아 있던 구형 절경 외곽선 루프를 제거해 렌더 경계 판정이 단일 경로로 동작하도록 정리했다.

## ver.0.3.45(260429-절경외곽선정합승격수제한)

- odd-r 헥스 이웃 규칙과 동일한 방향 오프셋으로 절경 경계 판정을 다시 맞춰, 내부 타일까지 전부 테두리가 그려지던 오동작을 수정했다.
- 절경 승격 로직을 조정해, 조건을 만족한 모든 군집을 지정하지 않고 `scenicScore` 상위 후보만 등급별 제한 수량으로 절경에 승격하도록 변경했다.
- 승격 제한 기준은 `small/medium/large` 별 최대 개수로 적용해 “특출난 경관만 선택”되는 결과를 보장했다.

## ver.0.3.44(260429-절경외곽선클릭하이라이트)

- 절경 레이어 렌더를 지형 베이스 위 오버레이 방식으로 고정해, 절경 모드에서도 기본 지형 색상을 유지하도록 정리했다.
- 절경 경계선은 타일별 전체 테두리가 아니라, 동일 절경 군집의 외곽 경계(edge)만 내부 인셋 라인으로 그리도록 변경했다.
- 절경 레이어에서 클릭한 타일이 속한 절경 전체를 하이라이트하고, 절경 등급/타일 수/설명을 팝업으로 보여주도록 상호작용을 추가했다.

## ver.0.3.43(260429-절경스키마레이어추가)

- 절경 18종(소형 8 / 중형 6 / 대형 4) 기준을 데이터로 고정하기 위해 `assets/data/scenic_catalog.json` 스키마 파일을 추가했다.
- `world_map.html` 레이어 버튼에 `절경` 모드를 추가해 절경 시각화를 별도 레이어로 켜고 끌 수 있게 했다.
- `assets/js/world_map.js`에 절경 카탈로그 기반 군집 판정(`applyScenicCatalog`)을 추가하고, 판정된 타일은 `tile.scenic` 메타를 저장하도록 확장했다.
- 절경 레이어 렌더는 타일 내부 인셋 헥스 라인으로만 표시해, 경계선이 인접 타일 영역을 침범하지 않도록 처리했다.

## ver.0.3.42(260429-월드맵생성과정제거핀치줌추가)

- 요청에 맞춰 월드 생성 단계 자동재생/수동선택 UI를 제거하고, 기존처럼 생성 후 바로 최종 렌더만 수행하도록 되돌렸다.
- `assets/js/world_map.js`에서 단계 디버그 스냅샷(`debugStages`) 및 관련 색상 오버레이 코드를 정리해 렌더 경로를 단순화했다.
- 모바일 조작 개선을 위해 멀티 포인터 기반 핀치 줌인/줌아웃을 추가했고, 핀치 중에는 드래그/클릭 판정이 충돌하지 않도록 처리했다.

## ver.0.3.41(260429-월드맵생성과정자동재생)

- 사용자 의도(“생성 중 유저가 과정을 보게”)에 맞춰, 맵 재생성 시 단계 뷰를 수동 선택이 아니라 자동 재생으로 변경했다.
- `assets/js/world_map.js`에 `playGenerationStages`를 추가해 `height → climate → biome → post → final` 순서로 짧은 간격 렌더를 진행하도록 적용했다.
- 재생 도중 새 월드가 생성되면 이전 재생을 중단하도록 토큰 기반 취소 가드를 추가해 화면 깜빡임/역재생을 방지했다.

## ver.0.3.40(260429-월드맵생성단계뷰어추가)

- `world_map.html`에 생성 단계 선택 UI(`generationStageSelect`)를 추가해, 최종 지형 외에 생성 중간 단계도 확인할 수 있게 했다.
- `assets/js/world_map.js`에 디버그 스냅샷(`rawFields`, `biomeTiles`, `postTiles`)을 저장하고, 선택한 단계에 따라 캔버스 렌더 색상을 바꿔 단계별 변화를 시각화하도록 확장했다.
- `assets/css/world_map.css`에 단계 선택 컨트롤 스타일을 추가해 기존 월드맵 UI 톤과 맞췄다.

## ver.0.3.39(260429-webgl전역상수충돌해결)

- `webgl_world_map.html`에서 `world_map.js`와 `webgl_world_map.js`를 함께 로드할 때 발생하던 전역 `const MAP_SIZE` 중복 선언 충돌을 해결했다.
- `assets/js/webgl_world_map.js`의 상수명을 `WEBGL_MAP_SIZE`로 변경해 `Identifier 'MAP_SIZE' has already been declared` 구문 오류로 WebGL 맵이 멈추던 문제를 수정했다.

## ver.0.3.38(260428-webgl동일월드생성절차연동)

- WebGL 월드맵이 기존 `world_map.js`와 **동일한 생성 절차**를 사용하도록, `window.NewtheriaWorldGen` 공유 API(`generateWorldMap`, `applyRerollSettingsFromValues`, `getConfig`)를 노출했다.
- `assets/js/world_map.js`는 월드맵 전용 DOM이 없을 때 자동 부팅을 건너뛰도록 가드(`canBootWorldMapScreen`)를 추가해, 생성 로직만 다른 화면(WebGL)에서 안전하게 재사용할 수 있게 정리했다.
- `webgl_world_map.html`에 기존 월드맵과 동일한 리롤 슬라이더/재생성 버튼을 추가하고, WebGL 화면에서 동일 파라미터를 적용해 월드를 생성하도록 연결했다.
- `assets/js/webgl_world_map.js`는 생성된 타일의 `tile.color`를 텍스처로 업로드해 WebGL 반복 샘플링으로 렌더하도록 교체해, 생성 규칙은 동일하게 유지하고 출력만 GPU 렌더링으로 분리했다.

## ver.0.3.37(260428-webgl월드맵테스트추가)

- 사용자 요청(“webgl로 한번 월드맵 짜줘”)에 맞춰, GPU 셰이더로 지형을 절차 생성하는 전용 테스트 페이지 `webgl_world_map.html`을 추가했다.
- `assets/js/webgl_world_map.js`에 WebGL 파이프라인(정점/프래그먼트 셰이더, full-screen quad, uniform 기반 카메라 제어)을 구현하고, 드래그 팬/휠 줌/R 리셋 입력을 연결했다.
- `assets/css/webgl_world_map.css`로 테스트 허브 스타일과 통일된 화면 테마를 적용하고, 조작 가이드를 오버레이로 제공했다.
- `test_mode.html` 테스트 허브에 “WebGL 월드맵 테스트” 진입 카드를 추가해 기존 기능과 함께 탐색할 수 있게 했다.

## ver.0.3.36(260428-월드맵raf렌더스케줄링적용)

- 월드맵 상호작용 성능 개선을 위해 `assets/js/world_map.js`에 `requestAnimationFrame` 기반 렌더 스케줄러(`scheduleRender`)를 도입했다.
- 휠 줌/포인터 드래그/레이어 전환/리사이즈처럼 고빈도 이벤트에서 즉시 렌더하던 경로를 스케줄 방식으로 통일해, 같은 프레임의 중복 렌더 요청이 1회로 병합되도록 개선했다.
- 페이지 이탈 시점 `cancelAnimationFrame` 정리를 추가해, 대기 중인 렌더 콜백이 남지 않도록 정리했다.

## ver.0.3.35(260428-타일객체가시영역렌더카메라조작)

- 월드 타일 데이터를 개별 객체(`WorldTile`)로 생성하도록 정리하고, 렌더러는 전체가 아니라 **뷰포트에 보이는 범위만** 계산해 그리도록 변경했다.
- 스크롤바 없이 조작할 수 있도록, 마우스 휠로 줌 인/아웃, 포인터 드래그로 카메라 이동(팬)하는 입력 체계를 추가했다.
- 카메라 좌표는 월드 1주기 픽셀 폭/높이 기준으로 래핑해 토러스 규칙(끝과 끝 연결)을 유지하며, 클릭 시 현재 카메라 기준으로 타일 정보를 다시 표시하도록 복원했다.
- `.world-map-viewport`는 `overflow: hidden`을 유지하고 `touch-action: none`/캔버스 grab 커서를 적용해 스크롤 없는 조작 UX를 강화했다.

## ver.0.3.34(260428-월드맵스크롤제거)

- 추가 요청(“스크롤도 없애줘”)을 반영해 월드맵 뷰포트의 스크롤 동작을 제거했다.
- `assets/js/world_map.js`에서 스크롤 래핑/중앙 재배치 함수와 스크롤 이벤트 연결을 삭제하고, 월드 생성 시 스크롤 좌표를 원점(0,0)으로 고정했다.
- `assets/css/world_map.css`의 `.world-map-viewport` `overflow`를 `hidden`으로 변경해 스크롤바가 나타나지 않도록 정리했다.

## ver.0.3.33(260428-월드맵화면렌더제거)

- 사용자 요청에 맞춰 월드맵 **생성 로직은 유지**하고, 화면 캔버스에 헥스를 그리는 렌더링 루프를 제거했다.
- `renderWorld`는 이제 월드 통계/메타 갱신만 수행하며, 캔버스는 비워진 상태를 유지하도록 정리했다.
- 렌더링 제거 상태와 일관되도록 캔버스 클릭 타일 팝업은 비활성화해 오동작 가능성을 방지했다.

## ver.0.3.32(260428-워프홀수y색상밀림부호방향보정)

- 상하 워프 시 홀수 y 행 색상 밀림이 남아 있던 원인을, 세로 반복 블록의 **dy 방향 부호 처리 누락**과 스크롤 x 보정 부호 불일치로 재정리하고 수정했다.
- 반복 렌더에서 세로 홀짝 반전 시 `oddRowQShift(-1)`은 유지하되, `dy < 0`인 위쪽 블록에만 전역 `q + 1`을 추가해 위/아래 반복 블록의 반 칸 정렬 방향이 서로 반대가 되도록 맞췄다.
- `maintainWrappedScroll`의 세로 워프 수평 보정 부호를 반대로 조정해, 위 경계→아래 블록 이동 시 `-hexWidth/2`, 아래 경계→위 블록 이동 시 `+hexWidth/2`가 적용되도록 정렬했다.

## ver.0.3.31(260428-워프홀수y색상밀림재원인분석)

- `ver.0.3.30` 수정 후에도 상하 워프 순간 **화면에 보이는 홀수 y행만** 색이 밀려 보이는 증상을 재점검했다.
- 재확인된 원인은 반복 렌더에서 세로 주기 홀짝 반전을 **행 단위 `q` 재매핑(`oddRowQShift`)으로 처리한 설계 자체**다.
- odd-r 세로 주기(높이 홀수) 보정은 타일 인덱스 재매핑이 아니라, 반복 블록 전체에 대한 `hexWidth/2` 수평 평행이동으로 처리해야 한다.
- 현재처럼 홀수 행에만 `q`를 ±1 보정하면 동일 위치에 서로 다른 타일이 겹쳐 그려져(후행 draw가 선행 draw를 덮음) “홀수 y 색만 이웃 색으로 바뀜”이 재발한다.
- 이번 항목은 원인 기록만 수행했으며, 추가 수정 코드는 적용하지 않았다.

## ver.0.3.30(260428-워프홀수y색상오버라이드수정)

- 상하 워프 시 홀수 `y` 타일 색이 `x+1` 이웃 색으로 덮어써지던 문제를 수정했다.
- 3x 반복 렌더 보정식에서 세로 홀짝 반전 시 홀수 행 `q` 보정을 `+1`에서 `-1`로 교체해, odd-r 좌표계 반복 블록 정렬이 한 방향으로 일관되게 유지되도록 조정했다.
- 스크롤 워프 단계(`maintainWrappedScroll`)에 세로 경계 재진입 시 `hexWidth/2` 수평 보정을 복원해, 상·하 워프 순간의 반 칸 점프를 상쇄했다.

## ver.0.3.29(260428-워프홀수y색상오버라이드원인분석)

- 사용자 제보(“상하 워프 시 홀수 y 타일 색이 x+1 색으로 보임”) 재현 기준으로 원인을 점검했다.
- 원인은 `assets/js/world_map.js`의 3x 반복 렌더 보정식에서, 세로 주기 홀짝 반전 시 홀수 행에 `q + 1`(`oddRowQShift`)을 강제 적용한 부분으로 확인했다.
- 위 보정이 상하 경계 재진입 구간에서 홀수 y 행의 샘플 타일 참조를 오른쪽 이웃(`x+1`)으로 밀어 색상 매칭이 덮어써지는 결과를 만들었다.
- 이번 항목은 원인 기록만 수행했으며, 보정/수정 코드는 적용하지 않았다.

## ver.0.3.28(260428-홀수y색상x밀림보정)

- 사용자 추가 제보(“워프 시 홀수 y 타일 색만 x=-1로 밀림”)를 기준으로, 워프 보정 위치를 스크롤 좌표가 아닌 **반복 렌더 좌표 계산부**로 재조정했다.
- 3x 반복 렌더에서 세로 주기 이동으로 행 홀짝이 뒤집히는 경우를 감지하고, 홀수 y행은 `q + 1` 보정을 적용해 색상-형상 매칭이 어긋나지 않도록 수정했다.
- `maintainWrappedScroll`의 세로 워프 시 강제 x 보정은 제거하고, 스크롤 워프는 주기 이동만 담당하도록 단순화했다.

## ver.0.3.27(260428-워프홀짝행오프셋보정)

- 스크롤 워프 순간 홀수/짝수 줄 기준으로 한 칸 밀려 보이던 현상의 원인을 **odd-r 좌표계의 세로 주기 워프 시 좌우 반 칸 보정 누락**으로 정리했다.
- `maintainWrappedScroll`에 세로 워프 발생 시 행 수 홀짝(`HEX_CONFIG.rows`)을 검사해, 홀수 높이일 때만 `hexWidth/2` 좌우 보정을 적용하도록 추가했다.
- 렌더/워프 주기 계산에서 `Math.round`를 제거하고 부동소수 원값을 유지해 주기 누적 오차로 인한 미세 seam/점프 체감을 완화했다.

## ver.0.3.26(260428-월드경계인덱스연속정렬)

- 사용자 피드백(“(199,x) 다음이 (0,x)처럼 이어져야 함”)에 맞춰, 3x 반복 렌더 오프셋 기준을 픽셀 추정값이 아닌 **타일 인덱스 1주기(width/height) 이동**으로 변경했다.
- 반복 렌더 시 각 타일 좌표를 `q + dx*width`, `r + dy*height`로 확장한 뒤 `hexToPixel`을 적용하도록 바꿔 가로/세로 경계가 동일 인접 규칙으로 이어지게 정렬했다.
- 스크롤 워프 기준 폭/높이도 타일 주기(`hexWidth * width`, `size * 1.5 * height`)로 맞춰 경계 재진입 시 위치 연속성을 강화했다.

## ver.0.3.25(260428-월드맵직접3x헥스렌더전환)

- 사용자 요청에 따라 월드맵 최종 출력 경로를 `pattern repeat` 기반에서 **메인 캔버스 직접 3x3 헥스 렌더링** 방식으로 전환했다.
- 오프스크린 1주기 텍스처(`worldTextureCanvas`)를 만든 뒤 `createPattern`으로 채우던 단계를 제거하고, 타일을 `(-1,0,1) x (-1,0,1)` 오프셋으로 메인 캔버스에 직접 그리도록 변경했다.
- 중앙 블록 기준 오프셋 렌더 + 기존 스크롤 워프(중앙 재배치) 조합은 유지해 토러스형 반복 탐색 동작은 동일하게 유지했다.

## ver.0.3.24(260428-월드맵0321롤백)

- 사용자 요청에 따라 월드맵 렌더링 로직을 `ver.0.3.21(260428-반복텍스처렌더전환)` 동작 기준으로 롤백했다.
- 롤백 범위는 `assets/js/world_map.js`의 후속 보정 단계(`HEX_RENDER_OVERLAP`, 텍스처 엣지 복사/스냅샷 처리)이며, 반복 텍스처 기본 렌더만 유지한다.
- 경계 후처리 없이 1주기 텍스처 + `repeat` 패턴 기반의 원형 구조로 되돌려, 0.3.22~0.3.23에서 추가된 보정에 따른 렌더 불안정 가능성을 제거했다.

## ver.0.3.23(260428-반복텍스처엣지복사안정화)

- 사용자 제보(“맵이 제대로 출력되지 않음”)를 기준으로 반복 텍스처 후처리 코드를 점검했다.
- 원인으로 확인된 지점은 **동일 캔버스를 source/destination으로 동시에 사용하는 엣지 `drawImage` 복사** 구간으로, 브라우저 구현에 따라 겹침 복사 아티팩트가 발생할 수 있었다.
- 엣지 채움은 이제 스냅샷 오프스크린 캔버스를 읽기 소스로 분리한 뒤 적용하도록 변경해, 경계 채움 안정성과 렌더 일관성을 높였다.

## ver.0.3.22(260428-육각경계겹침엣지채움)

- 사용자 분석(육각형 끝 타일 꼭지점 정렬 불일치)에 맞춰, 텍스처 생성 시 육각형을 소폭 겹쳐 그리는 `HEX_RENDER_OVERLAP` 렌더 여유값을 도입했다.
- 반복 텍스처 경계의 빈 띠가 보이는 구간은 반대편 엣지 픽셀을 복사해 채우는 엣지 채움 단계를 추가했다.
- 결과적으로 “겹쳐 표현 + 반대편 타일로 빈 부분 채우기” 두 접근을 함께 적용해 seam 노출을 추가 완화했다.

## ver.0.3.21(260428-반복텍스처렌더전환)

- 경계 seam 보정 패치 방식 대신, 월드맵 렌더를 오프스크린 1주기 텍스처 + `repeat` 패턴 채우기 방식으로 전환했다.
- 텍스처 생성 시 각 헥스를 ±1주기 오프셋까지 함께 그려, 경계를 넘는 헥스 면이 반대편에도 정확히 채워지도록 변경했다.
- 메인 캔버스는 3x3 영역을 패턴으로 직접 채우도록 바꿔, 블록 경계선에서 생기던 검은 줄 아티팩트를 구조적으로 제거했다.

## ver.0.3.20(260428-루프경계seam봉합렌더)

- 3x3 반복 렌더링 이후, 블록 경계에 남는 1~2px 검은 seam을 제거하기 위한 후처리(경계 픽셀 봉합) 단계를 추가했다.
- 수직/수평 경계선 위치에 인접 픽셀 컬럼/로우를 다시 덮어써, 스크롤 경계가 단일 맵처럼 이어져 보이도록 렌더 방식을 조정했다.
- 기존 토러스 생성/이웃 규칙은 유지하면서 시각적 경계선 아티팩트만 별도로 정리했다.

## ver.0.3.19(260428-호수해양토러스통일)

- 사용자 추가 요청에 따라, 호수/해양 타일 군집 판정(`convertSmallWaterBodiesToLakes`)도 경계 제한이 아닌 토러스 이웃 규칙으로 통일했다.
- 물 덩어리 BFS에서 `getNeighborsBounded`를 제거하고 `getNeighbors`(wrap)만 사용하도록 변경해 좌우/상하 경계를 넘어 연속적으로 계산되게 했다.
- 더 이상 사용하지 않는 경계 검사 유틸(`inBounds`)을 정리해 루프 계산 경로를 단순화했다.

## ver.0.3.18(260428-월드맵생성단계토러스연결)

- 사용자 피드백(경계 seam 잔존)에 맞춰, 렌더 단계 보정보다 앞선 **맵 생성(노이즈 샘플링) 단계 자체**를 토러스 주기 기반으로 전환했다.
- `sampleTileableNoise2D`, `fbmPerlinTileable`, `ridgedNoiseTileable`를 도입해 좌우/상하 끝 좌표가 같은 값으로 이어지도록 했다.
- `buildScalarFields`의 워프/고도/산맥/온도/습도 필드 생성을 타일러블 샘플러 기반으로 교체해, 처음 생성된 원본 맵 자체가 경계 없이 이어지게 조정했다.

## ver.0.3.17(260428-월드맵루프경계선제거)

- 루프 스크롤 3x3 반복 렌더링에서 경계에 검은 선처럼 보이던 seam 이슈를 수정했다.
- 반복 블록 주기 계산에서 인위적인 외곽 여백(+16px)을 제거해, 블록 경계가 원본 맵과 정확히 맞물리도록 조정했다.
- 이에 맞춰 타일 클릭 역변환(`pixelToHexCoord`) 기준도 0,0 원점으로 동기화해 선택 좌표 오차가 생기지 않도록 맞췄다.

## ver.0.3.16(260428-월드맵루프스크롤연결)

- 월드맵 뷰포트에 `id="worldMapViewport"`를 부여하고, 스크롤 이벤트를 받아 루프 스크롤을 유지하는 보정 로직을 연결했다.
- 캔버스를 단일 맵 크기에서 3x3 반복 타일 크기로 렌더링하도록 변경해, 경계를 넘어 스크롤해도 같은 월드 패턴이 자연스럽게 이어지게 했다.
- 뷰포트 스크롤 위치는 기본적으로 중앙 타일(1,1) 영역을 기준으로 유지하고, 가장자리 임계치를 넘으면 반대편으로 즉시 재배치해 끊김을 줄였다.

## ver.0.3.15(260428-월드맵토러스루프연결)

- 월드맵 헥스 이웃 계산을 토러스(루프) 방식으로 변경해, 좌우 끝과 위아래 끝이 서로 연결되도록 조정했다.
- 클릭 좌표 처리에도 동일한 래핑 규칙을 적용해, 경계를 넘어 선택해도 반대편 타일 정보를 일관되게 확인할 수 있게 했다.
- 단, 작은 물 웅덩이를 `호수`로 변환하는 판정은 대양 전체가 한 덩어리로 합쳐지는 부작용을 막기 위해 기존 경계 기반 탐색 규칙을 유지했다.

## ver.0.3.14(260428-038롤백)

- 사용자 요청에 따라 코드베이스를 `ver.0.3.8(260428-진입경로별프롤로그로드분기)` 시점 기준으로 롤백했다.
- 롤백 범위는 월드맵 테스트 화면 관련 최근 변경(`world_map.html`, `assets/js/world_map.js`, `assets/css/world_map.css`)이며, 해당 파일들을 `ver.0.3.8` 커밋 상태로 복원했다.
- 이력 문서에 롤백 배경과 범위를 명시해 이후 추적 시 어떤 버전 기준으로 되돌렸는지 확인할 수 있도록 정리했다.

## ver.0.3.13(260428-카드잔재강제제거취소)

- `ver.0.3.12`에서 추가했던 테스트 화면 런타임 카드 잔재 강제 제거 코드를 취소했다.
- 월드맵 테스트 페이지 UI는 `world_map.html`의 실제 마크업 상태를 기준으로 표시되며, 스크립트에서 추가 DOM 제거를 수행하지 않도록 되돌렸다.

## ver.0.3.12(260428-테스트맵카드잔재강제제거)

- 월드맵 테스트 창에서 `빌드 라운드`, `선택지 다시 뽑기` UI 잔재가 보인다는 피드백을 반영했다.
- `world_map.js` 초기화 시점에 구버전/캐시 DOM에 남아 있을 수 있는 카드 빌드 컨테이너(`.world-map-card-builder`, `#worldBuildCardFan`, `#drawBuildCardsButton`, `#buildRoundLabel`)를 강제로 제거하도록 안전 정리를 추가했다.

## ver.0.3.11(260428-대륙다이나믹형상보정)

- 월드맵 테스트 화면에 카드 선택 UI가 남아 보인다는 피드백을 반영해, 테스트 페이지(`world_map.html`)가 슬라이더 기반 설정만 가지는 상태를 다시 점검/정리했다.
- `landmassBias` 적용식을 재설계해 단순 고도 상향/하향이 아니라 **육괴 연결성(continental core) + 분절성(archipelago fragment)** 축으로 동작하도록 변경했다.
- 대륙형에서는 저주파 코어를 더 강하게 혼합해 땅이 뭉치게 하고, 다도해형에서는 분절 마스크를 강화해 섬 군집이 늘어나게 조정했다.
- 동시에 `effectiveRidgeStrength`와 `reliefBoost`를 도입해, 대륙형에서도 평지 일변도가 되지 않도록 고도 기복(산맥/험지)이 유지되게 보정했다.

## ver.0.3.10(260428-테스트맵카드제거대륙대비강화)

- 월드맵 테스트 화면에서 카드 빌드 UI를 제거했다. 테스트 목적에 맞게 슬라이더 기반 리롤 설정만 남기고, `card_templates` 리소스 로딩도 제외했다.
- `지형 형태(다도해 ↔ 대륙형)` 슬라이더는 유지하되, 지형 생성식에서 대륙/군도 차이가 더 강하게 보이도록 `landmassBias` 반영 강도를 상향했다.
- 초저주파 `continentalMask`를 추가하고, bias 방향에 따라 고도 오프셋/해양 산란 가중치를 크게 이동시켜 다도해와 대륙형 결과 대비를 강화했다.

## ver.0.3.9(260428-월드빌드카드다도해대륙축추가)

- 월드맵 테스트 화면에 `지형 형태(다도해 ↔ 대륙형)` 슬라이더를 추가하고, 생성 메타에 현재 축 값을 함께 표시하도록 확장했다.
- 월드 생성식(`buildScalarFields`)에 `landmassBias`를 반영해, 음수일 때는 섬 군집 성향, 양수일 때는 연결된 대륙 성향이 강화되도록 저주파/산란 가중치를 조정했다.
- 월드맵 헤더에 카드형 빌드 섹션을 추가해 `고도/해양/강/산맥/워프/지형형태` 축의 랜덤 카드 4장 중 1장을 고르는 라운드를 6회 반복할 수 있도록 구현했다.
- 카드 선택 결과는 기존 리롤 파라미터(`HEX_CONFIG`)에 누적 적용되며, 선택 완료 시 즉시 맵을 재생성하도록 연결했다.

## ver.0.3.8(260428-진입경로별프롤로그로드분기)

- 시작 화면 라우팅을 조정했다: `새로운 여정`은 `main.html?entry=new`, `어떤 모험가의 일지(불러오기)`는 `main.html?entry=load&slot={activeSlotId}` 형태로 진입하도록 변경했다.
- 메인 뷰 라우터에서 URL 쿼리(`entry`, `slot`)를 해석해 분기한다: `entry=load`가 아니면 항상 프롤로그 대화 뷰부터 시작한다.
- `entry=load`일 때는 전달된 슬롯 id를 실제 세이브 데이터(`newtheria.saveSlots.data.slots[slotId]`)에서 검증/적용한 뒤 기본 뷰를 연다.
- 로드 성공 시 `saveSlotsMeta.activeSlotId`, `saveSlotsData.activeSlotId`를 동기화하고, 런타임 접근용 `window.NewtheriaRuntimeSession`에 로드된 슬롯/데이터를 기록한다.
- 요청 슬롯이 유효하지 않으면 `slot1`로 1회 폴백해 기본 뷰를 열도록 안전장치를 추가했다.

## ver.0.3.7(260427-스위치불리언변수숫자제한)

- 전역 게임 상태 저장소(`game_state_store.js`)에서 값 타입 제약을 강화했다.
- 스위치 값은 `on/off` 또는 `true/false`(불리언/한글 별칭 포함)만 허용하고, 그 외 값은 갱신하지 않도록 변경했다.
- 변수 값은 숫자만 허용하고, 숫자로 파싱 불가능한 값은 거부하도록 변경했다.
- 변수 기본값을 빈 문자열이 아닌 `0`으로 통일해 타입 일관성을 확보했다.
- 대화 템플릿 디버그 로그 사유를 타입 검증 실패 상황까지 표현하도록 보강했다.

## ver.0.3.6(260427-전역게임상태저장소분리)

- 스위치/변수 관리 로직을 `assets/js/dialogue_template.js` 내부에서 분리해, 게임 전반에서 재사용 가능한 전역 모듈 `assets/js/game_state_store.js`로 이동했다.
- 전역 API `window.NewtheriaGameStateStore.getStore("global")`를 추가해 화면/대화 템플릿이 달라져도 동일한 상태 저장소를 공유하도록 구성했다.
- 대화 템플릿은 이제 전역 저장소를 참조해 템플릿 치환 스냅샷만 동기화하며, 기존 `defineVariables`/`defineSwitches`/`setVariable`/`setSwitch`/`setVariables` API는 호환 유지했다.
- `main.html`, `dialogue_prototype_test.html`에 전역 저장소 스크립트 로딩 순서를 추가해 대화 템플릿보다 먼저 초기화되도록 정리했다.

## ver.0.3.5(260427-스위치변수중앙관리도입)

- 대화 런타임에 스위치/변수 중앙 저장소를 도입했다. 각 항목은 `id(1부터 시작)`, `name`, `description`, `value`를 함께 보관한다.
- 생성 옵션으로 `variableDefinitions`/`switchDefinitions`를 받아 초기 정의를 등록할 수 있게 했고, 런타임 API `defineVariables`, `defineSwitches`, `setVariable`, `setSwitch`, `getRuntimeState`를 추가했다.
- 기존 `setVariables({ ... })`는 하위 호환으로 유지하되, key를 변수 name으로 자동 등록/갱신하도록 연결했다.
- 템플릿 치환 변수는 `{name}` 기준을 유지하고, 보조로 `{var_1}`, `{switch_1}` id 기반 키도 노출하도록 보강했다.

## ver.0.3.4(260427-대화페이지문장단위분할보강)

- 대화 페이지 분할 기준을 보강했다: 빈 본문 렌더 시 `clientHeight`가 1줄로 잡히는 경우를 보정하기 위해 `max-height`를 우선 반영해 페이지당 2~3줄 이상이 정상적으로 채워지도록 수정했다.
- 페이지 넘김은 문장 단위(`.`, `!`, `?`, `…`, `。`, `！`, `？`)를 우선 기준으로 적용해, 가능한 한 문장이 끊기지 않게 다음 페이지로 이동하도록 개선했다.
- 단일 문장 자체가 한 페이지를 넘는 경우에만 공백/문자 단위 보조 분할을 허용해 진행 막힘 없이 표시되도록 유지했다.

## ver.0.3.3(260427-대화오버플로우페이지분할)

- 고정 크기 대화창에서 본문이 넘칠 경우, 같은 block 안에서 자동 페이지 분할 후 다음 입력 시 다음 페이지를 먼저 표시하도록 개선했다.
- 타입라이터(`type_speed`)가 켜진 대사도 첫 페이지 기준으로 타이핑하고, 타이핑 완료 후 `다음` 입력 시 다음 페이지를 순차 노출하도록 연결했다.
- 인라인 특수 효과(`[[spoiler:...]]`, `[[glitch:...]]`) 라인은 기존 즉시 렌더 동작을 유지해 연출 호환성을 보존했다.

## ver.0.3.2(260427-메인대화뷰모듈리팩토링)

- `main.html`의 인라인 뷰 라우팅/대화 생명주기 코드를 `assets/js/main_view_router.js`로 분리해 구조를 단순화했다.
- 대화 뷰 종료 시 인스턴스를 파괴하지 않고 `clearDebugTrace + jumpTo`로 event/scene 진행 상태만 초기화해, UI 재사용성과 자원 효율을 함께 확보했다.
- `block.end` 감지 로직을 라우터 모듈 내부로 캡슐화해 메인 문서의 역할을 레이아웃 중심으로 정리했다.

## ver.0.2.136(260426-main초기화)

- 사용자 요청에 따라 `main.html` 내용을 전부 제거하고, 최소 HTML 골격만 남긴 초기 상태로 되돌렸다.
- 이후 프롤로그/월드 작성 흐름을 처음부터 다시 설계할 수 있도록 메인 페이지를 빈 상태로 정리했다.

## ver.0.2.135(260426-메인내월드맵dom직접구성)

- 사용자 피드백에 맞춰 `main.html`의 iframe 임베드 방식을 제거했다.
- `world_map.html`과 동일한 월드맵 화면 DOM(헤더/리롤 슬라이더/레이어 버튼/캔버스/월드정보 다이얼로그)을 `main.html` 내부에 직접 구성했다.
- 월드 작성 마지막 카드 선택 시 페이지 이동/임베드 없이 `main.html` 안에서 월드맵 스테이지를 활성화하고, 기존 `assets/js/world_map.js` 생성 로직을 동일하게 사용하도록 연결했다.

## ver.0.2.134(260426-프롤로그내월드맵생성기임베드)

- 프롤로그 월드 작성 마지막 카드 선택 시 `window.location.href`로 페이지 이동하지 않도록 수정했다.
- `main.html`에 월드맵 생성기 패널을 추가하고 `world_map.html`을 iframe으로 로드해, 같은 페이지 안에서 월드맵 생성을 진행하도록 전환했다.
- 월드 작성 카드 오버레이는 최종 선택 후 자동으로 닫히고, 즉시 임베드된 월드맵 생성 화면이 표시되도록 연결했다.

## ver.0.2.133(260426-프롤로그종료후월드작성카드연결)

- `main.html` 프롤로그에서 대화 스크립트 `end` 도달 시점을 추적 로그(`trace.type=end`)로 감지해, 이후 단계로 자동 진입하도록 연결했다.
- 프롤로그 종료 후에는 공용 카드 템플릿(`card_templates.js`)을 상속한 카드 선택 오버레이를 3단계로 표시해 세계 성향을 고르도록 구성했다.
- 마지막 카드 선택 시 `world_map.html`로 이동해 기존 월드맵 생성/작성 로직을 그대로 사용하도록 연결했다.

## ver.0.2.132(260426-프롤로그디버그패널제거)

- `main.html` 프롤로그 화면에서 좌상단 테스트/디버그 패널 전체를 제거해 본편 UI에는 대화창만 표시되도록 정리했다.
- 프롤로그는 고정 시작 포인터(`opening`/`scene 0`/`intro_start`)로 로드하고, `showMeta: false`를 유지해 `scene#`·`block#` 메타 문구가 노출되지 않게 했다.
- 테스트/디버그 패널은 `dialogue_prototype_test.html`에서만 계속 사용하도록 역할을 분리했다.

## ver.0.2.131(260426-프롤로그메타복귀버튼제거)

- `main.html` 프롤로그 진입 화면에서 상단 `시작화면` 복귀 버튼을 제거했다.
- 프롤로그 대화 렌더 시 `showMeta: false` 옵션을 적용해 `event/scene/block` 메타 라인(예: `scene#0 / block#3`)이 화면에 보이지 않도록 조정했다.
- 테스트 전용 페이지(`dialogue_prototype_test.html`)는 기존 메타 표시를 유지해 디버그 가시성을 보존했다.

## ver.0.2.130(260426-새로운여정메인프롤로그연결)

- 시작 화면 `새로운 여정` 카드 라우팅을 `main.html`로 연결해, 카드 선택 시 메인 프롤로그 화면으로 진입하도록 변경했다.
- 신규 `main.html`을 추가하고, 기존 대화창 테스트 화면 구성을 재사용해 프롤로그를 동일한 대화 UI로 시작할 수 있도록 구성했다.
- 메인 프롤로그 화면 우상단에 `시작화면` 복귀 버튼을 배치해, 초기 진입 테스트 중 빠르게 시작 화면으로 되돌아갈 수 있게 했다.

## ver.0.2.129(260426-새로운여정이동제거)

- 시작 화면에서 `새로운 여정` 카드를 선택했을 때 `journey_builder.html`로 이동하던 라우팅을 제거했다.
- 이제 `새로운 여정` 카드는 클릭해도 페이지 전환 없이 시작 화면에 유지된다.

## ver.0.2.128(260426-원페이지여정생성플로우추가)

- `새로운 여정` 메뉴 진입 시 테스트창이 아니라 본편 온보딩 페이지(`journey_builder.html`)로 연결되도록 시작 화면 라우팅을 추가했다.
- 신규 페이지에서 프롤로그 → 세계 생성 → 역사 시뮬레이션 → 캐릭터 메이킹을 한 화면에서 순차 진행할 수 있도록 구성했다.
- 각 단계의 입력/결과를 한 상태 객체로 공유하도록 구현해, 세계 시드 기반 역사 이벤트와 캐릭터 결과 요약이 자연스럽게 이어지도록 정리했다.
- 신규 여정 화면 전용 스타일(`assets/css/journey_builder.css`)을 추가해 단계형 UI/폼/결과 카드 레이아웃을 제공했다.

## ver.0.2.127(260426-뒷면탭제스처충돌해소)

- 캐릭터 정보 카드 뒷면 탭(관계/특성/스탯/성향)이 데스크탑에서 반응하지 않던 원인을 수정했다: 카드 루트가 `<button>`인 상태에서 탭도 `<button>`으로 중첩되어 상호작용 충돌이 발생할 수 있었다.
- 뒷면 탭 노드를 `<span role="tab" tabindex="0">`로 변경하고, `pointerdown/click/keydown`에서 상위 카드 선택·드래그 이벤트로 버블되지 않도록 처리했다.
- 키보드 접근성(Enter/Space 탭 전환)도 함께 유지했다.
- 탭 노드가 `<span>`으로 바뀌어도 기존 UI 크기가 유지되도록 `.profile-back-tab`에 `display:inline-flex` 정렬 스타일을 적용했다.
- NPC 테스트 화면 버전을 `ver.0.2.127(260426-뒷면탭제스처충돌해소)`으로 갱신했다.

## ver.0.2.126(260426-대화창정보창연결복구)

- 테스트 대화창에서 이름 클릭 시 정보창이 열리지 않던 원인을 확인했다: 대화 테스트 페이지에 `window.NewtheriaCharacterCard` 제공 스크립트가 로드되지 않아 호출 대상이 없었다.
- `dialogue_prototype_test.html`에 `npc_test.js`(module)를 로드해 전역 카드 API가 등록되도록 연결했다.
- 화면 간 동일 스타일 유지를 위해 공용 오버레이 스타일 파일 `assets/css/character_profile_overlay.css`를 분리/추가하고 NPC 테스트/대화 테스트 양쪽에서 공용으로 사용하도록 구성했다.
- NPC 테스트 화면 버전을 `ver.0.2.126(260426-대화창정보창연결복구)`으로 갱신했다.

## ver.0.2.125(260426-대화창이름객체연동)

- 대화창 이름(`.dialogue-prototype__name`)도 객체형 진입점으로 연결했다.
- 이름 표시 서식은 그대로 유지하고, 내부적으로 `data-character-actor-id`를 부여해 클릭/Enter/Space로 동일 캐릭터 정보 카드를 열 수 있게 했다.
- 공용 카드 API `window.NewtheriaCharacterCard`에 `openByActorId`를 추가하고, `bindTriggers`가 `data-character-actor-id`도 처리하도록 확장했다.
- NPC 테스트 화면 버전을 `ver.0.2.125(260426-대화창이름객체연동)`으로 갱신했다.

## ver.0.2.124(260426-전역카드오픈api추가)

- 캐릭터 정보 카드를 현재 화면 외 다른 화면에서도 같은 UI로 열 수 있도록 전역 API `window.NewtheriaCharacterCard`를 추가했다.
- API는 `openByNpc`, `openById`, `close`를 제공하고, `bindTriggers`로 `data-character-id` 요소 클릭/키입력 시 공통 카드 UI를 열 수 있게 했다.
- 이를 통해 새 페이지에서도 동일한 카드 스타일/동작을 재사용할 수 있는 연결 지점을 마련했다.
- NPC 테스트 화면 버전을 `ver.0.2.124(260426-전역카드오픈api추가)`으로 갱신했다.

## ver.0.2.123(260425-캐릭터코드정리리팩토링)

- 캐릭터 관련 코드 점검 후 실제로 더 이상 쓰이지 않는 유틸 함수(`addOneToSeedLeadingDigit`, `toStatPercent`)를 제거했다.
- 현재 UI에서 사용하지 않는 구형 스타일 셀렉터(`.profile-tabs`, `.profile-tab`, `.profile-panel`)를 정리해 CSS 노이즈를 줄였다.
- 동작 유지에 필요한 핵심 로직/스타일만 남겨 캐릭터 정보 카드 코드 가독성과 유지보수성을 높였다.
- NPC 테스트 화면 버전을 `ver.0.2.123(260425-캐릭터코드정리리팩토링)`으로 갱신했다.

## ver.0.2.122(260425-캐릭터탭렌더리팩토링)

- 캐릭터 정보 카드 뒷면 탭 렌더링 코드를 역할별 함수(`createRelationsTabPanel`, `createTraitsTabPanel`, `createStatsTabPanel`, `createDebugTabPanel`)로 분리해 유지보수성을 높였다.
- `openNpcProfile` 내부의 인라인 렌더링 중복을 제거하고 탭 정의는 키/라벨/함수 연결만 담당하도록 단순화했다.
- 기존 동작(부모 id 숨김, 자녀 객체형 클릭, 특성 희귀도 칩, 디버그 탭 노출 조건)은 그대로 유지했다.
- NPC 테스트 화면 버전을 `ver.0.2.122(260425-캐릭터탭렌더리팩토링)`으로 갱신했다.

## ver.0.2.121(260425-부모표시id숨김)

- 관계 탭 `아버지`/`어머니` 표기에서 `(#id)`를 제거하고 이름만 보이도록 변경했다.
- 부모 이름은 id를 숨겨도 기존처럼 클릭 시 해당 캐릭터 정보를 즉시 열 수 있다.
- NPC 테스트 화면 버전을 `ver.0.2.121(260425-부모표시id숨김)`으로 갱신했다.

## ver.0.2.120(260425-부성동기화배우자일대일보강)

- 자녀 성씨 동기화 로직을 보강했다: 아버지 링크가 있으면 항상 아버지 성씨/가문으로 재동기화하고, 아버지 링크가 비어 있어도 어머니 배우자가 유효한 경우 아버지 링크를 보강해 동일 규칙을 적용한다.
- 배우자 데이터 안전망 `enforceSpouseOneToOne`를 추가해, 생성 후 배우자 링크가 항상 1:1 상호 참조가 되도록 정규화했다.
- 이 보정은 데이터 오염/경계 케이스에서 발생할 수 있는 다중 배우자 표기를 사후 정리하는 용도다.
- NPC 테스트 화면 버전을 `ver.0.2.120(260425-부성동기화배우자일대일보강)`으로 갱신했다.

## ver.0.2.119(260425-자녀이름객체형복원)

- 관계 탭 `자녀` 압축 리스트를 단순 텍스트에서 이름 기반 객체형(클릭 가능)으로 변경했다.
- 자녀 이름을 누르면 해당 NPC 정보 카드가 즉시 열리도록 연결해, 객체 탐색 흐름을 유지했다.
- 압축 표시 정책은 유지하되 `id` 표기는 계속 숨김 처리해 가독성을 유지했다.
- NPC 테스트 화면 버전을 `ver.0.2.119(260425-자녀이름객체형복원)`으로 갱신했다.

## ver.0.2.118(260425-자녀압축표시id제거)

- 관계 탭 `자녀` 압축 텍스트 리스트 표기에서 `(#id)`를 제거하고 이름만 표시하도록 변경했다.
- 압축 리스트는 `이름 · 이름 · 이름` 형태로 유지해, 인원 수가 많아도 한 줄/자동 줄바꿈으로 빠르게 확인할 수 있게 했다.
- NPC 테스트 화면 버전을 `ver.0.2.118(260425-자녀압축표시id제거)`으로 갱신했다.

## ver.0.2.117(260425-뒷면드래그우선및자녀압축표시)

- 캐릭터 정보 카드 뒷면에서 드래그가 둔하게 느껴지던 원인을 `profile-back-content` 내부 스크롤 우선 처리로 판단하고, 해당 영역 스크롤을 제거(`overflow: hidden`)해 드래그 제스처 우선순위를 높였다.
- 관계 탭의 `자녀` 표시는 클릭 칩 나열 대신 압축 텍스트 라인으로 변경해, 자녀 수가 많을 때도 카드 하단이 덜 잘리고 한 번에 더 많이 보이도록 조정했다.
- NPC 테스트 화면 버전을 `ver.0.2.117(260425-뒷면드래그우선및자녀압축표시)`으로 갱신했다.

## ver.0.2.116(260425-여신종족특성고정)

- 사용자 요청에 따라 에스테리아(`id=0`)의 종족을 일반 종족이 아닌 특수 종족 `여신`으로 고정했다.
- 에스테리아의 후천 특성 목록을 기본 랜덤 부여 대신 빈 배열(`[]`)로 고정해 초기 상태에서는 특성이 없도록 조정했다.
- 고정 캐릭터 생성 헬퍼(`createFixedNpc`)에 `acquiredTraitIds` 오버라이드 인자를 추가해, 특수 캐릭터의 특성 정책을 개별 지정할 수 있게 보강했다.
- NPC 테스트 화면 버전을 `ver.0.2.116(260425-여신종족특성고정)`으로 갱신했다.

## ver.0.2.115(260425-여신관계차단시드동일화)

- 별의 여신 에스테리아(`id=0`)는 배우자/자녀/불륜 관계가 생성되지 않도록 가족·혼인·불륜 자동 연결 로직에서 제외했다.
- 안전망으로 생성 파이프라인 마지막에 에스테리아의 가족 링크를 항상 빈 상태로 재고정하도록 보강했다.
- 여신 시드는 플레이어 시드와 동일하게 사용하도록 변경했다. (기존 선두 자리 +1 규칙 제거)
- NPC 테스트 화면 버전을 `ver.0.2.115(260425-여신관계차단시드동일화)`으로 갱신했다.

## ver.0.2.114(260425-여신순례자고정동기화)

- NPC 고정 생성 캐릭터를 시작 화면 임시 대표 캐릭터 설정과 동기화했다.
- `id=0` 에스테리아는 `별의 여신` 타이틀/`goddess` actorId/여신 이미지(`assets/img/goddess.png`)를 사용하도록 수정했다.
- `id=1` 플레이어는 `순례자` 이름/`pilgrim` actorId/순례자 이미지(`assets/img/player.png`)를 사용하도록 수정했다.
- 고정 캐릭터 이름 조합 시 성 또는 이름이 비어도 표시가 어색하지 않도록 `full` 이름 생성에 `trim()` 처리를 추가했다.
- NPC 테스트 화면 버전을 `ver.0.2.114(260425-여신순례자고정동기화)`으로 갱신했다.

## ver.0.2.113(260425-npc버전체계리셋)

- 사용자 요청에 따라 NPC 제작 흐름 구간의 버전 체계를 `0.2.x` 단계로 재정렬했다.
- 기존 `ver.0.3.0`~`ver.0.31.0` 구간을 `ver.0.2.87`~`ver.0.2.112`로 순차 매핑해, NPC 관련 변경 이력이 같은 메이저/마이너 계열에서 연속 증가하도록 정리했다.
- NPC 테스트 화면 버전을 `ver.0.2.113(260425-npc버전체계리셋)`으로 갱신했다.

## ver.0.2.112(260425-불륜다중확률감쇠)

- 카사노바/팜므파탈 보유 캐릭터는 불륜 인원 제한은 유지하되, 이미 맺은 불륜 관계 수가 늘어날수록 다음 불륜 상대가 생길 확률이 단계적으로 감소하도록 조정했다.
- 다중 불륜 확률은 `0.22 * (0.7^현재 불륜 수)` 공식을 사용하도록 코드화했다. (예: 0명 22%, 1명 15.4%, 2명 10.8%)
- NPC 테스트 화면 버전을 `ver.0.2.112(260425-불륜다중확률감쇠)`으로 갱신했다.

## ver.0.2.111(260425-고정캐릭터시드연동)

- NPC 생성 규칙을 변경해 `id=0`은 항상 `에스테리아`, `id=1`은 항상 `플레이어`로 고정되도록 반영했다.
- 시드는 플레이어를 먼저 생성하고, 에스테리아 시드는 플레이어 시드의 맨 앞자리 숫자만 `+1`(9 다음은 0 순환)한 값을 사용하도록 연결했다.
- 카사노바(311)는 남성에게만, 팜므파탈(312)은 여성에게만 뽑히도록 성별 기반 특성 후보 필터를 추가했다.
- NPC 테스트 화면 버전을 `ver.0.2.111(260425-고정캐릭터시드연동)`으로 갱신했다.

## ver.0.2.110(260425-불륜라벨디버그탭확장)

- 캐릭터 정보 카드 상세 `관계` 탭에서 `연문 관계` 항목을 제거했다.
- `연문` 표기를 `불륜`으로 통일했다. (특성 설명/공용 특성 데이터 포함)
- 상세 `특성` 탭의 `후천 특성` 라벨을 `특성`으로 변경했다.
- 특성 희귀도(일반/희귀/영웅/전설)에 따라 특성 칩 배경/테두리 색이 달라지도록 적용했다.
- 상세 `스탯` 탭의 숫자 목록을 2열 칩 그리드로 변경해 스크롤 없이 한 번에 확인하기 쉽게 정리했다.
- 상세 탭의 `디버그`는 디버그 모드에서만 노출되도록 유지하고, 디버그 탭에 `불륜 관계` 항목을 추가했다.
- NPC 테스트 화면 버전을 `ver.0.2.110(260425-불륜라벨디버그탭확장)`으로 갱신했다.

## ver.0.2.109(260425-성향바치우침및색상분기)

- 성향 바 라벨을 고정값(호전/정착/명예)에서 현재 치우친 축 기준으로 바꿨다. 예: 호전↔온건 축에서 수치가 반대면 `온건`으로 표시.
- 성향 바 색상을 축 방향에 따라 분기했다: 호전/정착/명예 쪽(양수)은 붉은 계열, 반대 축(온건/방랑/실리)은 기존 푸른 계열.
- 위 규칙은 카드 앞면 미니 성향 바와 카드 뒷면 성향 탭 바 모두에 동일 적용했다.
- NPC 테스트 화면 버전을 `ver.0.2.109(260425-성향바치우침및색상분기)`으로 갱신했다.

## ver.0.2.108(260425-카드팬빈영역클릭차단해소)

- 상단 클릭이 계속 막히던 추가 원인을 수정했다: `profile-card-fan` 컨테이너 자체가 넓은 빈 영역까지 포인터 이벤트를 받으면서 클릭을 가로채고 있었다.
- 팬 컨테이너는 `pointer-events: none`으로 바꾸고 실제 카드(`.card-fan-card`)만 `pointer-events: auto`로 활성화해, 카드 바깥 영역 클릭 간섭을 제거했다.
- NPC 테스트 화면 버전을 `ver.0.2.108(260425-카드팬빈영역클릭차단해소)`으로 갱신했다.

## ver.0.2.107(260425-프로필상단클릭막힘수정)

- 원인: 프로필 오버레이의 본문(`profile-float__body`)이 `grid`의 1fr 영역을 전체 점유한 상태에서 `pointer-events: auto`라 상단 영역 클릭을 가로채고 있었다.
- 수정: `profile-float__body`는 `pointer-events: none`으로 바꾸고, 실제 카드 컨테이너(`profile-card-fan`)만 `pointer-events: auto`를 적용해 카드 영역 외 클릭 간섭을 제거했다.
- NPC 테스트 화면 버전을 `ver.0.2.107(260425-프로필상단클릭막힘수정)`으로 갱신했다.

## ver.0.2.106(260425-프로필오버레이클릭간섭해소)

- 프로필 오버레이(`profile-float`)가 화면 전체 클릭을 가로채던 문제를 수정했다.
- 오버레이 레이어는 `pointer-events: none`으로 바꾸고, 실제 상호작용이 필요한 카드/디스카드 존만 `pointer-events: auto`를 부여해 클릭 간섭을 제거했다.
- NPC 테스트 화면 버전을 `ver.0.2.106(260425-프로필오버레이클릭간섭해소)`으로 갱신했다.

## ver.0.2.105(260425-디스카드존드래그시노출)

- 프로필 디스카드 존이 평상시 항상 보이던 문제를 수정해, 카드 드래그 중일 때만 노출되도록 변경했다.
- 디스카드 컨트롤러에 프로필 전용 body 클래스(`profile-discard-active`, `profile-discard-visible`)를 연결하고, CSS에서 해당 클래스 상태일 때만 디스카드 존을 표시하도록 조정했다.
- NPC 테스트 화면 버전을 `ver.0.2.105(260425-디스카드존드래그시노출)`으로 갱신했다.

## ver.0.2.104(260425-중앙카드디스카드종료)

- 프로필 팝업 상단의 `NPC 카드` 텍스트와 닫기(X) 버튼을 제거하고, 화면 중앙 카드만 보이는 구조로 단순화했다.
- 카드 종료 방식은 하단 디스카드 존 드래그 앤 드롭으로 통일하고, 기존 수동 창 헤더 드래그/닫기 버튼 로직은 제거했다.
- 프로필 앞면 외형(레이어) 프리뷰 영역을 더 크게(120px) 조정했다.
- 카드 뒷면 탭은 고정된 상단 영역에 유지하고, 내부 콘텐츠만 스크롤되도록 레이아웃을 고정했다.
- 뒷면 탭에서 `정체성` 탭을 제거했다.
- NPC 테스트 화면 버전을 `ver.0.2.104(260425-중앙카드디스카드종료)`으로 갱신했다.

## ver.0.2.103(260425-프로필전면요약재배치)

- 프로필 카드 UI에서 별도 프로필 하위 구역(팬 래퍼/디스카드 전용 구역)을 제거하고, 프로필 플로트 본문 전체를 카드 템플릿 렌더 영역으로 사용하도록 단순화했다.
- 프로필 카드 오픈 시 앞면(간략 정보)이 먼저 보이도록 변경하고, 클릭은 선택만 처리하며 뒤집기는 템플릿 기본 롱프레스 동작으로만 수행되게 조정했다.
- 앞면 요약 레이아웃을 재구성했다: 좌상단 외형(레이어 프리뷰), 우상단 성·이름, 그 아래 미니 레이더 스탯, 하단 미니 성향 바.
- NPC 테스트 화면 버전을 `ver.0.2.103(260425-프로필전면요약재배치)`으로 갱신했다.

## ver.0.2.102(260425-npc프로필템플릿재구축)

- 기존 NPC 정보창 렌더링 코드를 재활용하지 않고 제거한 뒤, 공용 카드 템플릿 API(`renderCardFanCards`, `createCardFanBehavior`, `createDiscardZoneController`) 기반으로 프로필 카드를 다시 구성했다.
- 새 프로필 카드에서 템플릿 기본 상호작용(클릭 플립, 드래그, 디스카드 드롭)을 그대로 사용하도록 연결하고, 카드 뒷면 탭 콘텐츠는 NPC 데이터로 동적 주입하도록 재구축했다.
- NPC 테스트 화면 버전을 `ver.0.2.102(260425-npc프로필템플릿재구축)`으로 갱신했다.

## ver.0.2.101(260425-카드팩토리로딩보강)

- NPC 화면에서 인물 클릭 시 카드가 뜨지 않던 원인을 수정했다: 공용 카드 팩토리 스크립트(`assets/js/card_templates.js`)가 페이지에 선로드되지 않아 템플릿 생성이 실패하던 문제였다.
- `npc_test.html`에 공용 카드 팩토리 스크립트 로드를 추가하고, `npc_test.js`에는 템플릿 생성 실패 시 원인을 바로 확인할 수 있는 콘솔 경고/가드를 넣었다.
- NPC 테스트 화면 버전을 `ver.0.2.101(260425-카드팩토리로딩보강)`으로 갱신했다.

## ver.0.2.100(260425-템플릿상속규칙명문화)

- 카드 템플릿 사용 지시의 의미를 문서 규칙으로 명시했다: "템플릿 사용 = 공용 템플릿 상속(팩토리 직접 호출), 수동 DOM 복제 금지".
- 문서 운영 가이드와 카드 선택 연출 규칙에 동일한 강제 원칙을 추가해, 이후 작업에서도 같은 기준이 유지되도록 정리했다.
- NPC 테스트 화면 버전을 `ver.0.2.100(260425-템플릿상속규칙명문화)`으로 갱신했다.

## ver.0.2.99(260425-공용카드팩토리강제상속)

- NPC 프로필 카드 생성 시 수동 마크업을 조립하지 않고, 공용 카드 템플릿 팩토리(`NewtheriaCardTemplates.createCardFanCard`)를 **직접 호출**해 상속하도록 변경했다.
- 공용 템플릿으로 생성된 앞/뒷면 노드에 프로필 전용 콘텐츠만 주입하도록 구조를 정리해, 템플릿 변경분이 프로필 카드에도 자동 반영되게 했다.
- NPC 테스트 화면 버전을 `ver.0.2.99(260425-공용카드팩토리강제상속)`으로 갱신했다.

## ver.0.2.98(260425-공용카드템플릿상속적용)

- NPC 프로필 카드가 전용 카드 스타일을 따로 복제하지 않고, 시작화면/대화 선택지에서 쓰는 공용 카드 클래스(`card-fan-card`)를 직접 상속해 렌더링하도록 변경했다.
- 프로필 전용 CSS는 공용 템플릿 위에 필요한 크기/레이아웃만 오버라이드하도록 정리해, 이후 카드 스타일 변경 시 공용 템플릿 수정분을 함께 따라가도록 맞췄다.
- NPC 테스트 화면 버전을 `ver.0.2.98(260425-공용카드템플릿상속적용)`으로 갱신했다.

## ver.0.2.97(260425-독립드래그카드창)

- NPC 프로필을 `dialog` 종속 구조에서 분리해, 화면 위에 독립적으로 떠 있는 카드 창(`profile-float`)으로 변경했다.
- 카드 창 상단 핸들을 드래그하면 위치를 이동할 수 있도록 포인터 드래그 로직을 추가했고, 닫기 버튼/ESC로 창을 닫을 수 있게 했다.
- NPC 테스트 화면 버전을 `ver.0.2.97(260425-독립드래그카드창)`로 갱신했다.

## ver.0.2.96(260425-카드뒷면상세일체화)

- 캐릭터 프로필 템플릿 구조를 수정해, 상세 탭/패널이 카드 외부가 아니라 카드 **뒷면 내부**에서 렌더링되도록 변경했다.
- 카드 뒷면 상단에는 상태 헤더를 두고, 하단에는 탭/패널 스크롤 영역을 넣어 "정보가 카드 안에 모두 들어간 형태"를 유지했다.
- NPC 테스트 화면 버전을 `ver.0.2.96(260425-카드뒷면상세일체화)`로 갱신했다.

## ver.0.2.95(260425-캐릭터정보카드즉시상세노출)

- NPC 프로필 모달에서 카드 외부 제목/메타 줄을 제거해, 캐릭터 정보가 카드 템플릿 내부 컨텍스트로 일원화되도록 정리했다.
- NPC 카드를 열면 상세 탭이 즉시 펼쳐지도록 변경해, "카드만 보이고 정보를 다시 열어야 하는 단계"를 제거했다.
- NPC 테스트 화면 버전을 `ver.0.2.95(260425-캐릭터정보카드즉시상세노출)`으로 갱신했다.

## ver.0.2.94(260425-캐릭터정보카드템플릿화)

- NPC 프로필 팝업을 카드 템플릿 기반의 공용 캐릭터 정보 카드로 교체하고, 꾹 누르면 카드가 뒤집히며 확대된 상세 탭이 열리도록 변경했다.
- 카드 앞면에는 핵심 요약(이름/종족/가문/요약 스탯)을 배치하고, 상세 탭에는 정체성/관계/특성/스탯/성향을 분리해 재사용 가능한 구조로 템플릿화했다.
- 6대 스텟은 레이더 차트로, 성향 3축은 바 차트로 시각화했으며 시드/토러스/메타 정보는 디버그 모드(`?debug=1` 또는 `localStorage.newtheria.debugMode=1`)에서만 노출되도록 정리했다.
- NPC 테스트 화면 버전을 `ver.0.2.94(260425-캐릭터정보카드템플릿화)`로 갱신했다.

## ver.0.2.93(260425-관리자디버그모드로컬스토리지연동)

- 시작 화면 관리자 디버그 모드를 localStorage(`newtheria.adminDebugMode`)와 연동해, 새로고침 후에도 디버그 상태를 유지할 수 있게 했다.
- 코드 상단 기본값은 `ADMIN_DEBUG_MODE_DEFAULT`로 유지하고, 저장값이 없을 때만 기본값을 사용하도록 우선순위를 정리했다.
- 콘솔 디버깅 편의를 위해 `window.NewtheriaDebug.getAdminDebugMode()` / `setAdminDebugMode()`를 노출했다.

## ver.0.2.92(260425-시작화면관리자디버그모드토글)

- 시작 화면 스크립트 최상단에 `ADMIN_DEBUG_MODE` 상수를 추가해, 코드에서 한 줄 수정으로 관리자 디버그 모드를 켜고 끌 수 있게 했다.
- 관리자 디버그 모드가 `true`일 때만 `테스트 허브` 버튼이 노출되도록 변경했고, 기본값은 지속 디버깅을 위해 `true`로 설정했다.
- `body.admin-debug-mode` 클래스를 함께 토글하도록 해 향후 디버그 UI/기능을 같은 플래그 기준으로 확장할 수 있게 정리했다.

## ver.0.2.91(260424-관측소템플릿원형팬재정렬)

- 관측소 인물 카드를 커스텀 박스 대신 공용 카드 템플릿(`card_templates`)으로 렌더링하도록 변경했다.
- 관측소 카드 배치를 대형 원의 하단 호(arc) 구간에 재정렬해 화면에는 원형 팬의 일부만 보이도록 조정했다.
- 카드 반사광 효과를 제거하고, 테두리 없는 불투명 글라스 톤 중심으로 시각 스타일을 단순화했다.
- 화면 버전 표기를 `ver.0.2.91(260424-관측소템플릿원형팬재정렬)`으로 갱신했다.

## ver.0.2.90(260424-원형팬관측소유리카드개편)

- `npc_test.html` 관측소/가문 트리 구조를 요청한 콘셉트에 맞춰 재배치하고, 버전 표기를 갱신했다.
- `assets/css/npc_test.css`에서 격자형 별 배경을 불규칙 성운형으로 바꾸고, 관측소를 대형 원형 팬 카드 배치 + 불투명 글라스 반사광 스타일로 개편했다.
- `assets/js/npc_test.js`에서 검색 Enter 시 팬 회전 후 대상 프로필 팝업이 열리도록 추가하고, 캐릭터/특성 상호작용 요소를 버튼 대신 주변 서식과 맞춘 엔티티 스타일로 변경했다.
- 패밀리 트리는 기존 목록형에서 원형 노드 중심의 세대 행 배치(상단 조상 / 중앙 본인 / 하단 후손) 방식으로 재구성했다.

## ver.0.2.89(260424-세로혈통트리개편)

- 패밀리 트리 모달 구조를 좌우 2분할에서 세로 혈통 구조로 변경했다.
- 기준 인물을 중앙 루트 배지로 고정하고, 위 영역은 조상 라인, 아래 영역은 후손 라인으로 표시하도록 개편했다.
- 가문 트리 안내 문구와 UI를 사용자 요청 의미(위=조상, 아래=후손)에 맞춰 정렬했다.

## ver.0.2.88(260424-완전신규아스트랄ux)

- `npc_test.html/css/js`를 사실상 신규 화면으로 재작성해 기존 NPC 테스트 UX 구조를 전면 폐기하고 새 인터랙션 흐름으로 교체했다.
- 화면 구조를 관측소/속궁합/가문 3개 관제 스크린으로 분리하고, 카드 템플릿 액션 덱 기반 진입 흐름을 새로 구성했다.
- 특성은 이름만 노출하고 클릭 시 팝업으로 설명/메타 정보를 제공하며, 가문 패밀리 트리는 조상 라인/후손 라인 이중 트리로 분리했다.

## ver.0.2.87(260424-별테마NPC리뉴얼)

- `npc_test.html`에 별 레이어 배경과 검색 입력을 추가하고, 버전 문자열을 최신화했다.
- `npc_test.css`를 테두리 없는 불투명 글라스 중심으로 조정하고, 별빛 트윙클/드리프트 이펙트와 검색 필드 스타일을 추가했다.
- `npc_test.js`에 실시간 검색 필터를 추가하고, 가문 트리를 조상 트리/후손 트리로 분리해 패밀리 트리 가독성을 높였다.

## ver.0.2.86(260424-npc테스트전면재구축)

- `npc_test.html/css/js`를 기존 구조와 분리된 새 레이아웃으로 재작성해 불투명 글라스 스타일 기반의 탭+팝업 UX로 전환했다.
- NPC 카드/상세/비교/특성/가문 UI를 역할별 팝업으로 분리하고, 카드에서는 특성 이름만 노출한 뒤 클릭 시 메타정보 팝업을 열도록 정리했다.
- 가문 클릭 시 패밀리 트리(가계도) 팝업을 열어 부모/자녀 축을 최대 4세대로 시각화하고, 속궁합 비교를 좌측 프리뷰와 전용 팝업 양쪽에서 확인할 수 있게 했다.

## ver.0.2.83(260424-npc테스트ui전면리뉴얼)

- `npc_test.html/css`를 탭+팝업 중심 레이아웃으로 전면 개편해 요약/컨텍스트/비교 프리뷰를 분리했다.
- NPC 카드에 `비교 A/B 지정` 액션을 추가하고, 별도 비교 팝업에서 두 캐릭터의 속궁합 점수를 즉시 확인할 수 있게 했다.
- 상세 정보 팝업/비교 팝업/카드 인터랙션의 시각 효과를 정리해 더 깔끔한 UI와 다이내믹한 사용 경험을 제공하도록 개선했다.

## ver.0.2.82(260424-시드토러스속궁합계산)

- 속궁합 계산 방식을 히든 신체 스탯 기반에서 `uniqueSeed` 기반으로 변경했다.
- 시드는 `payload(9자리)=x.y.z 좌표` + `checksum(마지막 1자리)` 구조로 생성하도록 리팩터링했다.
- 토러스 거리 기반 점수식 `Score = 100 * (1 - (distance / maxDistance)^p)`을 적용하고, 평균 50 근처 분포를 위해 `p=1.2`를 기본값으로 설정했다.

## ver.0.2.81(260424-npc혼인연문상수리팩터링)

- `npc_test.js`의 혼인/연문/히든 속궁합 관련 매직넘버를 상수(`ADULT_AGE`, `SPOUSE_MAX_AGE_GAP`, `INTIMACY_BALANCE` 등)로 추출해 밸런스 조정 지점을 단일화했다.
- 특성 id 하드코딩을 `TRAIT_ID` 상수로 치환하고 성인 판정을 `isAdult` 헬퍼로 통일해 조건식 중복을 줄였다.

## ver.0.2.80(260424-특성메타한줄평추가)

- 특성의 기능 설명(`description`)과 별도로 메타 한줄평 데이터를 `TRAIT_META_REVIEW_BY_ID`로 추가했다.
- NPC 상세 패널의 후천 특성 라벨에 `특성이름 · 메타 한줄평`을 함께 표시하도록 개선했다.

## ver.0.2.79(260424-히든속궁합스탯추가)

- 성인 NPC에 한해 성별별 히든 신체 스탯(남성 길이/여성 깊이)을 생성하도록 추가했다.
- 배우자 관계가 있을 때 히든 스탯 차이 기반 `속궁합(%)`을 계산해 상세 패널에 표시하도록 반영했다.
- 히든 스탯은 내부 계산 전용으로 유지하고, 미성년 NPC는 관련 값이 항상 `null`이 되도록 처리했다.

## ver.0.2.78(260424-혼인예외특성페티시분류)

- `근친 성향(313)`/`가능충(314)` 특성을 `기타`에서 `페티시` 분류로 변경해 관리 카테고리를 통일했다.

## ver.0.2.77(260424-근친가능충혼인규칙추가)

- NPC 특성에 `근친 성향(313)`/`가능충(314)`을 추가했다.
- 배우자 매칭 규칙을 확장해, `근친 성향`이 있으면 형제(이복 포함) 간 혼인을 허용하고 `가능충`이 있으면 이종 간 혼인을 허용하도록 변경했다.
- 이종 혼인 예외가 생겨도 자녀 생성은 불가하도록 가족 링크 생성 로직에 안전 차단 조건을 추가했다.

## ver.0.2.76(260424-형제배우자매칭방지)

- `npc_test.js`에 형제(이복 포함) 판정 유틸(`areSiblings`)을 추가하고, 해당 관계는 배우자 매칭 후보에서 제외하도록 수정했다.
- 배우자 무결성 안전망에 형제 관계 검증을 추가해, 데이터 오염 시 형제 간 `spouseId`를 자동 해제하도록 보강했다.
- NPC 생성 파이프라인 순서를 조정해 가족 링크를 먼저 확정한 뒤 배우자 매칭을 수행하도록 변경했다.

## ver.0.2.75(260424-혼인시여성성씨유지)

- 혼인 예외 규칙을 조정했다: 여성은 결혼 후 배우자 가문(`familyId`)으로만 편입되고, 본인 성씨(`surname`)는 유지한다.
- `npc_test.js`의 배우자 매칭 처리에서 여성 성씨/전체 이름을 배우자 기준으로 덮어쓰던 로직을 제거했다.
- NPC/PC 정보 템플릿 문서에 "혼인 시 여성 성씨 유지" 정책을 반영했다.

## ver.0.2.74(260424-성씨기반가문및혼인편입규칙)

- NPC 가문 생성 규칙을 성씨 기준으로 재정의했다: 기본적으로 `surname -> familyId/family`가 일치하도록 생성한다.
- 결혼 예외 규칙을 반영해, 여성 NPC는 배우자 연결 시 배우자의 성씨/가문으로 편입되도록 처리했다.
- 자녀 성씨/가문은 아버지 기준 계승을 유지하고, 아버지 정보가 없을 때 어머니 성씨로 자동 덮어쓰지 않도록 조정했다.
- NPC/PC 정보 템플릿 문서에 성씨-가문/혼인/자녀 계승 규칙을 명시했다.

## ver.0.2.73(260424-배우자링크추가)

- NPC 관계 데이터에 `familyLinks.spouseId`를 추가해 배우자 관계를 저장할 수 있게 했다.
- `npc_test.js`에서 종족/성별/나이 조건 기반 배우자 자동 매칭(`assignSpouseLinks`)을 추가하고, 상세 카드/리스트 관계 요약에 배우자 정보를 표시했다.
- 샘플 캐릭터 데이터(`start_screen.js`)와 NPC/PC 정보 템플릿 문서에 `spouseId` 필드를 반영했다.
- 데이터 무결성 검증기(`character_data_utils.js`)에 배우자 참조 존재성 및 상호 참조 일관성 검사 로직을 추가했다.

## ver.0.2.72(260424-가문id표시명분리)

- 가문 데이터를 `familyId`(저장/참조)와 표시명(`family`) 분리 정책으로 정리했다.
- `npc_test.js`에서 가문 카탈로그를 `{ id, name }` 구조로 바꾸고, NPC 생성/관계 계승/컨텍스트 인덱스를 `familyId` 기준으로 동작하도록 수정했다.
- UI 상세 카드/메타 표시에서는 `familyId`를 표시명으로 변환해 노출하도록 변경했다.
- 샘플 캐릭터(`start_screen.js`)와 NPC/PC 정보 템플릿 문서에 `familyId` 필드를 반영했다.

## ver.0.2.71(260424-가문숫자코드패밀리네임전환)

- NPC 생성용 `FAMILY_POOL`을 숫자 코드형 가문명에서 패밀리 네임형 가문명으로 교체했다.
- 가문 필드 가이드를 "숫자 코드 권장"에서 "패밀리 네임 기반 문자열 사용"으로 변경했다.
- NPC/PC 정보 템플릿 예시의 `family` 값을 `패밀리네임가문 | null` 형식으로 업데이트했다.

## ver.0.2.70(260424-종족계승규칙필드중복제거)

- 캐릭터 개별 데이터에서 중복 메타 필드였던 `raceInheritanceRule`/`raceRule` 사용을 제거하고, 종족 계승 규칙을 전역 기본 정책으로 일원화했다.
- `npc_test.js` 상세 카드에서 `종족 계승 규칙` 행을 제거해 실제 동작에 쓰이지 않는 표시 항목 노출을 정리했다.
- NPC/PC 정보 템플릿 문서에 "종족 계승은 시스템 전역 규칙이며 캐릭터별 중복 필드를 두지 않는다"는 정책을 명시했다.

## ver.0.2.69(260424-객체클릭컨텍스트패널확장)

- NPC 테스트 패널에 `객체 컨텍스트` 영역을 추가해, 이름/종족/가문/특성 객체를 클릭했을 때 관련 NPC 목록을 즉시 확인할 수 있게 했다.
- 런타임 인덱스(`byId`, `byRace`, `byFamily`, `byTrait`)를 도입해 객체 클릭 시 전체 순회 없이 빠르게 대상 목록을 조회하도록 개선했다.
- 상세 카드에서 종족/가문/특성을 클릭 가능한 객체 버튼으로 렌더링하고, 부모/자녀 이름 클릭 이동과 함께 객체 탐색 흐름을 확장했다.

## ver.0.2.68(260424-이름객체화클릭정보카드연결)

- NPC 이름 데이터를 문자열에서 객체(`name.surname`, `name.given`, `name.full`)로 전환해, 성/이름/전체명을 용도별로 안정적으로 활용할 수 있게 했다.
- 리스트 카드에서 이름 자체를 버튼으로 분리하고, 이름 클릭 시 해당 NPC 상세 정보 카드(팝업)가 열리도록 상호작용을 추가했다.
- 상세 정보 카드의 부모/자녀 이름도 클릭 가능한 객체형 버튼으로 렌더링해, 관계 이름을 눌러 다른 NPC 카드로 즉시 이동할 수 있게 개선했다.

## ver.0.2.67(260424-종족별중세판타지이름체계반영)

- 사용자 제공 리스트를 기준으로 종족별(용인/엘프/드워프/인간) 남성·여성 이름과 성씨를 중세 판타지 스타일로 전면 교체했다.
- 이름 풀 파일 구조를 평면 배열에서 `race -> male/female/surnames` 구조로 개편해, 종족/성별에 맞는 이름 조합이 나오도록 생성 로직을 고도화했다.
- `npc_test.js`에서 종족 키 매핑 및 이름 풀 유효성 검증을 추가하고, 외부 파일 로드 실패 시에도 종족별 fallback 풀로 동작하도록 보강했다.

## ver.0.2.66(260424-npc이름풀확장레이더차트)

- NPC 종족 풀을 `엘프/인간/드워프/용인` 4종으로 고정하고, 랜덤 생성 로직에 동일 규칙을 반영했다.
- 이름 중복 체감을 줄이기 위해 이름 데이터를 `assets/data/npc_name_pools.json`으로 분리하고 성/이름 풀을 대폭 확장했다.
- NPC 고유 시드를 고정 증가값 대신 10자리 랜덤 값으로 생성하도록 바꿨고, 1회 생성 사이클 내 중복도 방지했다.
- 상세 정보창에 6대 스탯 레이더 차트(SVG)를 추가하고, 팝업/차트가 위로 올라오며 나타나는 애니메이션을 적용해 시각 완성도를 높였다.

## ver.0.2.65(260424-npc랜덤생성가족관계팝업UI)

- `NPC 템플릿 테스트` 페이지를 전면 교체해, 랜덤 NPC 100명을 즉시 생성/재생성하는 전용 테스트 화면으로 개편했다.
- 나이·종족 조건을 기반으로 부모/자식 연결을 자동 생성하고, 리스트 카드에서 관계 요약(부모 연결 수/자녀 수)을 바로 확인할 수 있게 했다.
- NPC 카드를 클릭(또는 키보드 Enter/Space)하면 상세 팝업이 열리도록 구현해, 가족 관계/특성/기본 메타데이터를 세련된 모달 UI에서 조회할 수 있게 했다.

## ver.0.2.64(260424-테스트창공통허브복귀버튼위치통일)

- 모든 테스트 창에서 `테스트 허브`로 돌아가는 버튼을 우상단 고정 위치로 통일했다. (`dialogue_prototype_test.html`, `world_map.html`, `npc_test.html`)
- `NPC 템플릿 테스트` 페이지의 하단 푸터 링크를 제거하고, 다른 테스트 창과 동일한 공통 복귀 버튼만 노출되도록 정리했다.
- 각 테스트 페이지 스타일 파일/인라인 스타일에 `.test-hub-button` 규칙을 추가해 위치/시각 스타일을 일관되게 맞췄다.

## ver.0.2.63(260424-npc템플릿테스트페이지추가)

- 테스트 허브(`test_mode.html`)에 `NPC 템플릿 테스트` 진입 카드를 추가했다.
- 신규 페이지 `npc_test.html`을 추가해 NPC JSON 편집/렌더 미리보기 흐름을 빠르게 검증할 수 있게 했다.
- `assets/js/npc_test.js`에서 `slot1`의 NPC 자동 불러오기와 기본 템플릿 복원/렌더 로직을 제공하고, `assets/css/npc_test.css`로 테스트 UI 스타일을 분리했다.

## ver.0.2.62(260423-문서번호중복해소및오해수정롤백)

- 사용자 지적 사항에 맞춰 문서 번호 중복을 해소했다: `00_문서운영가이드.md` → `98_문서운영가이드.md`, `03_대사작성문법.md` → `14_대사작성문법.md`.
- 문서 인덱스(`docs/README.md`)와 변경 이력 내 관련 경로를 신규 파일명 기준으로 일괄 정리했다.
- 직전 오해로 추가했던 00/03 경계 설명 섹션을 롤백해, 요청 의도(번호 중복 해소)에 맞는 변경만 남기도록 정리했다.

## ver.0.2.61(260423-00과03문서경계정리)

- 사용자 피드백에 맞춰 `00_MASTER_SPEC.md`와 `03_국가수도규칙.md`의 역할 경계를 명시했다.
- 00에는 국가/수도의 연결 개요만 유지하고, 상세 규칙값/조건식/예외는 03에서만 관리하도록 문서 정책을 분리했다.
- 03 문서에 "문서 범위"와 "소유 규칙" 섹션을 추가해 중복 작성 포인트를 줄였다.

## ver.0.2.60(260423-상태변경api및세이브직렬화규칙)

- 캐릭터 상태 변경 API를 추가했다: `applyCurrencyDelta`, `moveCharacterLocation`, `addInventoryItem`, `removeInventoryItem`.
- 세이브/불러오기 직렬화 규칙을 추가했다: `serializeCharacterState`, `deserializeCharacterState`, `SAVE_SCHEMA_VERSION`.
- 시작 화면 기본 세이브 구조에 `saveSchemaVersion: '1.0.0'`를 기록해 마이그레이션 기준점을 마련했다.

## ver.0.2.59(260423-캐릭터상태필드및숙원참조통합)

- 캐릭터 스키마에 개인 상태 필드(`wallet`, `location`, `inventory`)를 추가해 소지금/위치/보유 아이템 정보를 저장할 수 있도록 확장했다.
- 캐릭터 숙원 참조 필드를 `aspiration` 객체(`currentId`, `poolId`)로 통합해 중복 키(`aspirationId`, `aspirationPoolRefId`, `aspirationPoolRef`)를 정리했다.
- 무결성 검증기에 소지금/좌표/아이템 수량 검증을 추가하고, 숙원 참조 검증도 통합 객체 기준으로 갱신했다.

## ver.0.2.58(260423-ai행동연결및툴링검증추가)

- `character_data_utils`에 행동 점수 계산/선택 유틸(`scoreActionByAspiration`, `chooseActionByAspiration`)을 추가해 숙원 기반 AI 판단 규칙을 코드로 명세했다.
- 공용 풀 운영 검증용 스크립트 `scripts/validate_character_data.mjs`를 추가해 id 유일성/참조 무결성을 CLI에서 점검할 수 있게 했다.
- 템플릿 문서에 12) AI 행동 연결 명세, 13) 운영/툴링 명세를 추가해 6번/7번 항목의 적용 기준을 명확히 했다.

## ver.0.2.57(260423-무결성검증및인덱스유틸추가)

- `assets/js/character_data_utils.js`를 추가해 데이터 무결성 검증(`validateCharacterData`)과 id 기반 조회 인덱스 생성(`buildCharacterIndexes`) 유틸을 도입했다.
- 시작 화면에서 샘플 캐릭터 시드 직전에 무결성 검증을 수행하고, 공용 풀은 비동기로 프리로드해 인덱스를 구성하도록 연결했다.
- 문서 템플릿에 무결성 검증 규칙과 런타임 인덱스 규칙을 추가해, 1번(무결성)·3번(조회 최적화) 작업 기준을 명시했다.

## ver.0.2.56(260423-부모자식id참조필드추가)

- 사용자 요청에 맞춰 캐릭터 스키마에 혈연 id 참조 필드를 추가했다: `familyLinks.fatherId`, `familyLinks.motherId`, `familyLinks.childrenIds`.
- 시작 화면 샘플 캐릭터(순례자/아스테리아)에도 동일 구조를 반영하고, 부모 미확정 시 `null`, 자식 없음은 빈 배열(`[]`)로 저장하도록 정리했다.
- 템플릿 문서 예시/규칙을 업데이트해 부모/자식 관계를 문자열이 아닌 캐릭터 id로 참조하도록 명시했다.

## ver.0.2.55(260423-풀특성id참조최적화)

- 사용자 제안에 맞춰 선천 특성과 숙원 참조를 id 기반으로 정리했다. (`innateTraitIds`, `aspirationPoolRefId`, `aspirationId`)
- 공용 풀 파일에서 숙원 구조를 `aspirationPoolCatalog(id-목록)` + `aspirationDefinitions(상세)`로 분리해 문자열 중복을 줄였다.
- 템플릿 문서에 id 우선 저장 정책을 추가해 세이브 용량 최적화 기준을 명시했다.

## ver.0.2.54(260423-시드코드구조정리)

- 시작 화면 캐릭터 시드 코드를 정리해 상수/빌더 함수(`CHARACTER_POOLS_PATH`, `buildSampleCharacters`, `buildDefaultSaveSlotsData`) 기반 구조로 리팩터링했다.
- 샘플 캐릭터 규칙(ID 고정, 무가문 표기, 숙원 풀 참조)을 함수 주석에 명시해 유지보수 시 의도를 빠르게 파악할 수 있게 했다.
- 슬롯 데이터 생성을 팩토리 함수로 분리해 향후 샘플 캐릭터/슬롯 확장 시 변경 지점을 단일화했다.

## ver.0.2.53(260423-가문표기규칙및무가문캐릭터반영)

- `family` 필드 의미를 "가문"으로 고정하고, 값이 있는 경우 `000가문` 형식 사용 규칙을 문서에 명시했다.
- 순례자/아스테리아는 설정상 가문이 없으므로 샘플 캐릭터의 `family`를 `null`, `surname`을 빈 문자열로 조정했다.
- NPC/PC 템플릿 문서에 무가문 캐릭터 표기 규칙(`family=null`, `surname=""`)을 추가했다.

## ver.0.2.52(260423-숙원행동규칙스키마정의)

- 공용 풀 파일에 `selectionPolicy`를 추가해 숙원 선택 절차(조건 필터링 → 우선순위 정렬 → 가중 추첨 → 1개 확정)를 명시했다.
- 각 숙원 항목에 `behaviorProfile` 구조를 도입해, 선택 이후 캐릭터의 전반적 행동 방향/양식(결정 편향, 선호/회피 행동, 장기 지침)을 데이터로 정의했다.
- 템플릿 문서에 "게임 반영 기준" 섹션을 추가해 이벤트/전투/대사 시스템이 숙원 데이터를 어떻게 소비해야 하는지 규칙을 구체화했다.

## ver.0.2.51(260423-공용풀외부파일분리)

- 사용자 피드백에 따라 선천 특성/숙원 공용 풀을 세이브 데이터에서 제거하고 외부 파일(`assets/data/character_common_pools.json`)로 분리했다.
- 세이브 슬롯에는 공용 풀 원본 대신 `catalogRefs.characterPools` 경로 참조만 저장하도록 변경했다.
- NPC/PC 템플릿 문서에 "외부 파일 관리 + 세이브 참조" 구조를 반영해 데이터 용량 정책을 명확히 했다.

## ver.0.2.50(260423-숙원풀공용카탈로그분리)

- 용량 최적화를 위해 숙원 후보를 캐릭터별 중복 저장하지 않고, 슬롯 공용 `aspirationPoolCatalog`로 분리했다.
- 각 캐릭터에는 실제 후보 배열 대신 `aspirationPoolRef`만 저장하도록 스키마를 단순화했다.
- 템플릿 문서에 공용 숙원 풀 분리 저장 예시를 추가해, 캐릭터 스키마와 숙원 카탈로그 스키마를 분리 명시했다.

## ver.0.2.49(260423-숙원풀및10자리시드선천특성카탈로그)

- 캐릭터 고유 시드 규칙을 10자리 숫자 문자열로 통일하고, 기본 샘플 시드도 해당 포맷으로 갱신했다.
- 숙원 모델을 `aspirationCandidates`에서 `aspirationPool`로 개편해, 캐릭터마다 고정 숙원을 두지 않고 "조건 충족 후보 중 하나"를 선택하는 구조를 명시했다.
- PC/NPC 샘플에 다중 숙원 후보를 추가하고 우선순위(`priority`) 필드를 함께 배치했다.
- 슬롯 기본 데이터에 게임용 선천 특성 카탈로그 6종을 추가하고, 템플릿 문서에도 동일 추천 목록을 정리했다.

## ver.0.2.48(260423-캐릭터id고정매핑반영)

- 사용자 피드백을 반영해 캐릭터 ID 고정 규칙을 적용했다: `0=아스테리아`, `1=플레이어(순례자)`.
- 시작 화면 기본 슬롯 샘플에서 `asteria_npc.id`를 `0`, `pilgrim_pc.id`를 `1`로 조정하고 고유 시드 번호도 함께 정렬했다.
- NPC/PC 템플릿 문서에 동일한 고정 매핑 규칙을 명시해 작성 기준을 통일했다.

## ver.0.2.47(260423-npcpc필수필드및숙원규칙반영)

- NPC/PC 템플릿을 사용자 지정 필드 기준으로 재정의했다: `id(0부터)`, `uniqueSeed`, `이름/성`, `성별`, `나이/신장/체중`, `종족`, `traits(선천/후천)`, `layers`, `6대 스텟`, `alignment`, `aspiration`.
- 시작 화면 기본 슬롯 캐릭터 샘플을 새 템플릿에 맞춰 갱신하고, 각 필드 의도를 코드 주석으로 보강했다.
- 숙원은 초기 `null`에서 시작하고, `피의 복수`/`세계 제일의 상인` 같은 이벤트 조건형 후보를 명시해 확장 경로를 정리했다.

## ver.0.2.46(260423-npcpc정보템플릿및슬롯샘플추가)

- 사용자 요청에 맞춰 NPC/PC 공용 캐릭터 정보 스키마 문서를 신설했다. (`docs/13_NPC_PC_정보템플릿.md`)
- 문서 인덱스/마스터 스펙에 신규 템플릿 문서를 연결해 참조 경로를 정리했다.
- 시작 화면 기본 세이브 슬롯(`slot1`)에 PC/NPC 샘플 캐릭터 정보를 추가해, `id/name/layers` 호환 필드와 확장 `profile` 메타데이터를 함께 시드하도록 보강했다.

## ver.0.2.45(260423-화면효과단일키복합값지원)

- 사용자 피드백에 맞춰 `screen_flash`, `screen_blackout` 단일 키에서 `지속시간,변화시간` 복합값 문법을 지원했다. (예: `screen_flash = 220,120`)
- 기존 분리 키(`screen_flash_fade`, `screen_blackout_fade`)도 유지해 하위 호환을 보장하고, 단일 키 복합값이 있는 경우 변화시간 파싱에 함께 반영되도록 확장했다.
- 샘플 대사/문법 문서/테스트 힌트를 단일 키 복합값 방식 기준으로 동기화했다.

## ver.0.2.44(260423-페이드단독지정기본지속시간지원)

- 화면 효과 문법에서 `screen_flash_fade`/`screen_blackout_fade`를 단독으로 지정해도 동작하도록 런타임 조건을 확장했다.
- `*_fade` 단독 사용 시 기본 지속시간을 적용하도록 정책을 명시했다. (`flash=220ms`, `blackout=700ms`)
- 문법 문서/테스트 힌트에 "fade 단독 지정 가능" 규칙을 추가해 작성 혼란을 줄였다.

## ver.0.2.43(260423-화면효과변화시간문법추가)

- 화면 번쩍임/암전 연출에 변화시간을 줄 수 있도록 `screen_flash_fade`, `screen_blackout_fade` 문법(별칭 포함)을 추가했다.
- 화면 효과 오버레이를 키프레임 기반에서 `opacity transition` 기반으로 정리해, 지정한 변화시간(ms) 동안 서서히 켜지고/꺼지도록 동작을 개선했다.
- 샘플 대사/문법 문서/테스트 힌트를 신규 변화시간 문법 기준으로 동기화했다.

## ver.0.2.42(260423-speaker글리치실시간갱신수정)

- 화자(`speaker`)와 본문(`line`)의 글리치 타이머를 분리해, 본문 타자기/일반 렌더 시 정리 로직이 화자 글리치 애니메이션까지 중단시키던 문제를 수정했다.
- 인라인 효과 렌더러에 scope 개념을 추가해 `speaker`와 `line` 각각 독립적으로 생성/정리되도록 정리했다.
- 대화 블록 전환 시에는 전체 글리치 타이머를 정리하되, 동일 블록 렌더 안에서는 화자/본문을 독립 갱신하도록 호출 순서를 보강했다.

## ver.0.2.41(260423-대사화면번쩍임암전문법추가)

- 대사 블록 문법에 `screen_flash`(별칭: `flash`, `fx_flash`)와 `screen_blackout`(별칭: `blackout`, `fade_black`) 키를 추가해 화면 번쩍임/암전 연출을 block 단위로 선언할 수 있게 했다.
- 대화 런타임에 전용 오버레이 레이어(`.dialogue-prototype__screen-fx`)를 추가하고, 지정된 ms 동안 번쩍임/암전이 자동 재생 후 해제되도록 동작을 연결했다.
- 문법 가이드/테스트 힌트/샘플 대사를 신규 화면 연출 문법 기준으로 동기화했다.

## ver.0.2.40(260423-글리치폭고정및스포서식동기화)

- 글리치 텍스트가 매 프레임 글자 폭이 달라져 뒷 문장이 흔들리던 문제를 보완하기 위해, 원문 폭을 먼저 고정 확보한 뒤 그 영역 안에서 글리치 문자열만 교체되도록 렌더 구조를 개선했다.
- 스포일러 토큰 스타일을 기본 버튼 서식에서 대사 본문 서식 상속 방식으로 전환해, 글자 크기/행간/자간/색상이 원래 대사와 동일하게 보이도록 정리했다.

## ver.0.2.39(260423-speaker인라인효과지원)

- 기존 인라인 스포/깨진글 효과가 `line`에만 적용되던 제한을 해소하고, `speaker`에도 동일 문법이 동작하도록 렌더러를 확장했다.
- 화자명과 본문이 같은 프레임에서 인라인 효과를 함께 써도 글리치 타이머가 유지되도록 렌더 순서/정리 시점을 보강했다.
- 문법 가이드와 테스트 페이지 힌트에 인라인 효과 적용 범위(`line/speaker`)를 명시했다.

## ver.0.2.38(260423-스포방지깨진글문법및스킵정지확장)

- `UI 숨김` 버튼을 아이콘 버전에서 텍스트 버전(`UI 숨김`/`UI 표시`)으로 되돌렸다.
- 스킵 가속 정지 조건을 확장해, 기존 `선택지/씬전환/end` 외에 `skip_lock`이 걸린 블록에서도 자동 정지하도록 보강했다.
- 대사 인라인 문법에 스포 방지(`[[spoiler:...]]`, `[[스포:...]]`)를 추가해 블러 처리된 텍스트를 터치로 공개/비공개 전환할 수 있게 구현했다.
- 스포 방지 토큰 클릭은 대사 진행 입력으로 처리되지 않도록 이벤트를 분리해, 누를 때 대사가 넘어가지 않게 정리했다.
- 대사 인라인 문법에 깨진 글(`[[glitch:...]]`, `[[깨진글:...]]`)을 추가해 실시간으로 변형되는 문자열 표현을 지원했다.
- 문법 가이드/테스트 힌트를 신규 인라인 효과 문법 기준으로 동기화했다.

## ver.0.2.37(260423-ui숨김아이콘전환및진행입력잠금)

- 좌하단 `UI 숨김` 버튼을 눈 아이콘 기반 토글(표시: `👁`, 숨김 중: `🙈`)로 변경해 상태를 직관적으로 확인할 수 있게 했다.
- UI 숨김 모드에서는 배경/스탠딩 감상에 집중할 수 있도록, 화면 탭/클릭으로 대사가 넘어가지 않게 진행 입력을 잠그는 가드를 추가했다.
- 접근성을 위해 아이콘 상태에 맞춰 `aria-label`/`title`이 `UI 숨기기`/`UI 표시`로 함께 갱신되도록 정리했다.

## ver.0.2.36(260423-화자미지정시이름빈값처리)

- 대사 블록에서 `speaker`/`actor(speaker_id)`를 모두 지정하지 않았을 때 화자명이 `???`로 노출되던 표시를 빈 값으로 변경했다.
- 연출 전용 블록(배경/파티클/대기 등)에서 의도치 않은 가짜 화자 라벨이 나타나지 않도록 `resolveActorLabel()`의 fallback 정책을 정리했다.

## ver.0.2.35(260423-모바일스탠딩애니메이션정렬밀림수정)

- 모바일에서 특정 블록 전환 시 스탠딩이 오른쪽으로 간헐적으로 치우치던 원인을 `transform` 충돌로 확인했다.
- 기존에는 `standing_animation`이 슬롯 컨테이너(`.dialogue-prototype__actor`)에 직접 적용되어, 모바일 포커스 정렬용 `translate/scaleX`와 키프레임 `transform`이 서로 덮어쓰는 문제가 있었다.
- 애니메이션 적용 대상을 슬롯 컨테이너에서 실제 일러 레이어(`.dialogue-prototype__actor-layer`)로 옮겨, 정렬 transform과 연출 transform이 분리되도록 수정했다.
- 관련 CSS에 원인/해결 의도를 주석으로 명시해 동일 유형 회귀를 방지했다.

## ver.0.2.34(260423-대사스킵가속UI숨김추가)

- 대사창 `SKIP` 버튼에 실제 동작을 연결해, 현재 위치부터 빠르게 전개하며 `선택지`, `씬 전환 직후`, `종료(end)` 지점에서 자동 정지하도록 구현했다.
- 대사 문법에 `skip_lock`(별칭: `skip_disabled`, `skip`) 제어를 추가해 스킵 불가 상태를 block 단위로 선언할 수 있게 했고, 별도 해제가 없으면 scene 전환 전까지 잠금이 유지되도록 반영했다.
- 대사창 좌하단에 `UI 숨김` 버튼을 추가해 패널/선택지 UI를 토글할 수 있게 했고, 모바일에서는 UI 숨김 시 현재 포커스된 스탠딩 일러가 부드럽게 확대되도록 연출을 보강했다.
- 문법 가이드(`docs/14_대사작성문법.md`)와 테스트 페이지 힌트(`dialogue_prototype_test.html`)를 신규 스킵 잠금 문법 기준으로 동기화했다.

## ver.0.2.33(260423-샘플대사액터애니메이션적용)

- 사용자 후속 요청에 맞춰 샘플 대사(`assets/data/dialogue_sample.asdf`)의 스탠딩 애니메이션 구간을 actor id 지정 방식으로 갱신했다.
- `standing_animation_actor`와 인라인 actor 지정(`standing_animation = goddess:...,pilgrim:...`) 예시를 실제 대사 블록에 배치해, 좌/우 고정이 아닌 액터 기준 제어 흐름을 바로 확인할 수 있게 했다.
- 일부 블록에는 슬롯 키(`standing_animation_left`)·전체 키(`all:fade`)와 actor 지정 키를 함께 배치해 우선순위 동작을 샘플에서 검증할 수 있도록 주석과 함께 정리했다.

## ver.0.2.32(260423-스탠딩애니메이션액터지정확장)

- 사용자 피드백을 반영해 스탠딩 애니메이션을 좌/우 슬롯뿐 아니라 actor id 기준으로도 지정할 수 있게 확장했다.
- `standing_animation = goddess:float,pilgrim:shake` 같은 인라인 문법을 지원하고, 별도 키 `standing_animation_actor`도 동일 포맷으로 파싱하도록 엔진 로직을 보강했다.
- 렌더링 시 현재 좌/우 슬롯에 배치된 actor id와 매칭하여 actor 지정 애니메이션을 적용하고, 미지정 시 기존 좌/우/전체 fallback 우선순위를 유지하도록 정리했다.
- 대사 문법 문서에 actor 지정 애니메이션 예시를 추가했다.

## ver.0.2.31(260423-스탠딩애니메이션개별적용)

- 대화 블록에서 스탠딩 애니메이션을 좌/우 슬롯별로 독립 적용할 수 있도록 엔진 처리를 확장했다.
- `standing_animation_left`, `standing_animation_right` 키를 추가하고, 기존 `standing_animation`에 `left:float,right:shake` 같은 범위 지정 문자열도 지원하도록 파서를 보강했다.
- CSS 애니메이션 타겟을 배우 그룹(`.dialogue-prototype__actors`)에서 개별 배우 슬롯(`.dialogue-prototype__actor`) 기준으로 전환해, 한쪽만 다른 애니메이션을 재생할 수 있게 정리했다.
- 대사 문법 가이드에 개별 스탠딩 애니메이션 사용 예시를 추가했다.

## ver.0.2.30(260423-모바일스탠딩중앙전체노출)

- 사용자 요청에 맞춰 모바일 스탠딩을 좌/우 슬롯 오프셋 대신 중앙 기준(`left: 50%`)으로 통일했다.
- 모바일 스탠딩 레이어의 `background-size`를 `contain`으로 변경해, 화면 내에서 일러가 잘리지 않고 전체가 보이도록 조정했다.
- 포커스 등장 연출은 유지하되, 좌/우 모두 같은 중앙 위치에서 노출되도록 초기/표시 transform 규칙을 정리했다.

## ver.0.2.29(260423-모바일포커스슬라이드복원)

- 사용자 후속 요청에 따라 모바일 포커스 스탠딩의 중앙 고정 배치를 되돌리고, 기존 좌/우 슬롯 기반 슬라이드 인 동선을 복원했다.
- `.dialogue-prototype__actor--left/right`의 위치/초기 transform을 이전 값으로 환원해, 좌측은 좌측 외곽 대기 후 진입, 우측은 우측 외곽 대기 후 진입하도록 정리했다.
- 포커스 상태 transform도 `translate3d(0, 0, 0)` 기준으로 되돌려 기존 연출 체감과 일치시켰다.

## ver.0.2.28(260423-데스크탑라인축소모바일포커스중앙정렬)

- 데스크탑 대사창의 `line` 텍스트 크기를 `clamp(0.98rem, 1.9vw, 1.72rem)`로 조정해 과도한 확대 구간을 줄였다.
- 모바일 스탠딩 포커스 연출은 좌/우 슬롯 모두 `left: 50%` + `translateX(-50%)` 기준으로 통일해, 포커싱된 일러가 항상 화면 중앙에 보이도록 변경했다.
- 모바일 포커스 시 기존 슬라이드 인/아웃 이동을 제거하고 중앙 고정 노출 규칙으로 단순화했다.

## ver.0.2.27(260423-샘플대사원문로어대화재구성)

- 사용자 제공 원문 흐름을 기준으로 기존 샘플 대사를 전부 교체하고, 아스테리아-순례자 2인 대화 중심으로 재작성했다.
- 요청사항에 맞춰 아스테리아 말투를 시작부터 종료까지 동일 톤으로 유지하고, 자동 넘김(`wait`)과 선택지(`choice`, `hidden_choice`)는 과도하지 않게 최소 구간에만 배치했다.
- 문법 시연 목적을 유지하기 위해 배경/포커스/스탠딩/파티클/점프가능 선택/종료 등 핵심 키를 단일 샘플 시나리오 안에서 연속적으로 확인할 수 있게 정리했다.

## ver.0.2.26(260423-로어친화샘플대사전면개편)

- 사용자 요청에 맞춰 샘플 대사를 아스테리아(존대)와 순례자의 연속 서사 구조로 전면 재작성했다.
- 기존 샘플 분기 대화를 삭제하고, 우주의 밤·성역 도달·불안·섭리·새 차원 개시·자격의 고뇌·자유의지 선언·축복의 작별까지 이어지는 장문 대사 흐름으로 교체했다.
- 문법 시연 목적을 유지하기 위해 `choice/hidden_choice`와 `wait` 자동 넘김을 최소 횟수로 포함하고, `particle`, `standing_animation`, `move_left/right`, `hide`, `set_layers`, `end` 등 핵심 블록 키를 고르게 사용했다.

## ver.0.2.25(260423-모바일우측스탠딩반전크롭보정)

- 사용자 피드백 기준으로 우측 포커스 시 크롭 방향이 반대로 체감되는 문제를 재점검했다.
- 원인은 우측 슬롯 컨테이너가 `scaleX(-1)`로 반전되는 구조에서, 레이어 앵커를 `top left`로 주면 화면 기준 기대 방향과 반대로 보일 수 있다는 점이었다.
- 모바일 우측 슬롯 레이어 앵커를 `top right`로 수정해, 반전 후 화면 기준 안쪽(왼쪽) 가시 영역이 유지되도록 보정했다.

## ver.0.2.24(260423-모바일스탠딩내측가시정렬)

- 모바일 대화 UI에서 스탠딩이 `auto 100%`로 확대될 때 안쪽(대화창 방향) 얼굴/상반신이 잘리는 체감을 줄이기 위해 좌우 슬롯별 레이어 앵커를 분리했다.
- 왼쪽 슬롯은 `.dialogue-prototype__actor--left .dialogue-prototype__actor-layer { background-position: top right; }`로, 오른쪽 슬롯은 `.dialogue-prototype__actor--right .dialogue-prototype__actor-layer { background-position: top left; }`로 조정했다.
- 공통 `top center` 정렬은 유지하되 슬롯별 오버라이드 주석을 추가해, 이후 모바일 스탠딩 크롭 정책을 빠르게 파악할 수 있도록 정리했다.

## ver.0.2.23(260423-모바일스탠딩세로풀높이)

- 모바일 대화 UI에서 스탠딩 일러가 상단~하단(Top~Bottom)을 꽉 채우도록 정책을 조정했다.
- 모바일 슬롯 폭을 `100vw`로 고정하고, 실제 렌더 레이어(`.dialogue-prototype__actor-layer`) 스케일을 `background-size: auto 100%`로 변경해 높이 기준 풀사이즈를 보장했다.
- 기존 상단 앵커(`top center`)는 유지해 세로 화면에서도 머리 위 여백이 과도하게 생기지 않도록 정리했다.

## ver.0.2.22(260423-모바일스탠딩레이어앵커동기화)

- 모바일 스탠딩 상단 정렬 보정이 실제 렌더 레이어(`.dialogue-prototype__actor-layer`)에 누락되어, 세로 화면에서 일러가 아래로 깔려 보이던 원인을 수정했다.
- 모바일 미디어쿼리(`max-width: 760px`)에서 컨테이너(`.dialogue-prototype__actor`)와 레이어(`.dialogue-prototype__actor-layer`)의 `background-position`을 모두 `top center`로 동기화했다.
- 관련 주석을 보강해, 이후 유지보수 시 "컨테이너/레이어 선택자 불일치" 재발 포인트를 빠르게 파악할 수 있도록 정리했다.

## ver.0.2.21(260423-모바일스탠딩상단여백보정)

- 모바일 구간(`max-width: 760px`)에서 스탠딩 일러의 기준점을 `background-position: top center`로 조정했다.
- 기존 `bottom center` 앵커 때문에 일러 상단에 비어 보이던 여백 체감을 줄여, 모바일에서 캐릭터가 화면 위쪽부터 더 꽉 차게 보이도록 보정했다.
- 관련 CSS에 의도 주석을 추가해 유지보수 시 "상단 여백 제거 목적"을 바로 파악할 수 있게 정리했다.

## ver.0.2.20(260423-대사파티클연출추가)

- 대화 런타임에 파티클 오버레이 렌더를 연결해 블록 단위로 `particle` 프리셋을 지정할 수 있도록 확장했다.
- 신규 문법 `particle = snow|ember|off`, `particle_count`, `particle_duration`을 추가해 장면 분위기에 맞는 파티클 밀도/속도를 제어할 수 있게 했다.
- 샘플 ASDF와 테스트 페이지 문법 힌트, 대사 문법 문서를 파티클 문법 기준으로 동기화했다.

## ver.0.2.19(260423-대사변수치환타자기대기연출)

- ASDF 대화 런타임에 변수 치환을 추가해 `line`/`speaker`에서 `{player_name}` 같은 템플릿 토큰을 실시간으로 해석하도록 확장했다.
- `setVariables({...})`를 실제 동작으로 연결해 변수 패치 시 현재 블록을 즉시 재렌더하고, 디버그 로그에서 변경 키를 추적할 수 있게 했다.
- 블록 문법에 `type_speed`(타자기 속도)와 `wait`(자동 진행 대기 ms, `delay/pause/auto_next_ms` 별칭) 연출 키를 추가하고 샘플/가이드를 동기화했다.
- 대화 테스트 페이지의 문법 힌트와 변수 JSON placeholder를 신규 문법 기준으로 갱신했다.

## ver.0.2.18(260423-모바일스탠딩대형오프셋)

- 모바일 대화 UI에서 스탠딩 일러가 더 크게 보이도록 슬롯 폭을 `min(86vw, 620px)`로 확장해, 화면 높이에 가까운 체감 크기를 유지하도록 조정했다.
- 요청 의도에 맞춰 좌/우 스탠딩 앵커를 음수 오프셋으로 강화해, 왼쪽 스탠딩은 왼쪽으로·오른쪽 스탠딩은 오른쪽으로 각각 조금씩 삐져나오게 배치했다.
- 모바일 스탠딩 CSS 주석을 보강해 "배경을 일부 가리더라도 캐릭터 크기를 우선하는 정책"과 슬롯 오프셋 의도를 유지보수 시 바로 이해할 수 있도록 정리했다.

## ver.0.2.17(260423-버전폴백문구고정)

- 시작 화면/월드맵의 fallback 버전을 매번 수동 갱신하지 않도록 정책을 변경했다.
- 버전 문서 fetch/파싱 실패 시 표시값을 버전 문자열 대신 고정 문구 `version 불러오기 실패`로 통일했다.
- 관련 상수에 정책 의도 주석을 추가해 유지보수 시 배경을 빠르게 이해할 수 있도록 정리했다.

## ver.0.2.16(260423-씬내배경유지정책)

- 대화 런타임의 배경 적용 정책을 조정해, 같은 scene 안에서는 block에 `bg/background`가 명시된 경우에만 배경을 변경하도록 수정했다.
- 이제 scene 내부에서 배경 키를 생략하면 직전 block의 배경을 유지하고, scene이 바뀌었는데 새 scene 첫 block에 배경이 없을 때만 기본 `backgroundUrl`로 초기화한다.
- 관련 로직에 정책 의도를 설명하는 주석을 보강해 유지보수 시 동작 근거를 빠르게 파악할 수 있도록 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.16(260423-씬내배경유지정책)`으로 갱신했다.

## ver.0.2.15(260423-모바일포커스슬라이드연출)

- 모바일 스탠딩 높이를 `100svh` 기준으로 맞춰, 세로 화면에서 캐릭터가 작아 보이던 체감을 줄였다.
- 모바일에서는 좌/우 캐릭터를 기본적으로 화면 밖에 대기시키고, `standing_focus`로 지정된 캐릭터만 좌우에서 안쪽으로 슬라이드 인하도록 연출 규칙을 변경했다.
- 관련 CSS에 의도 주석을 보강해 "왜 한 명만 등장하는지"와 모바일 전용 동작 범위를 빠르게 이해할 수 있도록 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.15(260423-모바일포커스슬라이드연출)`으로 갱신했다.

## ver.0.2.14(260423-모바일스탠딩위치보정)

- 모바일 구간(`max-width: 760px`, `max-width: 520px`)에서 스탠딩 슬롯 기준점을 상향하고 크기 비율을 조정해, 일러가 하단에 과하게 붙어 "짧아 보이던" 체감을 완화했다.
- 모바일 스탠딩 CSS에 의도 주석을 보강해, 대화 패널 높이와 스탠딩 배치 간 상호작용을 유지보수 시 빠르게 이해할 수 있도록 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.14(260423-모바일스탠딩위치보정)`으로 갱신했다.

## ver.0.2.13(260423-일러디버그탭정리)

- 대화 프로토타입 디버그 스냅샷에 `standing` 상태를 추가해, 좌/우 슬롯 actor id, 포커스, hidden 목록, actor 레이어 목록, 실제 렌더 레이어 개수/가시성까지 한 번에 확인할 수 있도록 확장했다.
- 대화 테스트 설정창을 탭(`소스/시작`, `디버그`, `문법 힌트`)으로 분리해 항목이 많아져도 스크롤 부담 없이 필요한 설정만 빠르게 볼 수 있도록 정리했다.
- 디버그 탭에 `스탠딩 상태(일러 디버그)` 전용 출력창을 추가해 "왜 일러가 안 보이는지"를 UI에서 즉시 점검할 수 있도록 보강했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.13(260423-일러디버그탭정리)`으로 갱신했다.

## ver.0.2.12(260423-스탠딩기본배치유지수정)

- 샘플 대사에서 블록에 `standing_left`/`standing_right`가 없을 때도 좌우 슬롯이 빈 값으로 덮여 스탠딩이 사라지던 원인을 수정했다.
- 파서 정규화 단계에 `readOptionalBlockValue`를 추가해, 블록 키가 "미지정(null)"인지 "명시값(빈 문자열 포함)"인지 구분하도록 보강했다.
- 런타임 스탠딩 적용(`applyStandingFromBlock`)은 지시어가 실제로 주어진 경우에만 좌/우 슬롯을 갱신하고, 미지정이면 scene 기본 배치를 유지하도록 변경했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.12(260423-스탠딩기본배치유지수정)`으로 갱신했다.

## ver.0.2.11(260423-모바일대화배경공백수정)

- 모바일 대화 화면에서 배경 이미지가 상/하단에 비어 보이던 문제를 수정했다.
- `assets/css/dialogue_template.css`의 모바일 미디어쿼리(`max-width: 760px`)에서 배경 `background-size`를 `contain`에서 `cover` 동작으로 오버라이드해 세로 화면에서도 화면을 꽉 채우도록 조정했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.11(260423-모바일대화배경공백수정)`으로 갱신했다.

## ver.0.2.10(260423-히든선택지카드연결)

- 대화 선택지 렌더를 공용 카드 템플릿 중심으로 다시 정리해, 시작 화면과 동일한 카드 템플릿 톤(`NEWTHERIA`/`✦`)으로 일관되게 표시되도록 맞췄다.
- 블록 문법에 `hidden_choice = 라벨 -> anchor`를 추가하고, 일반 선택지 카드를 모두 버렸을 때 히든 선택지 카드가 나타나도록 동작을 확장했다.
- 히든 선택지까지 모두 버린 경우에는 기본 선택지 카드로 자동 복원되도록 안전장치를 유지해 진행 정지를 방지했다.
- 샘플 스크립트/문법 문서/테스트 힌트를 `hidden_choice` 기준으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.10(260423-히든선택지카드연결)`으로 갱신했다.

## ver.0.2.9(260423-대화선택지카드복구)

- 샘플 대화 선택지 렌더를 텍스트 버튼 방식에서 공용 카드 템플릿(`NewtheriaCardTemplates`) 기반으로 복구해, 시작 화면과 동일한 카드형 팬(Fan) UI로 다시 노출되도록 수정했다.
- 대화 선택지 카드에 팬 배치/호버/롱프레스 뒤집기/드래그/드롭 인터랙션을 다시 연결하고, 카드 디스카드 시 남은 선택지가 즉시 재배치되도록 보강했다.
- 모든 선택지 카드를 버려 진행이 막히는 상황을 방지하기 위해, 마지막 카드까지 디스카드된 경우 원본 선택지를 자동 복원하도록 안전장치를 추가했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.9(260423-대화선택지카드복구)`로 갱신했다.

## ver.0.2.8(260423-대표캐릭터세이브분리)

- 요청사항에 맞춰 캐릭터 저장 정책을 분리했다: localStorage의 `newtheria.characters`는 대표 캐릭터(여신/순례자 등 소수)만 유지한다.
- 대부분 캐릭터는 세이브 슬롯 데이터(`newtheria.saveSlots.data`)에 저장되도록 전제를 확정하고, actor id 해석 시 활성 세이브 슬롯 캐릭터를 local 대표 캐릭터보다 우선 조회하도록 보강했다.
- 시작 화면 시드에 `newtheria.saveSlots.data` 기본 구조를 추가하고 `activeSlotId`를 메타에 포함해 슬롯 단위 캐릭터 확장 기반을 마련했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.8(260423-대표캐릭터세이브분리)`으로 갱신했다.

## ver.0.2.7(260423-스토리지전략초기화)

- 시작 화면(`assets/js/start_screen.js`)에서 localStorage 기본 시드를 추가했다: `newtheria.characters`(여신/여행자 캐릭터 카탈로그), `newtheria.settings.meta`, `newtheria.saveSlots.meta`.
- 대화 템플릿(`assets/js/dialogue_template.js`)은 actor id 해석 시 localStorage의 `newtheria.characters`를 우선 병합해 캐릭터 이름/레이어를 자동 조회하도록 보강했다.
- 대화 진행 포인터는 sessionStorage(`newtheria.dialogue.progress`)에 저장/복원되도록 추가해, 같은 탭 내 페이지 이동 시 진행 재개가 가능해졌다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.7(260423-스토리지전략초기화)`로 갱신했다.

## ver.0.2.6(260423-actorid카탈로그조회지원)

- scene 캐스팅의 `actor` 문법을 `actor = id` 기준으로 정리하고, id를 캐릭터 카탈로그(또는 `cast_ref`/`cast_json` 번들)에서 조회해 이름/레이어를 해석하도록 파서를 보강했다.
- 런타임 생성 옵션에 `characterCatalog`, `castBundles` 주입 경로를 추가해 외부 캐릭터 리스트와의 연계를 준비했다.
- 샘플 스크립트는 임시 캐릭터 번들(`cast_json`)을 선언한 뒤 `actor = goddess`, `actor = pilgrim`처럼 id만 사용하도록 업데이트했다.
- 문법 가이드/테스트 힌트를 actor id 조회 방식으로 갱신하고, 시작 화면/월드맵 fallback 버전을 `ver.0.2.6(260423-actorid카탈로그조회지원)`으로 갱신했다.

## ver.0.2.5(260423-씬헤더캐스팅통합)

- `<scene_cast>` 헤더를 제거하고, `<scene#n:이름>` 직후 `{ ... }` 영역에서 씬 기본 캐스팅(`actor`, `slot_left/right`, `focus`)을 정의하도록 문법을 단순화했다.
- 향후 외부 캐릭터 번들 연동을 고려해 scene 캐스팅 키에 `cast_ref`, `cast_json` 예약 필드를 추가하고, 런타임에서 `cast_json` 파싱 경로를 미리 마련했다.
- 샘플 대사(`assets/data/dialogue_sample.asdf`)를 "씬 헤더 직후 캐스팅 정의" 방식으로 재구성하고, 문법 문서/테스트 힌트를 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.5(260423-씬헤더캐스팅통합)`으로 갱신했다.

## ver.0.2.4(260423-씬캐스트스탠딩제어)

- `assets/js/dialogue_template.js`에 `<scene_cast>` 헤더를 추가해 씬 단위 기본 캐스팅(배우 id/이름/레이어), 좌/우 슬롯, 기본 포커스를 미리 선언할 수 있도록 확장했다.
- 블록에서는 `move_left`, `move_right`, `focus`, `hide`, `set_layers` 중심으로 상태 제어만 수행하도록 스탠딩 제어 흐름을 정리했다.
- `speaker`(표시명)와 `speaker_id`/`actor`(실제 발화 actor id)를 분리해 "화자 식별자"와 "대사창 이름"을 독립적으로 다룰 수 있게 보강했다.
- 스탠딩 렌더를 다중 레이어 구조(`.dialogue-prototype__actor-layer`)로 확장해 단일 일러/복수 레이어를 동일한 방식으로 표현하도록 맞췄다.
- 샘플 스크립트/문법 문서/테스트 힌트와 fallback 버전을 `ver.0.2.4(260423-씬캐스트스탠딩제어)`로 동기화했다.

## ver.0.2.3(260423-화자스탠딩일러지원)

- ASDF 대화 템플릿(`assets/js/dialogue_template.js`)에 화자/스탠딩 연출 필드를 확장했다. `standing_left`, `standing_right`, `standing_focus`, `standing_animation` 키를 파싱하고 런타임 렌더에 반영한다.
- 대화 레이아웃(`assets/css/dialogue_template.css`)에 좌/우 스탠딩 슬롯 스타일과 포커스 하이라이트 규칙을 추가해, 화자 중심 강조가 가능하도록 정리했다.
- 샘플 스크립트(`assets/data/dialogue_sample.asdf`)와 문법 문서(`docs/14_대사작성문법.md`)를 스탠딩 키 예시 기준으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.3(260423-화자스탠딩일러지원)`으로 갱신했다.

## ver.0.2.2(260422-문법문서전체비움)

- 사용자 요청에 따라 문법 정의 영역을 전면 삭제했다. (`docs/14_대사작성문법.md`)
- 샘플 스크립트(`assets/data/dialogue_sample.nds`)도 문법 placeholder 주석만 남기고 비움 상태로 전환했다.
- 테스트 페이지 문법 힌트를 "문법 문서 비움" 상태 안내로 변경했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.2(260422-문법문서전체비움)`으로 갱신했다.

## ver.0.2.1(260422-엔진스켈레톤초기화)

- 사용자 요청에 따라 `assets/js/dialogue_template.js`의 엔진 구현부를 스켈레톤 모드로 초기화했다.
- 런타임/파서/로더는 no-op 골격만 남기고, 디버그 추적으로 상태 확인이 가능하도록 최소 API를 유지했다.
- 문법 가이드와 샘플 스크립트를 "단계별 재구축" 기준으로 재정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.1(260422-엔진스켈레톤초기화)`으로 갱신했다.

## ver.0.2.0(260422-노드기반엔진재구축)

- `assets/js/dialogue_template.js`를 nodeId 기반 신규 엔진으로 전면 교체했다. 기존 scene/tag/anchor 중심 파서/큐/점프 구조를 폐기하고 `@nodeId` 블록 문법을 기본으로 재구성했다.
- 신규 공식 섹션을 `대사처리`, `흐름제어`, `그림제어`, `소리제어`, `연출제어`, `상태제어`, `선택지제어`, `메타정보`로 정의했다.
- 샘플 스크립트(`assets/data/dialogue_sample.nds`)와 문법 가이드(`docs/14_대사작성문법.md`), 테스트 페이지(`dialogue_prototype_test.html`)를 node 기반 흐름으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.2.0(260422-노드기반엔진재구축)`으로 갱신했다.

## ver.0.1.80(260422-대화템플릿문법초기화)

- 대화 템플릿 점검을 쉽게 하기 위해 샘플 스크립트(`assets/data/dialogue_sample.nds`)를 최소 구조(대사출력/그림제어/흐름제어/선택지)로 전면 초기화했다.
- 대사 문법 가이드(`docs/14_대사작성문법.md`)를 초기화 버전 기준으로 다시 작성해, 복잡한 확장 문법 대신 기본 동작 확인 순서를 우선하도록 정리했다.
- 테스트 페이지 기본 scene/anchor와 문법 힌트를 초기화 문법에 맞춰 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.80(260422-대화템플릿문법초기화)`으로 갱신했다.

## ver.0.1.79(260422-배경방식스탠딩일러복원)

- 대화 템플릿에 `dialogue-prototype__actors` 레이어를 추가해, 스탠딩/그림을 배경과 동일한 `background-image` 합성 방식으로 다시 출력하도록 복원했다.
- `스탠딩` 문법(clear/좌/우 URL)과 `그림`/`그림애니메이션` 값을 렌더 상태에 반영하도록 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.79(260422-배경방식스탠딩일러복원)`으로 갱신했다.

## ver.0.1.78(260422-배경기반일러렌더전환)

- 대화 템플릿에서 그림/스탠딩 전용 렌더 레이어를 제거하고, 배경 레이어 중심 렌더링으로 단순화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.78(260422-배경기반일러렌더전환)`으로 갱신했다.

## ver.0.1.77(260422-그림유지정책배경동기화)
- 사용자 요청에 맞춰 그림(`그림`/`illustration`) 출력 정책을 배경과 동일하게 동기화했다. 이제 현재 줄에서 그림 URL이 비어 있으면 직전 그림을 유지한다.
- `assets/js/dialogue_template.js`의 `setIllustration`에서 clear 동작을 명시 토큰(`off`, `none`, `clear`, `-`) 기반으로 제한하고, 빈 값은 clear가 아니라 유지로 처리하도록 수정했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.77(260422-그림유지정책배경동기화)`으로 갱신했다.

## ver.0.1.76(260422-그림제어전면재작성)
- 그림제어 블록을 전면 재작성했다. 배경/그림/스탠딩 모두 "즉시 렌더 + 사후 진단 프리로드" 공통 정책으로 통일해, 특정 환경에서 이미지 onload 타이밍 때문에 비가시화되는 체감 문제를 줄였다.
- `assets/js/dialogue_template.js`에 공통 유틸 `preloadImageForDiagnostics`를 추가해 로딩 실패 추적(`background-load-error`, `illustration-load-error`, `standing-load-error`)과 캐시 처리 경로를 단일화했다.
- 스탠딩/그림 clear 토큰(`off`, `none`, `clear`, `-`) 처리, 상태 동기화, 슬롯별 추적 payload를 재정리해 디버깅 가독성을 높였다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.76(260422-그림제어전면재작성)`으로 갱신했다.

## ver.0.1.75(260422-그림즉시렌더보강)
- 사용자 제보(배경은 출력되지만 그림/컷인이 전혀 보이지 않음) 기준으로 원인 경로를 재점검하고, 그림 레이어가 프리로드 완료 콜백에 렌더 타이밍을 의존하던 구조를 정리했다.
- `assets/js/dialogue_template.js`의 `setIllustration`을 스탠딩/배경과 동일한 정책으로 통일해, `background-image`를 즉시 반영하고 프리로드는 성공/실패 캐시와 `illustration-load-error` 추적만 담당하도록 변경했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.75(260422-그림즉시렌더보강)`으로 갱신했다.

## ver.0.1.74(260422-스탠딩즉시렌더보강)
- 추가 원인 점검 결과, 스탠딩 렌더가 프리로드(onload) 완료 콜백에 의존하던 경로가 특정 실행 환경에서 "초기 앵커부터 스탠딩 미노출"을 유발할 여지가 있어 표시 경로와 진단 경로를 분리했다.
- `assets/js/dialogue_template.js`에서 스탠딩 `background-image` 적용을 즉시 수행하도록 되돌리고, 프리로드는 사후 성공/실패 캐시 및 `standing-load-error` 추적만 담당하도록 보강했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.74(260422-스탠딩즉시렌더보강)`으로 갱신했다.

## ver.0.1.73(260422-중간앵커스탠딩부트스트랩)
- 원인 분석 결과, 샘플 NDS의 `스탠딩` 선언이 도입부 anchor(`rebirth_intro#awakening`)에만 있어 중간 anchor로 바로 진입하면 초기 스탠딩 상태가 비어 캐릭터가 전혀 보이지 않는 흐름을 확인했다.
- `assets/js/dialogue_template.js`의 `applySelection`에 스탠딩 부트스트랩 로직을 추가해, 첫 줄에 `스탠딩`이 없고 `scene+anchor` 중간 진입인 경우 같은 scene의 anchor 이전 블록에서 마지막 스탠딩 선언을 자동 복원하도록 보강했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.73(260422-중간앵커스탠딩부트스트랩)`으로 갱신했다.

## ver.0.1.72(260422-스탠딩로드실패추적보강)
- `assets/js/dialogue_template.js`의 스탠딩 렌더 경로에 이미지 프리로드/캐시를 추가해, 스탠딩 URL 로딩 성공/실패를 배경·컷인과 같은 수준으로 추적하도록 보강했다.
- 스탠딩 이미지 로딩 실패 시 `standing-load-error` 추적 이벤트와 콘솔 경고를 남기도록 해, "배경은 보이는데 스탠딩만 안 보이는" 케이스의 원인을 즉시 확인할 수 있게 개선했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.72(260422-스탠딩로드실패추적보강)`으로 갱신했다.

## ver.0.1.71(260422-샘플그림제어경로복구)
- 샘플 스크립트(`assets/data/dialogue_sample.nds`)의 도입부 `배경` 값을 `assets/img/background.jpg`로 수정해, 루트 상대 경로(`background.jpg`)로 인한 404 때문에 그림제어가 멈춘 것처럼 보이던 문제를 복구했다.
- `assets/js/dialogue_template.js`에 배경/그림 로딩 실패 추적(`background-load-error`, `illustration-load-error`)과 콘솔 경고를 추가해 원인 파악을 빠르게 할 수 있도록 보강했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.71(260422-샘플그림제어경로복구)`으로 갱신했다.

## ver.0.1.70(260422-샘플대화자동넘김대폭축소)
- 샘플 대사 스크립트(`assets/data/dialogue_sample.nds`)에서 대부분의 `자동넘김` 지시를 제거해, 핵심 연출 구간을 제외하면 사용자가 수동으로 대사를 넘기도록 조정했다.
- 자동 진행이 꼭 필요한 무대 연출 블록(`rebirth_climax#no_dialogue_blessing_fx`)의 `자동넘김 = 700`만 유지해 템포 제어는 보존했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.70(260422-샘플대화자동넘김대폭축소)`으로 갱신했다.

## ver.0.1.69(260422-대화점프디버그보강)
- `dialogue_prototype_test.html` 기본 scene/anchor를 샘플 스크립트 시작점(`rebirth_intro#awakening`)으로 맞추고, `@scene#anchor` 단일 입력을 자동 분해해 테스트 재현 편의를 높였다.
- `assets/js/dialogue_template.js` 점프 실행 경로에 예외 보고를 추가해, 잘못된 타겟으로 이동 시 진행이 조용히 멈춘 것처럼 보이던 문제를 디버그 콜백/콘솔에서 바로 확인할 수 있게 보강했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.69(260422-대화점프디버그보강)`으로 갱신했다.

## ver.0.1.68(260422-샘플대화배경jpg지정)
- 요청사항을 정정 반영해, 공통 기본값이 아니라 샘플 대사 파일(`assets/data/dialogue_sample.nds`)의 `배경` 값을 `background.jpg`로 지정했다.
- 이전 변경에서 임시 전환했던 프로토타입 입력 기본 배경/템플릿 fallback 배경은 원래 값(외부 샘플 URL)으로 되돌려, 변경 범위를 샘플 대화 데이터로 한정했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.68(260422-샘플대화배경jpg지정)`으로 갱신했다.

## ver.0.1.67(260422-대화기본배경로컬jpg전환)
- 대화 프로토타입 임시 테스트 페이지의 기본 배경 URL을 외부 이미지 대신 같은 경로의 `./background.jpg`로 변경했다.
- 대화 템플릿 기본 fallback 대사(`DEFAULT_DIALOGUES`)도 동일하게 `./background.jpg`를 사용하도록 맞춰, 네트워크 의존 없이 로컬 배경 테스트가 가능하도록 조정했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.67(260422-대화기본배경로컬jpg전환)`으로 갱신했다.

## ver.0.1.66(260422-이스케이프개행파싱복원)
- 대사 스크립트 입력이 단일 라인 문자열로 들어오면서 `\n`이 실제 줄바꿈이 아닌 이스케이프 텍스트로 남는 경우를 대비해, 파싱 전 개행 복원(`normalizeScriptLineBreaks`)을 추가했다.
- 이 보강으로 `; =====` 주석 배너와 다음 `@scene#anchor`가 한 줄로 붙어 통째로 주석 처리되던 케이스를 완화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.66(260422-이스케이프개행파싱복원)`으로 갱신했다.

## ver.0.1.65(260422-앵커세미콜론내성보강)
- 대화 스크립트 파서에 `cleanAnchorToken` 정규화 단계를 추가해 `@scene#anchor;`, `점프 = scene#anchor;`, 선택지 타깃 `scene#anchor;`처럼 세미콜론/인라인 메모가 붙은 앵커를 안전하게 해석하도록 보강했다.
- 세미콜론이 토큰에 섞여 다음 앵커 이동이 실패하던 케이스를 방지해, 임시 테스트 스크립트의 진행 중단 현상을 완화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.65(260422-앵커세미콜론내성보강)`으로 갱신했다.

## ver.0.1.64(260422-환생서사샘플대사개편)
- 샘플 대사(`assets/data/dialogue_sample.nds`)를 요청한 4막 구조(도입/전개/절정/결말) 기반으로 전면 개편했다.
- 아스테리아(별의 여신)와 순례자(플레이어)의 환생 서사를 중심으로, 세계관 설명형 장문 대화 흐름을 강화했다.
- 샘플 목적을 문법 시연으로 유지해 모든 핵심 문법(흐름제어/대사출력/소리제어/그림제어/연출제어/변수제어/선택지/히든선택지/점프/종료)을 최소 1회 이상 포함했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.64(260422-환생서사샘플대사개편)`으로 갱신했다.

## ver.0.1.63(260422-로어친화샘플대사종합시연)
- 샘플 대사(`assets/data/dialogue_sample.nds`)를 아스테리아(별의 여신)와 플레이어(별의 여행자) 중심의 장문 로어 친화 대화로 전면 재작성했다.
- 샘플 목적을 문법 시연에 맞춰 `[흐름제어]`, `[대사출력]`, `[소리제어]`, `[그림제어]`, `[연출제어]`, `[변수제어]`, `[선택지]`, `[히든선택지]`를 모두 실사용 예시로 포함했다.
- 대사 없는 연출 블록, 점프, 조건, 효과, 히든선택지, 종료, 오디오 채널 제어(stop/play), 스탠딩 id + 화자포커스 문법까지 한 시나리오 안에서 확인할 수 있도록 구성했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.63(260422-로어친화샘플대사종합시연)`로 갱신했다.

## ver.0.1.62(260422-화자포커스스탠딩id지원)
- 화자명과 스탠딩 하이라이트 기준을 분리하기 위해 `화자포커스`/`포커스` 키를 추가했다. 이제 화자 표시명과 무관하게 원하는 스탠딩 슬롯을 포커싱할 수 있다.
- 스탠딩 문법에 선택 식별자(`id=...`)를 지원해, 향후 다중 레이어 캐릭터 구성에서도 `화자명==스탠딩이름` 규칙에 묶이지 않도록 확장했다.
- 하이라이트 해석 우선순위를 `activeSlot > 화자포커스/포커스 > 화자명`으로 정리해 안정성을 높였다.
- 샘플/가이드에 스탠딩 id + 화자포커스 예시를 추가하고, 시작 화면/월드맵 fallback 버전을 `ver.0.1.62(260422-화자포커스스탠딩id지원)`로 갱신했다.

## ver.0.1.61(260422-배경유지스탠딩포커스보강)
- 대화 줄에 `배경` 값이 없을 때 기본 배경으로 되돌아가지 않고 직전 배경을 유지하도록 렌더 정책을 보강했다. (초기 1회만 기본 배경 사용)
- 스탠딩 포커스 계산에서 화자값이 `좌/우(left/right)`로 들어와도 해당 슬롯 딤 해제가 동작하도록 보강했다.
- 문법 가이드에 배경 유지 정책을 명시했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.61(260422-배경유지스탠딩포커스보강)`로 갱신했다.

## ver.0.1.60(260422-대사문법섹션전면개편)
- 대사 스크립트 문법을 섹션형 NDS v2로 전면 개편했다. 블록 내부를 `[흐름제어]`, `[대사출력]`, `[소리제어]`, `[그림제어]`, `[연출제어]`, `[변수제어]`, `[선택지]`, `[히든선택지]`로 나눠 비전문가 가독성을 강화했다.
- 파서가 `키 = 값`과 `키: 값`을 모두 허용하도록 확장해 작성 오류 여지를 줄였다.
- 선택지 섹션에서 `- 라벨 => scene#anchor` 리스트 문법을 지원하고, 선택지 없이 `점프 = scene#anchor`만으로 다음 앵커 이동이 가능하도록 유지했다.
- 대사가 없는 블록에서도 소리/애니메이션/연출이 그대로 실행되도록 동작을 유지했다.
- 샘플 스크립트/문법 가이드/테스트 페이지 힌트를 NDS v2 기준으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.60(260422-대사문법섹션전면개편)`로 갱신했다.

## ver.0.1.59(260422-대사없는블록진행정지수정)
- NDS 파서가 빈 줄을 블록 종료로 잘못 해석하던 문제를 수정했다. 이제 블록 내부 공백 줄은 무시되고, 블록 종료는 `---` 또는 다음 `@scene#anchor`에서만 발생한다.
- 이 수정으로 `대사:`가 비어 있는 연출 블록에서도 `자동넘김`/`점프`가 의도대로 같은 블록에서 동작해 진행 정지 현상을 해소했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.59(260422-대사없는블록진행정지수정)`로 갱신했다.

## ver.0.1.58(260421-대사스크립트NDS전환)
- 대사 데이터 문법을 CSV에서 NDS(Newtheria Dialogue Script) 블록 문법(`@scene#anchor` + `키: 값` + `---`)으로 전환했다.
- `assets/js/dialogue_template.js` 로더를 신규 스크립트 파서 기반으로 교체하고, 대사 없이도 오디오/연출이 동작하도록 빈 `대사` 블록을 허용했다.
- 선택지 없이 다음 앵커로 이동할 수 있도록 `점프` 문법을 추가했다.
- 샘플 데이터를 `assets/data/dialogue_sample.nds`로 교체하고, 테스트 페이지/문법 가이드를 새 문법 기준으로 업데이트했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.58(260421-대사스크립트NDS전환)`로 갱신했다.

## ver.0.1.57(260421-순례자스탠딩경로분리)
- 샘플 CSV의 순례자 스탠딩 경로를 여신 공용 이미지(`assets/img/goddess.png`)에서 전용 경로 `assets/img/player.png`로 분리했다.
- 아스테리아(좌)와 순례자(우) 스탠딩이 서로 다른 파일을 바라보도록 정리해 실제 에셋 교체 시 확인이 쉬워졌다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.57(260421-순례자스탠딩경로분리)`로 갱신했다.

## ver.0.1.56(260421-샘플대사로어확장전면개편)
- 샘플 CSV(`assets/data/dialogue_sample.csv`)를 기존 내용에서 전면 교체해, 아스테리아(여신)와 순례자(플레이어) 중심의 로어 친화 장문 대화로 재작성했다.
- 선택지는 간헐적으로만 배치하고, 전체 흐름은 연속 대화 비중이 높도록 구성해 설명형 튜토리얼+서사형 진행을 동시에 검증할 수 있게 정리했다.
- 한 파일 안에서 `스탠딩`, `템플릿 변수`, `조건/효과`, `카메라`, `ui`, `진동`, `파티클`, `조명`, `씬전환`, `오디오 stop/play`, `종료` 등 확장 문법을 모두 실사용 예시로 포함했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.56(260421-샘플대사로어확장전면개편)`으로 갱신했다.

## ver.0.1.55(260421-대화스탠딩좌우하이라이트문법)
- 대사 템플릿에 `스탠딩` 문법을 추가해 좌/우 캐릭터 배치, 화자 하이라이트(딤 해제), 간단 이동(`move`)과 방향(`auto/flip`) 제어를 지원했다.
- 스탠딩 일러스트 기본 방향을 우측 바라봄 기준으로 두고, 우측 슬롯은 자동 반전해 서로 마주보는 구도를 기본값으로 적용했다.
- `assets/css/dialogue_template.css`에 좌/우 스탠딩 레이어 스타일과 발화자 강조/비발화자 딤 표현을 추가했다.
- 샘플 CSV, 테스트 페이지 힌트, 문법 가이드를 스탠딩 컬럼 기준으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.55(260421-대화스탠딩좌우하이라이트문법)`로 갱신했다.

## ver.0.1.54(260421-샘플대사연속형재구성)
- 샘플 CSV(`assets/data/dialogue_sample.csv`)를 선택지 중심 흐름에서 연속 대사 중심 흐름으로 전면 재구성했다.
- 전체 18줄 시나리오에서 대부분의 확장 문법(`그림/애니메이션`, `bgm/bgs/voice/sfx`, `자동넘김`, `화면효과`, `카메라`, `ui`, `진동`, `파티클`, `조명`, `씬전환`, `조건`, `효과`, `템플릿 변수`)을 실제 서사 순서로 사용하도록 정리했다.
- 선택지는 최소 구간(`intro#story_11`, `intro#secret_01`)에만 배치해 요청사항대로 연속 진행 중심 검증이 가능하도록 조정했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.54(260421-샘플대사연속형재구성)`으로 갱신했다.

## ver.0.1.53(260421-템플릿연출문법풀셋확장)
- 이전 제안 목록을 모두 반영해 비대사 연출 문법을 확장했다: `카메라`, `ui`, `진동`, `파티클`, `조명`, `씬전환`.
- `assets/js/dialogue_template.js`에 각 문법 파서와 실행기를 추가했다. 카메라(pan/zoom/reset), UI 토글/선택지 잠금, 진동 패턴, 파티클(snow/ember), 조명 tint, 페이드 전환을 줄 단위로 적용할 수 있다.
- `assets/css/dialogue_template.css`에 연출 레이어(`light`, `particles`, `transition`)와 파티클/잠금 스타일, 관련 애니메이션을 추가했다.
- `docs/14_대사작성문법.md`, `assets/data/dialogue_sample.csv`, `dialogue_prototype_test.html` 힌트를 신규 컬럼 기준으로 동기화해 사용자 친화적인 템플릿 예시를 제공했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.53(260421-템플릿연출문법풀셋확장)`으로 갱신했다.

## ver.0.1.52(260421-비대사연출문법확장)
- 대사 템플릿 문법에 비대사 연출 컬럼 `sfx`, `자동넘김`, `화면효과`를 추가해 한 줄 단위로 효과음/자동 진행/화면 FX를 제어할 수 있게 확장했다.
- `assets/js/dialogue_template.js`에 `sfx` 채널 처리, `자동넘김(ms)` 타이머, `화면효과(flash/shake/dim)` 적용 로직을 추가했다.
- `assets/css/dialogue_template.css`에 비대사 연출용 화면 FX 애니메이션(`dialogue-fx-flash`, `dialogue-fx-shake`)과 dim 상태 스타일을 추가했다.
- 샘플 CSV(`assets/data/dialogue_sample.csv`)와 문법 가이드(`docs/14_대사작성문법.md`), 테스트 페이지 힌트(`dialogue_prototype_test.html`)를 신규 컬럼 기준으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.52(260421-비대사연출문법확장)`으로 갱신했다.

## ver.0.1.51(260421-대화템플릿스크립트명정렬)
- 대화 템플릿 스크립트 파일명을 역할에 맞춰 `assets/js/dialogue_prototype.js`에서 `assets/js/dialogue_template.js`로 변경했다.
- 테스트 페이지 스크립트 로더와 생성 API 호출을 `window.NewtheriaDialogueTemplate.createDialogueTemplate` 기준으로 정리했다.
- 하위 호환을 위해 `window.NewtheriaDialoguePrototype` 네임스페이스 별칭을 유지해 기존 호출부도 계속 동작하도록 처리했다.
- 문법 가이드의 기준 파일 경로를 신규 파일명으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.51(260421-대화템플릿스크립트명정렬)`로 갱신했다.

## ver.0.1.50(260421-대화미디어문법확장)
- 대화 CSV 문법을 확장해 `그림`, `그림애니메이션`, `bgm`, `bgs`, `voice` 컬럼을 추가하고, 대사 외 연출(이미지 컷인/애니메이션/오디오 재생·정지)을 한 줄 단위로 제어할 수 있도록 구현했다.
- `assets/js/dialogue_prototype.js`에 미디어 파서/실행기를 추가해 `play|url|volume|loop`, `stop`, `url` 축약 문법을 지원하고, BGM/BGS/Voice 3채널을 분리 재생하도록 확장했다.
- 대화 레이아웃 템플릿(`assets/css/dialogue_template.css`)에 그림 레이어와 애니메이션 프리셋(`fade`, `float`, `zoom-in`, `shake`)을 추가했다.
- 문법 가이드(`docs/14_대사작성문법.md`)와 샘플 CSV(`assets/data/dialogue_sample.csv`), 테스트 페이지 힌트를 신규 미디어 문법 기준으로 동기화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.50(260421-대화미디어문법확장)`으로 갱신했다.

## ver.0.1.49(260421-모바일선택지카드중앙정렬보정)
- 모바일 대화 화면에서 선택지 카드가 오른쪽으로 치우쳐 보이던 원인을 `width`와 `--card-w` 기준값 불일치로 확인했다.
- `dialogue_template.css`에서 모바일 카드 폭을 직접 `width`로 덮어쓰는 대신, `.dialogue-prototype` 범위의 `--card-w`를 반응형으로 동기화해 카드 기준점(`margin-left: calc(var(--card-w) / -2)`)과 실제 폭을 일치시켰다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.49(260421-모바일선택지카드중앙정렬보정)`로 갱신했다.

## ver.0.1.48(260421-대화배경로딩배지제거)
- 요청사항에 맞춰 대화 프로토타입의 `배경 이미지 로딩 중...` UI를 완전히 제거했다. (초기 진입/대화 전환/화면 전환 포함)
- `dialogue_prototype.js`에서 로딩 텍스트 DOM 생성/갱신 코드를 정리하고, 배경 로딩 실패 시에도 화면에 로딩 문구를 다시 띄우지 않도록 처리했다.
- `dialogue_template.css`에서 `.dialogue-prototype__loading` 스타일 블록을 제거했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.48(260421-대화배경로딩배지제거)`로 갱신했다.

## ver.0.1.47(260421-시작카드뒷면공용화배경로딩점멸완화)
- 카드 뒷면 배경/심볼/브랜드 타이포 값을 `assets/css/card_templates.css` 공용 변수로 승격해, 시작 화면 카드 뒷면도 공용 템플릿만으로 유지되도록 정리했다.
- 대화 선택지 카드의 뒷면 커스텀은 `dialogue_template.css`에서 공용 변수 오버라이드 방식으로 전환해 카드 뒷면 스타일 중복을 제거했다.
- 대화 흐름 진행 시 같은 배경 URL을 다시 표시할 때 `배경 이미지 로딩 중...` 문구가 반복 점멸하지 않도록 배경 로드 캐시/현재 URL 추적 로직을 추가했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.47(260421-시작카드뒷면공용화배경로딩점멸완화)`로 갱신했다.

## ver.0.1.46(260421-대화css공용템플릿분리)
- `assets/css/dialogue_template.css`를 신설해 대화창 레이아웃/애니메이션 규칙을 공용 템플릿 파일로 분리했다.
- `assets/css/dialogue_prototype.css`는 토큰(색상/팬 배치 변수)만 정의하고 공용 템플릿을 import하는 얇은 진입점으로 재구성했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.46(260421-대화css공용템플릿분리)`로 갱신했다.

## ver.0.1.45(260421-대화변수재정규화수정)
- 대화 레코드를 재정규화할 때 `variables/effects`가 이미 객체·배열 형태인 경우 문자열로 재파싱하지 않고 그대로 정규화하도록 보강했다.
- 이 수정으로 테스트 대화창 초기 변수 스냅샷에 `"[object Object]": ""`가 생기던 현상을 제거하고, `playerName/신뢰도/별조각/경계태세/피로도`처럼 의도한 키가 정상 유지되도록 개선했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.45(260421-대화변수재정규화수정)`로 갱신

## ver.0.1.44(260421-샘플연속대화확장)
- 샘플 대화 CSV(`assets/data/dialogue_sample.csv`)에 선택지 없이 이어지는 연속 대화 구간(`intro#story_*`, `advanced#briefing_*`)을 추가해, 서사형 진행 테스트를 더 길게 검증할 수 있도록 확장했다.
- 기존 분기 학습 동선에 연속 대화 진입 선택지를 추가해 필요 시 분기형/비분기형 흐름을 각각 선택해 확인할 수 있게 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.44(260421-샘플연속대화확장)`으로 갱신

## ver.0.1.43(260421-대화테스트설정창재구성)
- 대화 테스트 설정창 UI를 전면 재구성해 컨테이너 폭/높이가 뷰포트를 넘지 않도록 정리하고, 내용 영역에 스크롤을 적용해 항목 누락 없이 확인 가능하게 보강했다.
- 설정 항목을 `데이터 소스`, `태그/앵커`, `배경`, `디버그`, `문법 힌트` 섹션으로 분리하고 `details` 접기/펼치기(최소화) 구조를 적용해 필요한 항목만 선택적으로 조절할 수 있게 변경했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.43(260421-대화테스트설정창재구성)`으로 갱신

## ver.0.1.42(260421-대화조건순차평가보정)
- 대화 큐를 만들 때 줄 조건을 한 번에 정적 필터링하지 않고, 앞줄의 변수/효과를 순차 반영하며 다음 줄 조건을 계산하도록 보정했다.
- 이 변경으로 `advanced#start` 이후 분기에서 중간 앵커 조건이 누락되어 `advanced#ending`으로 직행하던 흐름을 완화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.42(260421-대화조건순차평가보정)`으로 갱신

## ver.0.1.41(260421-샘플대사확장문법가이드강화)
- 샘플 대화 CSV(`assets/data/dialogue_sample.csv`)를 장문 시나리오형으로 확장해 기초/확장 문법을 한 번에 실습할 수 있도록 재구성했다.
- 샘플 안에 조건식(AND/OR/NOT), 효과 누적, 선택지 메타(`[조건]/[효과]`), 히든선택지, 종료 플래그, 문자열 상태 전이(퀘스트 단계) 예시를 추가해 문법 변형/확장 사용법을 실제 분기 흐름으로 제시했다.
- `docs/14_대사작성문법.md`를 확장 사용 중심으로 전면 보강해 변수 타입, 연산자, 종료 판정, 실무 패턴(상점/재화/히든루트/이벤트 플래그)과 장문 템플릿 예시를 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.41(260421-샘플대사확장문법가이드강화)`로 갱신

## ver.0.1.40(260421-대화테스트성능추적패널개선)
- 대화 선택지 카드를 반복 로드할 때 카드 팬 인터랙션 이벤트가 누적되어 점점 느려지던 문제를 수정했다. `createCardFanBehavior`에 `AbortController` 기반 리스너 정리를 적용해 재렌더/재진입 시 기존 이벤트를 즉시 해제하도록 보강했다.
- 대화 테스트 설정 패널 입력 UI의 높이/배치를 통일하고, `scene/tag + anchor`를 2열 레이아웃으로 정리해 크기가 들쭉날쭉해 보이던 인상을 개선했다.
- 테스트 화면에 변수 추적기(변수 JSON 적용, 현재 변수 스냅샷, 최근 이벤트 로그, 로그 초기화)를 추가하고, 대화 프로토타입 API에 `subscribeDebug/getDebugState/clearDebugTrace`를 확장해 변수/분기 흐름을 실시간 추적할 수 있게 했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.40(260421-대화테스트성능추적패널개선)`로 갱신

## ver.0.1.39(260421-대화선택지초기플리커보정)
- 대사 선택지 카드가 첫 표시 시 `animation-delay` 구간에서 잠깐 보였다가 다시 사라지는 플리커 원인을 확인하고, `.dialogue-prototype__choice`에 `opacity: 0` + `animation-fill-mode: both`를 적용해 초기 페인트를 안정화했다.
- 선택지 등장 애니메이션(`choice-pop-in`)은 유지하면서 지연 구간도 0% 키프레임 상태를 유지하도록 보정했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.39(260421-대화선택지초기플리커보정)`으로 갱신

## ver.0.1.38(260421-변수재평가현재줄유지보정)
- `setVariables()` 호출 시 대사 큐를 무조건 첫 줄로 되돌리던 흐름을 보정해, 조건 재평가 후에도 가능하면 현재 줄 커서를 유지하도록 개선했다.
- 같은 줄 내 `변수` 선언은 기존대로 즉시 템플릿 치환에 적용되고, 다음 줄에서도 공용 변수로 이어지도록 유지했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.38(260421-변수재평가현재줄유지보정)`으로 갱신

## ver.0.1.37(260421-대화변수누적적용수정)
- 대화창에서 CSV `변수` 컬럼 값이 현재 줄에만 적용되고 다음 줄 템플릿(`{{playerName}}`, `{{신뢰도}}`)으로 이어지지 않던 문제를 수정해, 줄 진입 시 공용 변수 상태에 누적 반영되도록 보강했다.
- `setVariables()` 호출 시 현재 필터(scene/anchor) 기준으로 대사 큐를 재평가하도록 변경해, 변수 변경 이후 조건 분기/노출 여부가 즉시 반영되도록 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.37(260421-대화변수누적적용수정)`으로 갱신

## ver.0.1.36(260421-샘플대사설명중심간소화)
- 샘플 대화 CSV의 내용을 퀘스트 진행형에서 설명 중심 안내형으로 재구성해, 비전공자가 문법 학습용으로 바로 읽기 쉽게 단순화했다.
- 아스테리아 대사는 반존대 톤을 유지하면서 `태그/앵커/변수/조건/효과/종료`를 자연스럽게 설명하는 구조로 정리했다.
- `docs/14_대사작성문법.md`의 최소 예시 템플릿도 동일한 설명형 톤으로 맞춰 샘플 데이터와 문서 톤을 일치시켰다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.36(260421-샘플대사설명중심간소화)`으로 갱신

## ver.0.1.35(260421-아스테리아반존대간결대사문법가이드)
- 샘플 대화 CSV를 반존대 말투의 아스테리아 대화로 재정리하고, 장편 시나리오 대신 가볍게 테스트 가능한 간결 대화 흐름(`intro/chapter1`)으로 축약했다.
- 로어 톤(검은해 균열, 별조각)은 유지하되 문장 길이와 분기 수를 줄여 비전공자 샘플로 읽기 쉽게 개선했다.
- 신규 문서 `docs/14_대사작성문법.md`를 추가해 헤더 의미, 조건/효과/종료, 선택지 메타, 템플릿 변수 문법을 한 페이지 가이드로 정리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.35(260421-아스테리아반존대간결대사문법가이드)`으로 갱신

## ver.0.1.34(260421-아스테리아로어샘플대사확장)
- 샘플 대화 CSV를 여신 `아스테리아`와의 대화 중심으로 전면 개편해 세계관(검은해 균열, 루메니아 서약, 별조각) 맥락이 자연스럽게 이어지도록 로어 친화 대사를 대폭 확장했다.
- `prologue/chapter1` 구간에 조건/효과/종료/히든선택지 예시를 풍부하게 배치해 비전공 작가도 CSV 문법을 읽으며 분기 설계를 이해할 수 있도록 개선했다.
- 선택지 라벨을 행동 중심 문장으로 정리하고, 대사 내 템플릿 변수(`{{playerName}}`, `{{신뢰도}}`, `{{별조각}}`) 활용 예시를 강화했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.34(260421-아스테리아로어샘플대사확장)`으로 갱신

## ver.0.1.33(260421-csv조건효과종료문법확장)
- 대화 CSV 단일 스키마를 확장해 `조건`, `효과`, `종료` 칼럼을 지원하고, 조건 미충족 줄은 자동 필터링되도록 대화 큐 선택 로직을 보강했다.
- 선택지 문법에 부가 메타 블록(`[조건: ...]`, `[효과: ...]`)을 추가해 비전공자도 한 셀에서 분기 노출 조건/선택 후 변수 변화량을 읽기 쉽게 작성할 수 있도록 개선했다.
- 효과 문법(`신뢰도+2`, `열쇠-=1`, `플래그=!`)과 불리언 종료 문법(`종료=예`)을 적용해 CSV만으로 중간 종료/상태 누적 흐름을 표현할 수 있게 했다.
- 샘플 CSV를 신규 헤더(`태그,앵커,화자,대사,배경,변수,조건,효과,종료,선택지,히든선택지`)와 조건/효과/종료 예시 포함 데이터로 갱신했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.33(260421-csv조건효과종료문법확장)`으로 갱신

## ver.0.1.32(260421-대화선택지카드재구현)
- 대화 프로토타입의 선택지 카드 렌더링을 수동 DOM 조합 방식에서 공용 카드 템플릿 API 기반 재생성 방식으로 전면 교체해 클릭/드래그 분기 실행 안정성을 높였다.
- 선택지 카드 생성/바인딩 로직을 정리하고, 템플릿 미로딩 환경에서도 분기 클릭이 동작하도록 폴백 버튼 경로를 분리했다.
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.32(260421-대화선택지카드재구현)`로 갱신

## ver.0.1.31(260421-대화카드스타일동일화)
- 시작 화면 카드 동작 코드와 대화 선택지 카드를 비교한 결과, 대화 전용 `.dialogue-prototype__choice`가 `transform/transition/touch-action` 등 카드 템플릿 공용 속성을 중복 선언해 런타임 변형 경로가 분산되는 차이를 확인
- 대화 선택지 CSS에서 공용 카드 템플릿과 겹치는 변형 관련 선언을 제거하고, 위치(anchor) + 등장 타이밍만 남겨 시작 화면과 동일한 transform 제어 체계(`card_templates.css` + JS)로 일원화
- 기존 진단(애니메이션 transform 충돌 제거)과 함께 적용해 hover 팝업/주변 카드 밀림/드래그 이동이 같은 코드 경로로 처리되도록 정렬
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.31(260421-대화카드스타일동일화)`로 갱신

## ver.0.1.30(260421-대화카드변형충돌해소)
- 테스트 대화창 카드 동작 정밀 진단 결과, `.dialogue-prototype__choice`의 `choice-pop-in` 애니메이션이 `transform`을 점유(`fill-mode: both`)해 JS 팬 인터랙션(transform 기반 hover/drag/밀어내기)을 덮어쓰는 충돌을 확인
- 선택지 카드 등장 애니메이션을 transform 비의존(opacity-only)으로 변경해 `createCardFanBehavior`의 실시간 변형값(`translate/rotate/scale`)이 정상 반영되도록 수정
- 대화 선택지 CSS의 별도 `:hover { transform: ... }` 오버라이드를 제거해 hover 연출을 카드 템플릿 JS 로직 하나로 일원화
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.30(260421-대화카드변형충돌해소)`로 갱신

## ver.0.1.29(260421-대화카드인터랙션안정화)
- 테스트 대화창 선택지 카드 팬 인터랙션의 원인 분석 결과, 레이아웃이 초기 렌더 시점 폭에 고정되어 카드가 과밀 배치되는 케이스를 확인하고 자동 재레이아웃(윈도우 리사이즈 + `ResizeObserver`)을 추가
- 카드 hover 반응이 입력 장치별로 누락되지 않도록 `mouseenter/leave` 외에 `pointerenter/leave`를 함께 바인딩해 호버 팝업/주변 카드 밀림 연출 안정성을 보강
- 대화 선택지 컨테이너 폭/높이와 팬 간격(`--fan-gap`)을 상향 조정해 카드가 과도하게 모여 보이던 배치 체감을 완화
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.29(260421-대화카드인터랙션안정화)`로 갱신

## ver.0.0.105(260421-카드css템플릿공용화)
- 카드 스타일을 `assets/css/card_templates.css` 공용 템플릿으로 분리해 시작 화면/대화 선택지가 같은 카드 구조를 공유하도록 정리
- `start_screen.css`, `dialogue_prototype.css`에서 카드 공통 스타일 중복을 제거하고 `@import` 기반으로 템플릿을 적극 재사용
- 대화 선택지는 템플릿 스타일 위에 위치/연출 오버라이드만 남겨 유지보수 포인트를 단순화
- 시작 화면/월드맵 fallback 버전을 `ver.0.0.105(260421-카드css템플릿공용화)`로 갱신

## ver.0.1.28(260421-대화카드팬인터랙션복구)
- 대화 선택지 CSS에 카드 팬 공용 변수(`--fan-gap`, `--fan-tilt`)를 추가해 카드가 중앙에 겹쳐 보이던 배치 오류를 복구
- 대화 선택지 카드에 뒷면(face back) 마크업/스타일을 보강해 롱프레스 뒤집기 시 시각 피드백이 실제로 보이도록 수정
- 대화 배경은 `background-size: contain` + `no-repeat`로 조정해 일러스트가 과도하게 확대되어 잘리는 현상을 완화
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.28(260421-대화카드팬인터랙션복구)`로 갱신

## ver.0.1.27(260421-샘플배경경로고정)
- 요청사항에 맞춰 샘플 CSV 배경 값을 외부 URL이 아닌 고정 경로 `assets/img/goddess.png`로 통일
- 이미지 파일 자체는 레포에 포함하지 않고, 경로만 유지해 사용자가 직접 파일을 채워 넣을 수 있도록 구성
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.27(260421-샘플배경경로고정)`으로 갱신

## ver.0.1.26(260421-바이너리이미지제거)
- 패치 내 바이너리 파일 사용 제한 이슈 대응을 위해 `assets/img/goddess.png`를 레포지토리에서 제거
- 샘플 CSV 배경 경로를 바이너리 에셋 대신 외부 이미지 URL(`https://picsum.photos/...`) 기반으로 교체
- 단일 CSV 문법(`태그/앵커/화자/대사/배경/변수/선택지/히든선택지`)과 변수 치환 예시는 유지
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.26(260421-바이너리이미지제거)`로 갱신

## ver.0.1.25(260421-csv단일문법정리)
- CSV 문법을 단일 스키마로 정리: `태그, 앵커, 화자, 대사, 배경, 변수, 선택지, 히든선택지`
- 선택지는 `라벨 => scene#anchor` 단일 표현식만 사용하고, 다중 선택지는 `||` 구분자로 연결하는 방식으로 통일
- 과거 다중 헤더/별칭 기반 파서를 제거하고 `parseChoiceList` + `parseChoiceDescriptor` 중심의 단순한 파이프라인으로 재구성
- 샘플 CSV와 테스트 페이지 힌트를 단일 문법 기준으로 갱신해 작성자 혼선을 줄임
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.25(260421-csv단일문법정리)`로 갱신

## ver.0.1.24(260421-csv변수템플릿문법지원)
- CSV 대사/선택지에 템플릿 변수 문법을 추가: `{{playerName}}`, `{{상대이름}}`, `{{currentSpeaker}}` 등 치환 지원
- 새 `변수` 컬럼(`playerName=여행자;title=별의방문자`)을 도입해 줄 단위 변수 주입을 지원하고, 런타임 API `setVariables()`로 공용 변수 갱신 가능
- 선택지 compact 문법(`라벨 => scene#anchor`)과 변수 치환을 함께 사용할 수 있도록 파서/렌더를 확장
- 샘플 CSV 배경 이미지를 요청사항대로 `assets/img/goddess.png`로 통일하고, 샘플 문장에 변수 예시를 반영
- 대화 테스트 페이지 힌트에 변수 문법 안내를 추가하고, 시작 화면/월드맵 fallback 버전을 `ver.0.1.24(260421-csv변수템플릿문법지원)`으로 갱신

## ver.0.1.23(260421-csv대화추출간결문법지원)
- 선택지 CSV에 간결 문법을 추가: `선택지_1 = 라벨 => scene#anchor` 또는 `라벨 | scene | anchor` 형태 한 칸만으로 라벨/이동 대상 작성 가능
- `parseChoiceDescriptor`를 도입해 compact 셀 문법을 파싱하고, 기존 `choiceNLabel/choiceNScene/choiceNAnchor` 3분리 컬럼과 완전 호환 유지
- 샘플 CSV를 신규 간결 문법 기반으로 재작성해 가독성을 높이고 작성 난이도를 낮춤
- 대화 테스트 페이지 CSV 힌트를 신규 문법 기준으로 정리
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.23(260421-csv대화추출간결문법지원)`으로 갱신

## ver.0.1.22(260421-csv샘플신규문법동기화)
- CSV 샘플 파일(`assets/data/dialogue_sample.csv`) 헤더를 신규 권장 문법(한글 + 언더스코어 조합)으로 갱신: `태그/앵커/인물명/대사/배경`, `선택지_1_라벨`, `히든선택지_1_포인트` 등
- 선택지 인덱스 감지 정규식을 보강해 `선택지_1_라벨`, `히든선택지_2_장면`처럼 prefix와 번호 사이에 `_`가 있어도 동적 N 슬롯을 정확히 인식하도록 개선
- 대화 테스트 페이지 헤더 힌트를 신규 권장 문법 기준으로 업데이트하고, 기존 영문 헤더 호환 안내를 함께 표기
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.22(260421-csv샘플신규문법동기화)`로 갱신

## ver.0.1.21(260421-csv선택지파서확장형리팩토링)
- CSV 선택지 파서를 반복 하드코딩 배열 대신 조합형 템플릿(`prefix + index + suffix`) 기반으로 리팩토링해 구조를 간결화
- `CHOICE_PREFIX_BY_TYPE`, `CHOICE_FIELD_SUFFIX_BY_PROP`, `buildChoiceKeys`를 도입해 normal/hidden + label/scene/anchor 키 생성 규칙을 공통화
- 신규 헤더 포맷이 추가돼도 템플릿 배열만 확장하면 전체 선택지 파싱 동작이 자동 반영되도록 확장성을 강화
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.21(260421-csv선택지파서확장형리팩토링)`으로 갱신

## ver.0.1.20(260421-테스트패널전체최소화버전연동)
- 대화 테스트창 좌상단 설정 패널에 전체 최소화 토글을 추가해, CSV URL/배경/파일 업로드/헤더 힌트까지 포함한 전체 영역을 한 번에 접고 펼칠 수 있도록 개선
- 기존 `대화 앵커/태그 설정` 섹션 단위 접기와 별개로 패널 전체 `열기/최소화` 상태를 제공해 화면 가림을 줄이고 테스트 몰입도를 높임
- 시작 화면 버전 버튼(`easterEgg`)은 고정 문자열 대신 `docs/99_변경이력.md`의 최신 `## ver...` 헤더를 fetch/파싱해 자동 표기하도록 변경
- 시작 화면/월드맵 fallback 버전을 `ver.0.1.20(260421-테스트패널전체최소화버전연동)`으로 갱신

## ver.0.1.19(260421-대화선택지한글헤더별칭보강)
- 테스트 대화창에서 선택지가 노출되지 않는 케이스를 재현/분석한 결과, CSV가 `선택지N라벨/선택지N장면/선택지N포인트`, `히든선택지N라벨/...` 형태를 사용할 때 기존 파서 별칭에 없어 선택지를 비우는 문제가 있음을 확인
- 대화 CSV 파서에 한글 별칭(`선택지N라벨`, `선택지N장면`, `선택지N포인트`, `히든선택지N라벨`, `히든선택지N장면`, `히든선택지N포인트`)을 추가해 실데이터 포맷 편차에서도 선택지 렌더링이 동작하도록 보강
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.19(260421-대화선택지한글헤더별칭보강)`로 갱신

## ver.0.1.18(260421-히든선택지유동csv지원)
- 선택지를 전부 버린 경우 `hiddenChoiceN*`가 정의되어 있으면 히든 선택지 세트를 노출하고, 히든 선택지가 없으면 기본 선택지 세트를 자동 복원하도록 분기 처리
- CSV 선택지 파서를 고정 3개 슬롯에서 동적 N개 슬롯으로 확장해 `choice1~choiceN`, `hiddenChoice1~hiddenChoiceN` 구조를 유동 개수로 지원
- 한글 헤더(`선택지N/분기N라벨`, `히든선택지N/히든분기N라벨`)도 동일하게 동적 인덱스 기반으로 처리
- 샘플 CSV를 4개 일반 선택지 + 히든 선택지 예시를 포함하도록 갱신하고, 대화 테스트 페이지의 힌트 문구도 신규 스키마로 업데이트
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.18(260421-히든선택지유동csv지원)`로 갱신

## ver.0.1.17(260421-대화선택지드래그디스카드연동)
- 대화 선택지 카드에 카드 템플릿 공용 인터랙션(`createCardFanBehavior`)을 연결해 드래그 이동/버리기(discard) 동작이 실제로 작동하도록 수정
- discard 오버레이/드롭존을 대화 프로토타입 내부 레이어로 추가하고, 드래그 중 near/hot 상태 시각 피드백을 시작 화면과 동일 톤으로 제공
- 선택지를 전부 버려 진행이 막히는 상황을 방지하기 위해 마지막 카드까지 discard한 경우 현재 선택지를 즉시 복원하도록 안전 처리
- 대화 테스트 페이지에서 `card_templates.js`를 선로드해 템플릿 인터랙션 API가 항상 활성화되도록 보강
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.17(260421-대화선택지드래그디스카드연동)`로 갱신

## ver.0.1.16(260421-대화선택지카드템플릿동일화)
- 대화 선택지 카드의 폭/비율/타이포/내부 face 구조를 시작 화면 카드 템플릿과 동일한 값으로 맞춰 크기와 밀도를 완전히 통일
- 선택지 카드에 `desc` 슬롯을 추가해 시작 화면 카드와 같은 레이아웃 계층을 유지하고, 분기 목적지(scene·anchor)를 보조 정보로 표시
- 모바일(760px/520px)에서도 시작 화면 카드와 같은 축소 규칙(`40vw/184px`, `42vw/168px`)을 적용
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.16(260421-대화선택지카드템플릿동일화)`로 갱신

## ver.0.1.15(260421-대화선택지카드정렬개선)
- 대화창 패널 높이/패딩을 줄여 화면 점유가 과하게 크지 않도록 조정하고, 모바일 높이도 함께 축소
- 대화 선택지를 패널 내부가 아닌 배경 중앙 상단 레이어로 분리해 대화창과 겹치지 않게 배치
- 선택지 버튼 마크업/스타일을 시작 화면 카드 템플릿(face/inner 구조)과 동일한 형태로 맞춰 시각 언어를 통일
- 대화 테스트 페이지에서 scene/tag + anchor 설정 영역을 `<details>`로 감싸 접기/펼치기 가능하도록 개선
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.15(260421-대화선택지카드정렬개선)`로 갱신

## ver.0.1.14(260421-대화전환연출강화)
- 대사 전환 시 패널/텍스트 진입 애니메이션을 추가해 넘김 피드백이 더 역동적으로 느껴지도록 개선
- 선택지 카드에 순차 등장 딜레이와 pop-in 모션을 적용해 분기 등장 연출을 강화
- 다음 버튼에 주기적 pulse 애니메이션을 넣어 진행 가능 상태를 더 직관적으로 안내
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.14(260421-대화전환연출강화)`로 갱신

## ver.0.1.13(260421-대화창반투명블러강화)
- 대화창 패널에 반투명 배경 + 강화된 backdrop blur(`blur(16px)`)를 적용해 뒷배경이 은은하게 비치도록 조정
- `-webkit-backdrop-filter`와 보조 오버레이 레이어(`::before`)를 추가해 브라우저별 블러 체감 차이를 완화
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.13(260421-대화창반투명블러강화)`로 갱신

## ver.0.1.12(260421-대화메타기본숨김)
- 대사창 상단의 scene/anchor 메타(`[intro] #end`)는 디버그 정보이므로 기본값에서 숨기도록 `showMeta=false` 옵션을 도입
- 필요 시 `createDialoguePrototype({ showMeta: true })`로 메타를 다시 표시할 수 있도록 유지해 디버그/실사용 모드를 분리
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.12(260421-대화메타기본숨김)`으로 갱신

## ver.0.1.11(260420-대화창고정팬선택지)
- 대화 선택지를 중앙 카드 팬(부채꼴) 배치로 조정해 기존 시작화면 카드 전개 느낌과 맞추도록 개선
- 대화창 패널 높이를 고정(`height`)으로 변경하고, 대사 영역에 내부 스크롤을 적용해 내용 길이와 무관하게 창 크기가 흔들리지 않도록 안정화
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.11(260420-대화창고정팬선택지)`로 갱신

## ver.0.1.10(260420-대화전체클릭카드선택지)
- 대화 프로토타입(`assets/js/dialogue_prototype.js`)에서 대사 진행 구간은 대화창 어느 영역을 클릭해도 다음 줄로 넘어가도록 입력 처리를 확장
- 선택지 구간은 실수 진행을 방지하기 위해 자유 클릭 진행을 막고, 선택지 카드 버튼 클릭으로만 분기하도록 보호 로직 추가
- 선택지 UI(`assets/css/dialogue_prototype.css`)를 화면 중앙 카드형 레이아웃으로 개편해 기존 프로젝트 카드 톤과 유사한 시각 언어로 통일
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.10(260420-대화전체클릭카드선택지)`로 갱신

## ver.0.1.9(260420-대화프로토타입주석보강)
- 대화 프로토타입 모듈(`assets/js/dialogue_prototype.js`)의 핵심 흐름(파싱/정규화/구간선택/분기점프/렌더링/API)에 설명 주석을 추가해 유지보수 가독성을 강화
- 시작화면 테스트 모드 진입 핸들러(`assets/js/start_screen.js`)에 동작 의도를 설명하는 주석을 보강
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.9(260420-대화프로토타입주석보강)`로 갱신

## ver.0.1.8(260420-대사분기선택지지원)
- CSV 대사에 분기 선택지 컬럼(`choice1Label/choice1Scene/choice1Anchor` ~ `choice3...`)을 추가해 대화 중 선택지 버튼으로 다른 scene/anchor 구간으로 점프할 수 있도록 확장
- `assets/js/dialogue_prototype.js`에 전체 레코드 기반 점프(`jumpTo`) 및 선택지 렌더링 로직을 추가하고, 선택된 대사의 scene/anchor 메타를 대화창에 함께 표시
- 임시 테스트 페이지(`dialogue_prototype_test.html`)와 샘플 CSV(`assets/data/dialogue_sample.csv`)를 분기 예시 데이터로 갱신
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.8(260420-대사분기선택지지원)`로 갱신

## ver.0.1.7(260420-테스트모드허브추가)
- 시작화면 우측 상단에 `TEST MODE` 진입 버튼을 추가하고, 테스트 허브 화면(`test_mode.html`)에서 기능별 테스트 항목을 선택할 수 있도록 구성
- 테스트 모드에는 현재 `대화창 테스트(dialogue_prototype_test.html)`와 `월드맵 생성 테스트(world_map.html)` 진입 카드를 제공
- 인덱스 진입점을 월드맵이 아닌 시작화면(`start_screen.html`)으로 변경
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.7(260420-테스트모드허브추가)`로 갱신

## ver.0.1.6(260420-대사태그앵커선택지원)
- CSV 대사 로딩 시 어떤 대사를 불러올지 지정할 수 있도록 `scene/tag` + `anchor` 선택 기능을 추가
- `assets/js/dialogue_prototype.js`에 세그먼트 선택기(`selectDialogueSegment`)를 도입하고 `createDialoguePrototype`/CSV 로더에 `sceneTag`, `anchorId` 옵션을 확장
- 임시 테스트 페이지(`dialogue_prototype_test.html`)에 scene/tag, anchor 입력 UI를 추가해 원하는 구간만 즉시 검증 가능하도록 개선
- 샘플 CSV(`assets/data/dialogue_sample.csv`)에 `scene`, `anchor` 컬럼을 추가하고, 시작 화면 버전 버튼/월드맵 fallback 버전을 `ver.0.1.6(260420-대사태그앵커선택지원)`로 갱신

## ver.0.1.5(260420-csv대사로드전환)
- 바이너리(.xlsx) 대신 텍스트 기반 CSV 대사 로딩으로 전환해 파일 호환성을 개선
- 임시 테스트 페이지(`dialogue_prototype_test.html`)를 CSV URL/CSV 파일 업로드 방식으로 수정하고 샘플 데이터를 `assets/data/dialogue_sample.csv`로 교체
- 대화 프로토타입 모듈(`assets/js/dialogue_prototype.js`)에 CSV 파서/URL 로더를 추가하고 배경 이미지는 기존처럼 컬럼(`backgroundUrl`/`배경`) 또는 기본 URL에서 불러오도록 유지
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.5(260420-csv대사로드전환)`로 갱신

## ver.0.1.4(260420-엑셀대사임시테스트페이지추가)
- 임시 검증용 화면 `dialogue_prototype_test.html`을 추가해 대화 프로토타입 모듈을 즉시 확인할 수 있도록 구성
- 대사 스크립트는 엑셀(.xlsx)에서 불러오도록 `assets/js/dialogue_prototype.js`에 Excel URL/파일 로더 API를 추가하고, 샘플 파일 `assets/data/dialogue_sample.xlsx`를 제공
- 배경 이미지는 기본 URL 또는 엑셀 행별 컬럼(`backgroundUrl`/`배경`)에서 불러오는 방식으로 확장
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.4(260420-엑셀대사임시테스트페이지추가)`로 갱신

## ver.0.1.3(260420-대화프로토타입모듈화)
- 프로토타입 전용 HTML 파일은 제거하고, 재사용 가능한 대화창 프로토타입 모듈(`assets/js/dialogue_prototype.js`) + 스타일(`assets/css/dialogue_prototype.css`)만 유지
- 모듈 API(`window.NewtheriaDialoguePrototype.createDialoguePrototype`)를 통해 임의의 mount 요소에 대화 UI를 동적으로 생성/제거할 수 있도록 구성
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.3(260420-대화프로토타입모듈화)`로 갱신

## ver.0.1.2(260420-대화프로토타입분리)
- 대화창은 시작 화면에 통합하지 않고, 별도 프로토타입 화면(`dialogue_prototype.html`)으로 분리
- 프로토타입 전용 스타일/스크립트(`assets/css/dialogue_prototype.css`, `assets/js/dialogue_prototype.js`)를 추가해 배경 이미지 로딩, 인물명/대사, 우하단 SKIP, 대사 순환(▼) 구성 제공
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.2(260420-대화프로토타입분리)`로 갱신

## ver.0.1.1(260420-시작화면대화창통합)
- 별도 HTML 파일 없이 `start_screen.html` 내부에 대화 레이어를 통합해 배경 이미지 로딩, 인물명/대사, 우하단 SKIP 버튼 구성을 한 화면에서 제공
- `assets/css/start_screen.css`, `assets/js/start_screen.js`에 대화 UI/로딩/대사 순환/스킵 로직을 합치고, SKIP 이후 기존 시작 카드 화면이 자연스럽게 노출되도록 연결
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.1.1(260420-시작화면대화창통합)`으로 갱신

## ver.0.0.104(260420-98버전롤백)
- 최근 변경(103~99)에서 추가된 프롤로그 화면/대화창/CSV 대사 로딩 구성을 제거하고, 시작 화면 흐름을 `ver.0.0.98` 동작 기준으로 롤백
- `새로운 여정` 진입 경로를 프롤로그가 아닌 기존 월드맵 진입 구조로 되돌리고 관련 파일(`prologue.html`, `assets/js/prologue.js`, `assets/js/dialogue_ui.js`, `assets/js/dialogue_repository.js`, `assets/data/dialogues.csv`, `assets/css/prologue.css`)을 정리
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.104(260420-98버전롤백)`으로 갱신

## ver.0.0.98(260420-시작별속도광량개별점멸)
- 시작 화면 배경의 글로우 별 레이어 밀도/반경을 하향해 별의 양과 크기를 줄이고, 드리프트 애니메이션 주기를 늘려 이동 속도를 완화
- 반짝임 레이어를 개별 DOM 별(`.twinkle-star`) 기반으로 재구성해 각 별마다 서로 다른 점멸 주기/지연 타이밍을 갖도록 변경
- 점멸 키프레임의 최소 광량을 `opacity: 0`으로 내려 반짝임 중 완전 암전 구간이 발생하도록 조정
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.98(260420-시작별속도광량개별점멸)`로 갱신

## ver.0.0.97(260420-리롤디스카드공용화)
- 카드 팬 리롤 시퀀스를 공용 API(`createCardFanReroll`)로 분리해 카드 뒤집기/수렴/회전/셔플/재배치/앞면 복귀 흐름을 재사용 가능하게 정리
- 디스카드 드롭 존 판정/표시 로직을 공용 컨트롤러(`createDiscardZoneController`)로 분리해 드래그 중 near/inside 판정, 표시/강조 클래스 토글을 공용화
- 카드 활성화 상태 토글 유틸(`setActiveCard`)을 추가하고 시작 화면에서 공용 API를 사용하도록 전환
- 시작 화면 버전 버튼을 `ver.0.0.97(260420-리롤디스카드공용화)`로 갱신

## ver.0.0.96(260420-카드롱프레스글로우동기화)
- 카드 호버/선택/드래그 글로우를 `.card-fan-card`가 아닌 뒤집힘 레이어 `.card-fan-card-inner::after`로 이동해, 롱프레스 뒤집기 시 글로우도 함께 회전하도록 동기화
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.96(260420-카드롱프레스글로우동기화)`로 갱신

## ver.0.0.95(260420-모바일탭포커스프록시추가)
- 모바일에서는 마우스 `hover`가 없으므로 카드 탭 선택 시 `applyCardTransforms(card)`를 함께 호출해 호버 대응 포커스(주변 카드 밀림/선택 카드 리프트)가 유지되도록 보완
- 모바일 드래그 시작 시에는 포커스 프록시 상태를 해제해 드래그 중 시각 상태가 겹치지 않도록 정리
- 카드 바깥 영역을 탭하면 모바일에서 `active`/포커스 프록시를 해제해, 데스크톱의 `mouseleave`와 유사한 해제 동작을 제공
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.95(260420-모바일탭포커스프록시추가)`로 갱신

## ver.0.0.94(260420-카드롱프레스뒤집기모바일튜닝)
- 시작 화면 카드 공용 인터랙션에 롱프레스 타이머를 추가해 카드를 길게 누르면 앞/뒷면이 토글되도록 확장하고, 롱프레스 직후 클릭 이벤트는 소비해 의도치 않은 선택 전환을 방지
- 포인터 이동량 기반 롱프레스 취소 허용치(`longPressMoveTolerancePx`)를 분리해 드래그 시작 전 미세 손떨림으로 뒤집기가 취소되지 않도록 안정화
- 모바일 뷰포트에서는 드래그 임계값을 높이고 롱프레스 시간을 단축해(짧게 꾹) 터치 환경에서 `탭=선택 / 꾹 누름=뒤집기 / 드래그=이동·버리기`가 더 명확히 구분되도록 조정
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.94(260420-카드롱프레스뒤집기모바일튜닝)`으로 갱신

## ver.0.0.93(260420-모바일우측드래그랙완화)
- 시작 화면 카드 드래그 렌더를 `requestAnimationFrame` 기반으로 전환해 포인터 이동 이벤트 폭주 시 transform 갱신 횟수를 프레임 단위로 제한
- 하단 discard 존 판정을 위해 포인터 이동마다 `getBoundingClientRect()`를 2회 호출하던 구조를 드래그 시작 시 캐시 기반 계산으로 변경해 모바일 경계 구간 레이아웃 스래싱을 완화
- 드래그 중 카드에 `will-change: transform`을 적용해 GPU 합성 힌트를 제공하고, 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.93(260420-모바일우측드래그랙완화)`로 갱신

## ver.0.0.92(260420-카드선택딤해제복구)
- 시작 화면 카드 선택 콜백에서 `menu.classList.add('selecting')`를 제거해, 클릭/포인터 업 이후 다른 카드가 지속적으로 반투명 처리되던 현상을 해소
- 선택된 카드 강조 스타일을 `.card-fan.selecting` 상태 의존에서 `.card-fan-card.active` 기반으로 변경해, 비활성 카드 딤 없이도 선택 카드 강조가 유지되도록 조정
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.92(260420-카드선택딤해제복구)`로 갱신

## ver.0.0.89(260420-시작화면카드전부버림시기본복원)
- 시작 화면에서 카드를 모두 버렸을 때 기본 카드 4장을 즉시 다시 생성하도록 복원 로직 추가
- 카드 인터랙션 재바인딩을 분리해 카드 재생성 시 드래그/선택 동작이 정상 유지되도록 조정
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.89(260420-시작화면카드전부버림시기본복원)`로 갱신

## ver.0.0.88(260420-카드하단드롭삭제)
- 카드 드래그 중 화면 전체에 반투명 딤 오버레이를 표시하고, 화면 하단에 `DISCARD`/휴지통 표시 드롭 구역이 나타나도록 시작 화면 UI를 확장
- 드래그한 카드를 하단 드롭 구역 위에서 놓으면 해당 카드를 팬에서 제거(삭제)하고, 구역 위에 진입했을 때는 강조 상태(`is-hot`)로 시각 피드백을 제공
- 카드 팬 공용 인터랙션(`createCardFanBehavior`)에 드래그 상태/이동/폐기 드롭 콜백을 추가해 화면별로 폐기 규칙을 주입할 수 있도록 확장
- 시작 화면 버전 버튼과 월드맵 fallback 버전을 `ver.0.0.88(260420-카드하단드롭삭제)`로 갱신

## ver.0.0.87(260420-카드클릭오버레이제외)
- 카드 선택 콜백(`onCardSelected`)에서 전체 화면 오버레이(`.overlay.play`)를 트리거하던 로직을 제거해, 카드 클릭 시 약 0.6~1초 동안 파란/청색 컨테이너가 덮이는 연출이 나타나지 않도록 조정
- 카드 버튼에 `-webkit-tap-highlight-color: transparent`를 추가해 모바일 탭 하이라이트 잔상 가능성을 완화
- 시작 화면 버전 버튼을 `ver.0.0.87(260420-카드클릭오버레이제외)`로 갱신

## ver.0.0.86(260420-카드글로우영역정렬)
- 시작 화면 카드 버튼(`.card-fan-card`)에 기본 패딩 제거(`padding: 0`)와 `box-sizing: border-box`를 적용해 클릭/선택 시 글로우(`box-shadow`)가 카드보다 좌우로 넓게 보이던 현상을 정렬
- 시작 화면 버전 버튼을 `ver.0.0.86(260420-카드글로우영역정렬)`로 갱신

## ver.0.0.85(260420-시작화면카드앵커조건부적용)
- 시작 화면 카드 중앙 앵커(`left: 50%` + 절반 폭 보정)를 전역이 아니라 모바일 구간(`max-width: 760px`)에서만 적용하도록 조정
- PC/와이드 화면에서는 기존처럼 좌측 기준 팬 배치를 유지하고, 모바일에서는 상황에 맞춰 중앙 기준 배치가 적용되도록 유동화
- 시작 화면 버전 버튼을 `ver.0.0.85(260420-시작화면카드앵커조건부적용)`로 갱신

## ver.0.0.84(260420-시작화면모바일카드위치유동화)
- 시작 화면 카드에 기준점(`left: 50%` + 카드 폭 절반 보정)을 적용해 모바일에서도 카드 팬이 화면 중앙을 기준으로 배치되도록 조정
- 카드 팬 배치 계산에서 뷰포트 폭과 카드 폭을 반영한 `responsiveFanGap`을 도입해 작은 화면에서는 카드 간격을 자동 축소하고 좌우 잘림을 완화
- 시작 화면 버전 버튼을 `ver.0.0.84(260420-시작화면모바일카드위치유동화)`로 갱신

## ver.0.0.83(260420-시작화면모바일가독성개선)
- 시작 화면 CSS에 모바일 전용 반응형 규칙(760px/520px 브레이크포인트)을 추가해 카드 간격/기울기/폰트/패딩을 화면 폭에 맞게 최적화
- 모바일 환경에서 세로 스크롤 및 안전 영역(safe-area) 여백을 허용해 하단 버전 버튼과 카드 콘텐츠가 잘리지 않도록 조정
- 시작 화면 스크립트에 뷰포트 기반 UI 튜닝을 적용해 모바일에서는 카드 호버 이동 폭/리프트/스케일을 완화하고 버전 표기를 `ver.0.0.83(260420-시작화면모바일가독성개선)`으로 갱신

## ver.0.0.82(260420-카드팬인터랙션공용화)
- `assets/js/card_templates.js`에 카드 팬 인터랙션 공용 API(`createCardFanBehavior`)를 추가해 드래그/호버/포커스/팬 배치/셔플 로직을 모듈화
- 시작 화면 스크립트에서 카드 행동 로직을 공용 API 호출 방식으로 전환해 다른 화면에서도 동일 인터랙션을 재사용 가능하게 정리
- 시작 화면 버전 버튼을 `ver.0.0.82(260420-카드팬인터랙션공용화)`로 갱신

## ver.0.0.81(260420-시작화면카드템플릿공용화)
- 시작 화면 카드 HTML 템플릿을 `assets/js/card_templates.js`로 분리해 공용 렌더 API(`window.NewtheriaCardTemplates`)를 추가
- `start_screen.html`의 카드 마크업을 비우고 공용 템플릿 스크립트를 로딩하도록 변경
- `start_screen.js`에서 카드 정의 데이터(`CARD_MENU_ITEMS`)를 통해 카드 DOM을 초기화하도록 갱신

# 99. 변경 이력

## ver.0.0.79(260415-월드정보팝업디자인개선)
- `월드 정보 보기` dialog를 브리핑 스타일로 재구성해 키커/부제/카드형 섹션(지도 요약, 달력 요약)으로 정보를 더 읽기 쉽게 정리
- 팝업 메타 텍스트를 한 줄 나열 방식에서 줄바꿈 기반 요약 포맷으로 전환해 시드/맵 크기/지형 분포/달력 정보를 스캔하기 쉽게 개선
- 시작 화면 이스터에그 버전 버튼 및 월드맵 fallback 버전을 `ver.0.0.79(260415-월드정보팝업디자인개선)`으로 갱신

## ver.0.0.78(260415-월드맵정보팝업파라미터확장)
- 월드맵 리롤 설정에 `지형 워프 강도`, `산맥 강도`, `강 생성 밀도` 슬라이더를 추가해 해양 비율/고도 강도 외에도 지형 패턴을 세밀하게 조절할 수 있도록 확장
- 월드맵 헤더의 달력/시드 메타 정보를 기본 화면에서 숨기고, `월드 정보 보기` 버튼으로 여는 팝업(`dialog`) 안에서만 확인하도록 UI를 정리
- 월드맵 버전 표기는 하드코딩 상수 대신 `docs/99_변경이력.md`의 최신 항목을 fetch/파싱해 표시하도록 변경하고, 시작 화면 버전 버튼도 `ver.0.0.78(260415-월드맵정보팝업파라미터확장)`으로 동기화

## ver.0.0.77(260415-산맥능선지형강화)
- 월드맵 고도 생성에 능선형 리지드 노이즈(`ridgedNoise`)를 추가해 산악 지형이 점 형태로 흩어지지 않고 긴 산맥 띠처럼 이어지도록 조정
- `HEX_CONFIG`에 산맥 주파수/강도 파라미터(`ridgeFrequency`, `ridgeDetailFrequency`, `ridgeStrength`)를 도입해 대륙 스케일 능선과 세부 산줄기를 동시에 제어 가능하게 구성
- 월드맵 버전 상수를 `ver.0.0.77(260415-산맥능선지형강화)`로 갱신

## ver.0.0.76(260415-턴진행규칙모드분리)
- 공용 달력 모듈(`assets/js/calendar.js`)에 턴 진행 API를 추가해 모드별 시간 단위를 표준화: 일반 월드맵(`1턴=1주`), 역사 모드(`1턴=1년`)
- `advanceTurn(dateObj, mode)`와 `TURN_MODE`, `getTurnRuleLabel(mode)`를 공개해 다른 화면/시뮬레이터에서도 동일 규칙을 재사용 가능하도록 정리
- 월드맵 달력 메타 영역에 현재 적용 턴 규칙 라벨을 함께 표시하고, 버전 표기를 `ver.0.0.76(260415-턴진행규칙모드분리)`으로 갱신

## ver.0.0.75(260415-공용달력모듈분리)
- 역법을 게임 전역에서 재사용할 수 있도록 공용 스크립트 `assets/js/calendar.js`를 신설하고, 달력 데이터/정규화/정보 계산 로직을 `window.NewtheriaCalendar`로 분리
- 월드맵 스크립트는 공용 달력 API를 참조하도록 변경해 화면 전용 중복 구현을 제거하고, 달력 모듈이 없을 때의 안전한 폴백 표시를 추가
- 월드맵 HTML에서 공용 달력 스크립트를 선로드하도록 구성하고, 시작 화면 버전 버튼 및 월드맵 버전을 `ver.0.0.75(260415-공용달력모듈분리)`로 갱신

## ver.0.0.74(260415-월드맵달력정보표시추가)
- 외부 스크립트의 달력 로직(절기/월 룬/주차 테마)을 월드맵 스크립트에 이식해 `getCalendarInfo` 유틸리티로 통합
- 월드맵 헤더에 달력 정보 표시 영역(`calendarMeta`)을 추가해 현재 게임 날짜(연/월/주)와 절기·월·주차 설명을 한 줄로 확인 가능하도록 구성
- 시작 화면 버전 버튼과 월드맵 버전 상수를 `ver.0.0.74(260415-월드맵달력정보표시추가)`로 동기화

## ver.0.0.71(260413-사막군집화지형명단순화)
- 고온·저습 지형에서 `사막오아시스` 분기 확률을 낮추고(해안 제외), `rebalanceBiomeDiversity`의 최소 출현 보정 대상에서 오아시스를 제외해 오아시스 권역이 과도하게 넓어지던 현상을 완화
- 사막 군집 보정 패스(`clusterAridTiles`)를 추가해 고립된 `건조사막` 타일은 주변 기후에 맞는 일반 지형으로 환원하고, 건조 인접 타일은 사막으로 묶어 사막 지형이 점묘처럼 끊기지 않도록 조정
- 일부 난해한 지형명을 더 직관적인 명칭으로 단순화(예: `고대용의둥지→용의둥지`, `빛나는수정동굴→수정동굴`, `얼어붙은툰드라→빙원`)하고 시작 화면/월드맵 버전 표기를 `ver.0.0.71(260413-사막군집화지형명단순화)`로 갱신

## ver.0.0.70(260410-붉은고원완화해양해안청색분리)
- 고원 분류에서 `heat=high & moisture=low` 조건이 항상 `붉은고원`으로 귀결되던 비율을 확률 분기로 축소해, 대부분 `구릉지`로 대체되도록 조정
- 바이옴 다양성 보정(`rebalanceBiomeDiversity`)의 최소 출현 대상에서 `붉은고원`을 제외하고, 승격 조건도 더 엄격하게 바꿔 붉은 계열 타일의 과도한 튐 현상을 완화
- 해양(`water`)·해안(`coast`) 그라데이션을 모두 하늘색/파란색 계열로 재정렬하되 명도 대역을 분리해 서로 구분이 잘 되도록 개선하고, 모래톤은 `모래해변`에만 제한 적용
- 시작 화면/월드맵 버전 표기를 `ver.0.0.70(260410-붉은고원완화해양해안청색분리)`으로 갱신

## ver.0.0.69(260410-월드맵지형대비해변색상개선)
- 월드맵 지형 계열별 그라데이션 시작/종료 색상을 재조정해 계열 간 대비를 높이고, 인접 타일이 지나치게 비슷하게 보이던 문제를 완화
- 해안 계열(`산호초해안/얕은해안/해안/모래해변`)에 따뜻한 모래 톤 혼합을 추가해 해변이 노란색 계열로 더 자연스럽게 보이도록 개선
- 지형 기본색(`HEX_CONFIG.terrains`)을 보조 틴트로 섞는 단계를 도입해 “지구 사진 같은” 해안-내륙 색 분리를 강화하고 시작 화면/월드맵 버전 표기를 `ver.0.0.69(260410-월드맵지형대비해변색상개선)`으로 갱신

## ver.0.0.68(260410-타일그라데이션강축소조정)
- 지형 계열별(수역/해안/초지/숲/습지/건조/산악/설원) 그라데이션 색상 시스템을 추가해 같은 계열은 자연스럽게 이어지고 계열 간 경계는 유지되도록 타일 색상을 재구성
- `구릉지` 포함 기본 지형이 단색으로 칙칙하게 보이던 문제를 완화하기 위해 고도·습도·온도 기반 혼합치로 색상 변주를 적용
- 강 생성에서 시도 수/최대 길이/내리막 허용치를 하향하고 강 예산(`riverBudget`) 최소값·계수를 축소해 과도하게 긴 강이 반복 형성되는 현상을 완화, 버전 표기를 `ver.0.0.68(260410-타일그라데이션강축소조정)`으로 갱신

## ver.0.0.67(260410-바이옴다양성균형화)
- 바이옴 분류 임계값을 고정값 중심에서 분위수(quantile) 기반 밴딩으로 전환해 시드마다 특정 바이옴으로 쏠리는 현상을 완화
- 고도·습도·온도 3축 밴드 조합에 확률 분기를 도입해 동일 조건에서도 복수 바이옴이 나오도록 다양성을 강화
- 1차 분류 후 최소 출현량 보정 패스를 추가해 희귀 바이옴(맹그로브습지/운무고원림/건조암봉산맥/빙하설산 등)이 맵에 안정적으로 등장하도록 조정하고 버전 표기를 `ver.0.0.67(260410-바이옴다양성균형화)`으로 갱신

## ver.0.0.66(260410-지형워프3축바이옴확장)
- 월드맵 스칼라 필드 샘플링에 도메인 워프를 도입해 해안선/기후 경계가 길게 펴지는 인공적인 선형 패턴을 완화
- 지형 분류를 고도·습도·온도 3축 기반으로 확장해 맹그로브습지/운무고원림/건조암봉산맥/고산열대운무림 등 고습온·고지대 변형 바이옴을 추가
- 고산 판정을 해수면 연동 임계값으로 조정해 산맥 출현감을 강화하고 시작 화면/월드맵 버전 표기를 `ver.0.0.66(260410-지형워프3축바이옴확장)`으로 갱신

## ver.0.0.65(260410-월드맵기후레이어팝업추가)
- 월드맵 상단에 레이어 버튼(지형/고도/습도/온도)을 추가하고, 선택한 항목만 시각화되도록 렌더 색상 모드를 분리
- 고도/습도/온도 레이어는 각각 값 기반 그라디언트 컬러맵으로 렌더링해 분포를 즉시 파악할 수 있도록 개선
- 헥스 타일 클릭 시 좌표·지형·고도·습도·온도 정보를 표시하는 팝업을 추가하고 버전 표기를 `ver.0.0.65(260410-월드맵기후레이어팝업추가)`로 갱신

## ver.0.0.63(260409-강축소지형복잡도강화)
- 강 생성량을 대폭 줄이기 위해 강 예산(`riverBudget`)을 하향 조정하고 수원지 조건(고도/습도)·흐름 길이·내리막 허용치 기준을 강화
- 고도/습도/온도 노이즈 주파수와 옥타브를 상향하고 `ruggedNoise`를 추가해 지형 디테일(굴곡/복잡도)을 강화
- 고도 스무딩 패스를 2회에서 1회로 줄여 미세 지형이 더 살아나도록 조정하고 버전 표기를 `ver.0.0.63(260409-강축소지형복잡도강화)`으로 갱신

## ver.0.0.62(260409-중앙대륙강제제거)
- 월드맵 고도 생성에서 중앙 대륙형 분포를 강제하던 방사형 `edgeMask` 적용을 제거
- 고도 필드는 펄린 노이즈 합성 결과를 직접 사용하고, 저주파 해양 산란 노이즈(`oceanScatter`)를 감산해 바다/육지가 맵 전역에 분산되도록 조정
- 시작 화면 이스터에그 버튼과 월드맵 버전 표기를 `ver.0.0.62(260409-중앙대륙강제제거)`으로 갱신

## ver.0.0.61(260409-펄린노이즈맵생성전환)
- 월드맵 생성에서 기존 판구조/도메인워프/침식 중심 프로세스를 제거하고 펄린 노이즈(fBm)만으로 고도·습도·온도 스칼라 필드를 생성하도록 단순화
- 생성된 3개 필드(고도/습도/온도)와 해수면/해안 인접 판정만으로 지형 타입을 분류하도록 월드 생성 흐름을 재구성
- 시작 화면 이스터에그 버튼과 월드맵 버전 표기를 `ver.0.0.61(260409-펄린노이즈맵생성전환)`으로 갱신

## ver.0.0.60(260409-시작화면별만조정)
- 시작 화면 배경에서 하단 지평선 글로우/성운 레이어를 제거하고 별/반짝임 레이어 중심으로 단순화
- 별 밀도·크기·색상 랜덤 생성 로직은 유지해 사진풍 별 배경 느낌은 유지하되 요청대로 별 연출만 남기도록 조정
- 시작 화면 이스터에그 버튼과 월드맵 버전 표기를 `ver.0.0.60(260409-시작화면별만조정)`으로 갱신

## ver.0.0.59(260409-시작화면별연출개선)
- 시작 화면 별 배경 레이어를 다층 구조(미세 별/큰 별/반짝임/성운/지평선 글로우)로 확장해 참고 이미지와 유사한 우주 분위기를 강화
- 랜덤 별 생성 파라미터(개수/반경/색상/투명도)를 재조정해 별 밀도 대비와 청록 포인트 별 표현을 개선
- 시작 화면 이스터에그 버튼과 월드맵 버전 표기를 `ver.0.0.59(260409-시작화면별연출개선)`으로 동기화

## ver.0.0.56(260331-월드맵렌더오류수정)
- 월드맵 생성 스무딩 단계에서 존재하지 않는 변수(`w`)를 참조하던 런타임 오류를 `weight`로 수정해 스크립트가 중단되지 않도록 복구
- 해당 오류로 캔버스 렌더링이 시작되지 않아 맵이 비어 보이던 현상을 해결
- 시작 화면/월드맵 버전 표기를 `ver.0.0.56(260331-월드맵렌더오류수정)`으로 갱신

## ver.0.0.50(260331-헥스해양편향수정)
- 월드 생성 결과가 해양으로 과도하게 치우치던 문제를 수정하기 위해 고도 분포 기반의 적응형 해수면(quantile) 계산 로직을 추가
- 적응형 해수면을 해양 거리 계산/지형 분류/강 흐름 판정에 공통 적용해 육지 비율이 안정적으로 확보되도록 개선
- 시작 화면/월드맵 버전 표기를 `ver.0.0.50(260331-헥스해양편향수정)`으로 갱신

## ver.0.0.49(260331-헥스판구조기후개선)
- 헥스 월드 생성을 판(plate) 기반 고도 분포 + 도메인 워핑 노이즈로 재구성해 대륙/해양 경계와 산맥대가 더 자연스럽게 형성되도록 개선
- 해양 거리 기반 습윤도 보정과 위도 기반 열량 분포를 결합해 사막/숲/습지/설산 등이 기후대처럼 보이도록 조정
- 월드맵 화면 상단에 버전 정보 표기(`ver.0.0.49(260331-헥스판구조기후개선)`)를 추가하고 시작 화면 버전 버튼도 동일 값으로 갱신

## ver.0.0.48(260331-헥스지형현실화)
- 헥스 월드 생성 로직을 완전 랜덤 분포에서 노이즈 기반 지형 생성으로 교체해 대륙/해양/위도 기반 기후가 반영되도록 조정
- 고도·수분·열량값을 바탕으로 지형을 분류하고, 해안/해변/산맥/설산/사막/숲 등의 분포가 자연스럽게 이어지도록 규칙을 재구성
- 고지대 수원에서 해수면으로 흐르는 하천(강) 조각 로직을 추가해 지형 연속성을 보강하고 버전 표기를 `ver.0.0.48(260331-헥스지형현실화)`로 갱신

## ver.0.0.47(260331-헥스월드맵초안)
- 헥스 타일 기반의 랜덤 월드맵 임시 화면(`world_map.html`)을 추가하고, 지상계 200x200 기준으로 총 40,000개 타일을 생성/렌더링하도록 구성
- 월드 규칙 명세의 타일 필드 표준(coord, terrainType, manaSaturation 등)에 맞춘 랜덤 타일 데이터 생성을 반영
- `index.html` 기본 진입점을 임시로 월드맵 화면으로 변경하고 시작 화면 버전 버튼 표기를 `ver.0.0.47(260331-헥스월드맵초안)`로 갱신

## ver.0.0.46(260331-카드3회전방향교차)
- 시작 화면 셔플 회전 연출을 카드당 3회전으로 복원
- 카드별 회전에서 1회전마다 방향이 번갈아 바뀌도록 키프레임(정방향→역방향→정방향)으로 조정
- 시작 화면 버전 버튼 표기를 `ver.0.0.46(260331-카드3회전방향교차)`로 갱신

## ver.0.0.45(260331-셔플카드회전방향교차)
- 시작 화면 셔플 회전량을 카드당 3회전(1080deg)에서 1회전(360deg)으로 조정
- 카드 인덱스 기준으로 회전 방향을 교차 적용해 시계/반시계가 번갈아 나타나도록 유지
- 시작 화면 버전 버튼 표기를 `ver.0.0.45(260331-셔플카드회전방향교차)`로 갱신

## ver.0.0.44(260331-시작화면스크립트모듈화)
- 시작 화면 스크립트를 `layout`/`effects`/`reroll`/`drag`/`bindEvents` 단위로 재구성해 역할별 책임을 분리
- 기존 카드 선택/드래그/셔플/리롤/별 배경 동작은 동일하게 유지하면서 코드 가독성과 유지보수성을 개선
- 시작 화면 버전 버튼 표기를 `ver.0.0.44(260331-시작화면스크립트모듈화)`로 갱신

## ver.0.0.43(260331-시작화면파일분리상수화)
- 시작 화면의 인라인 `<style>`/`<script>`를 각각 `assets/css/start_screen.css`, `assets/js/start_screen.js`로 분리
- 시작 화면 동작 관련 매직 넘버를 `UI` 상수 객체로 정리해 유지보수성을 개선(동작/타이밍 값은 기존과 동일 유지)
- 시작 화면 버전 버튼 표기를 `ver.0.0.43(260331-시작화면파일분리상수화)`로 갱신

## ver.0.0.38(260331-카드뒤집힘플래튼수정)
- 카드 뒤집기 축인 `.card-fan-card-inner`에서 `overflow: hidden`/`border-radius`를 제거해 3D 플래튼(flatten) 현상을 방지
- 카드 페이스(`.card-fan-card-face`)에 `overflow: hidden`을 적용해 모서리 클리핑은 유지하면서 뒷면 렌더링이 정상 노출되도록 조정
- 시작 화면 버전 버튼 표기를 `ver.0.0.38(260331-카드뒤집힘플래튼수정)`로 갱신

## ver.0.0.37(260331-카드뒷면에테리아디자인)
- 카드 뒷면이 앞면 반전처럼 보이던 문제를 완화하기 위해 카드 페이스에 `-webkit-backface-visibility: hidden`을 추가
- 카드 뒷면 디자인을 시질 아이콘(✦) + `AetheriA` 워드마크 중심의 테마 스타일로 교체
- 시작 화면 버전 버튼 표기를 `ver.0.0.37(260331-카드뒷면에테리아디자인)`로 갱신

## ver.0.0.36(260331-카드전체뒤집힘수정)
- 카드 외곽 프레임(배경/테두리/그림자) 스타일을 고정 컨테이너(`.card-fan-card`)에서 뒤집힘 레이어(`.card-fan-card-inner`, `.card-fan-card-face`)로 이동
- 리롤 뒤집기 시 카드 안쪽 면만 회전하던 문제를 수정해 카드 전체가 뒤집히는 것처럼 보이도록 개선
- 시작 화면 버전 버튼 표기를 `ver.0.0.36(260331-카드전체뒤집힘수정)`로 갱신

## ver.0.0.35(260331-카드셔플회전뒤집힘수정)
- 시작 화면 카드 내부 스핀/뒤집힘 레이어(`.card-fan-card-spin`, `.card-fan-card-inner`)에 `display: block`을 추가해 transform이 실제 적용되도록 수정
- 리롤 시 중앙 수렴 이후 카드 뒷면 뒤집힘과 카드별 회전 연출이 보이지 않던 원인을 해결
- 시작 화면 버전 버튼 표기를 `ver.0.0.35(260331-카드셔플회전뒤집힘수정)`로 갱신

## ver.0.0.34(260331-카드뒷면셔플전반전)
- 시작 화면 리롤 시퀀스에서 카드들을 셔플 동작 이전에 먼저 뒷면으로 뒤집도록 단계 순서를 조정
- 셔플/재전개 단계에서는 카드 뒷면 상태를 유지하고, 마지막에 앞면으로 순차 복귀하도록 연출을 명확화
- 시작 화면 버전 버튼 표기를 `ver.0.0.34(260331-카드뒷면셔플전반전)`로 갱신

## ver.0.0.30(260329-카드팬선택지리뉴얼)
- 시작 화면 카드 선택지를 `card-fan` 기반 구조(앞/뒷면, 내부 스핀 레이어 포함)로 재구성
- 셔플 단계에서 카드 본체 이동(transform)과 카드 페이스 뒤집기(transform)를 분리해 애니메이션 꼬임을 완화
- 버전 표기를 `ver.0.0.30(260329-카드팬선택지리뉴얼)`로 갱신

## ver.0.0.29(260329-시작화면카드셔플안정화)
- 시작 화면 카드 셔플 시퀀스의 타이밍을 조정해 수렴/회전/재전개 흐름을 더 안정적으로 보이도록 개선
- 셔플 단계에서 카드 배열을 실제로 무작위 재배치하도록 수정해 연출과 결과가 일치하도록 개선
- 시작 화면 버전 버튼 표기를 `ver.0.0.29(260329-시작화면카드셔플안정화)`로 갱신

## ver.0.0.27(260329-시작화면버전표시개선)
- 시작 화면 카드 배경을 완전 불투명으로 조정
- 좌하단 "카드 베이스: 황금비(1:1.618) · Fan 배치" 문구 제거
- 우하단 텍스트를 `ver.0.0.27(260329-시작화면버전표시개선)` 형식의 버전 정보 버튼으로 교체하고, 모바일에서도 잘리지 않도록 정렬/여백/글자 크기 조정
- 버전 정보 버튼 클릭 시 셔플(리롤) 연출은 기존과 동일하게 유지

## ver.0.0.26(260329-시작화면버전22롤백)
- 시작 화면 구현을 `ver.0.0.22` 상태(황금비 카드 구성/팬 배치/기본 셔플 연출)로 되돌림
- `ver.0.0.23`~`ver.0.0.25`에서 추가된 카드 드래그·고정 셔플·카드 페이스 확대 변경을 롤백

## ver.0.0.25(260329-카드페이스크기보강)
- 시작 화면 카드 전체 폭을 추가 상향하고 카드 앞면/뒷면 요소 크기(텍스트·문양·중앙 원형)를 키워 체감 크기를 보강

## ver.0.0.24(260329-카드크기복원)
- 시작 화면 카드 크기를 기존 체감 크기로 복원(데스크톱/모바일 모두 상향)

## ver.0.0.23(260329-시작화면셔플드래그개선)
- 시작 화면 카드 배경을 불투명 스타일로 조정하고 카드 뒷면에 심플 문양을 추가
- 셔플 연출을 고정 시퀀스(중앙 수렴 → 뭉치 뒤집힘 → 카드별 3회 회전 → 위치 복귀 → 순차 뒤집힘)로 변경
- 카드 상시 드래그(드래그 추종, 해제 시 원위치 복귀), 별자리 라인 연출, 모바일 우하단 버전 텍스트 노출 개선 반영

## ver.0.0.22(260329-시작화면황금비카드리뉴얼)
- 버전 표기 정책을 변경해 `99_변경이력.md`에만 버전 정보를 유지하고, 나머지 문서의 버전 라인을 제거
- 시작 화면 카드를 가로:세로 1:1.618(황금비) 기반 세로형 카드로 재구성하고 팬(Fan) 배치로 정렬
- 카드 셔플/리롤 베이스 연출(수렴→뒤집힘→셔플→재전개)을 시작 화면 이스터에그에 반영

## ver.0.0.21(260329-인덱스진입페이지추가)
- 루트 기본 진입점을 위한 `index.html` 문서 추가
- `index.html`에서 `start_screen.html`로 즉시 이동하도록 메타 리다이렉트와 스크립트 리다이렉트를 구성
- 시작 화면 규칙 문서에 인덱스 기본 진입 동작을 명시

## ver.0.0.19(260329-별자리라인연출추가)
- 시작 화면 배경에 별자리 라인(점 연결) 드로우/페이드 연출 추가
- `12_시작화면연출규칙.md`에 별자리 라인 생성/소멸 규칙 및 옵션 추가
- 마스터 문서 시작화면 인터페이스 요약에 별자리 라인 연출 반영

## ver.0.0.18(260329-시작화면카드연출추가)
- 시작 화면 UX 규칙 문서 `12_시작화면연출규칙.md` 추가
- 메인 카드 선택지 4종, 별 배경 확산 전환, 버전 클릭 셔플 이스터에그 정의
- 마스터/인덱스에 시작 화면 시스템 연계 반영

## ver.0.0.17(260327-저장불러오기모드시스템추가)
- 저장/불러오기 시스템 규칙 문서 `10_저장불러오기규칙.md` 추가
- 모드 설치/스크립트 훅/권한/샌드박스 규칙 문서 `11_모드시스템규칙.md` 추가
- 마스터/연계맵/인덱스에 저장·모드 시스템 의존관계 반영

## ver.0.0.16(260326-문서리팩토링)
- 마스터 문서를 상세복제형에서 연계/우선순위 중심 구조로 리팩토링
- `09_시스템연계맵.md` 문서 신설(도메인 간 의존 관계/데이터 오너십 정리)
- 문서 인덱스를 운영/코어/확장/연계 기준으로 재정렬

## ver.0.0.15(260326-문서체계재정렬)
- 문서 체계를 코어/세부/확장 기준으로 재정렬
- `98_문서운영가이드.md` 문서 신설(읽기순서/수정우선순위/충돌해결 규칙)
- `docs/README.md`를 카테고리형 인덱스로 개편

## ver.0.0.14(260326-카드선택팬리롤연출추가)
- 카드 선택지 UI를 팬(Fan) 형식으로 배치하는 규칙 추가
- 리롤 시 중앙 수렴 → 뒤집힘 → 셔플 → 재전개 연출 시퀀스 추가
- `08_카드선택연출규칙.md` 문서 신설

## ver.0.0.13(260326-캐릭터레이어렌더링추가)
- 캐릭터 부위별 레이어 합성 렌더링 규칙 추가
- 그레이스케일 원본 + 런타임 틴트 방식 정책 추가
- `07_캐릭터레이어렌더링규칙.md` 문서 신설

## ver.0.0.12(260326-달력시스템추가)
- 계절/월/주 기반 세계관 달력 시스템 추가
- `06_달력규칙.md` 문서 신설 및 룬/절기/주차 테마 반영
- 마스터 문서에 달력 시스템 섹션 추가

## ver.0.0.11(260326-연령필드제거프로필정리)
- 캐릭터 프로필에서 연령(age) 필드 제거
- 프로필 허용 필드를 비성적 정보 중심으로 재정리
- 캐릭터 프로필 문서 및 마스터 문서의 안전 기준 문구 정리

## ver.0.0.10(260326-캐릭터프로필확장안전기준)
- 캐릭터 프로필 확장 필드(가문/성별/나이/종족/키/몸무게 등) 추가
- 프로필 전용 문서 `05_캐릭터프로필규칙.md` 신설
- 연령 포함 시스템에 맞춘 비성적 안전 기준 명시

## ver.0.0.9(260326-클래스리스핵심스탯체계개편)
- 고정 직업 제거(클래스리스) 규칙 추가
- 대표 스탯 6개 + 파생 스탯 체계 추가
- 스탯/행동 이력 기반 스킬 학습 규칙 추가

## ver.0.0.8(260326-정착지레벨대칭행동규칙추가)
- 플레이어/NPC 행위 대칭 원칙 추가(전략/전투 공통)
- 야영지 특수 규칙 추가(인구수만 보유)
- 정착지 레벨 시스템 및 승급 조건(레벨+조건) 추가

## ver.0.0.7(260326-특수타일규칙추가)
- 세계수/드래곤 둥지/고대 성소/미스릴 광산 특수 타일 규칙 추가
- 세계수 숲 둘러싸임 규칙 추가
- 드래곤 둥지 최고 고도 생성 규칙 추가
- 고대 성소 이벤트 생성(최초 성소 도시 폐허화) 규칙 추가
- 미스릴 광산 산맥 제한 및 드래곤 둥지 최소거리 규칙 추가

## ver.0.0.6(260326-레포문서초안반영)
- 기획 내용을 레포지터리 문서 구조로 분리
- 지상계 우선 정책 명문화
- 정착지 단계를 5단계로 정리
- 수도를 정착지 단계와 분리한 국가 속성으로 정리
